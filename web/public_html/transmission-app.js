/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./style/transmission-app.scss":
/*!******************************************************************************************************************!*\
  !*** ./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./style/transmission-app.scss ***!
  \******************************************************************************************************************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../node_modules/css-loader/dist/runtime/cssWithMappingToString.js */ "./node_modules/css-loader/dist/runtime/cssWithMappingToString.js");
/* harmony import */ var _node_modules_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../node_modules/css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js");
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../node_modules/css-loader/dist/runtime/getUrl.js */ "./node_modules/css-loader/dist/runtime/getUrl.js");
/* harmony import */ var _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _images_toolbar_folder_png__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./images/toolbar-folder.png */ "./style/images/toolbar-folder.png");
/* harmony import */ var _images_toolbar_close_png__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./images/toolbar-close.png */ "./style/images/toolbar-close.png");
/* harmony import */ var _images_toolbar_start_png__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./images/toolbar-start.png */ "./style/images/toolbar-start.png");
/* harmony import */ var _images_toolbar_pause_png__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./images/toolbar-pause.png */ "./style/images/toolbar-pause.png");
/* harmony import */ var _images_toolbar_info_png__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./images/toolbar-info.png */ "./style/images/toolbar-info.png");
/* harmony import */ var _images_three_dots_vertical_svg__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./images/three-dots-vertical.svg */ "./style/images/three-dots-vertical.svg");
/* harmony import */ var _images_blue_turtle_png__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./images/blue-turtle.png */ "./style/images/blue-turtle.png");
/* harmony import */ var _images_chevron_up_svg__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./images/chevron-up.svg */ "./style/images/chevron-up.svg");
/* harmony import */ var _images_chevron_down_svg__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./images/chevron-down.svg */ "./style/images/chevron-down.svg");
/* harmony import */ var _images_audio_x_generic_png__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./images/audio-x-generic.png */ "./style/images/audio-x-generic.png");
/* harmony import */ var _images_folder_png__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./images/folder.png */ "./style/images/folder.png");
/* harmony import */ var _images_application_x_executable_png__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./images/application-x-executable.png */ "./style/images/application-x-executable.png");
/* harmony import */ var _images_font_x_generic_png__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./images/font-x-generic.png */ "./style/images/font-x-generic.png");
/* harmony import */ var _images_image_x_generic_png__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./images/image-x-generic.png */ "./style/images/image-x-generic.png");
/* harmony import */ var _images_package_x_generic_png__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./images/package-x-generic.png */ "./style/images/package-x-generic.png");
/* harmony import */ var _images_text_x_generic_png__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./images/text-x-generic.png */ "./style/images/text-x-generic.png");
/* harmony import */ var _images_video_x_generic_png__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./images/video-x-generic.png */ "./style/images/video-x-generic.png");
/* harmony import */ var _images_pause_circle_idle_svg__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./images/pause-circle-idle.svg */ "./style/images/pause-circle-idle.svg");
/* harmony import */ var _images_pause_circle_active_svg__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./images/pause-circle-active.svg */ "./style/images/pause-circle-active.svg");
/* harmony import */ var _images_play_circle_idle_svg__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./images/play-circle-idle.svg */ "./style/images/play-circle-idle.svg");
/* harmony import */ var _images_play_circle_active_svg__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! ./images/play-circle-active.svg */ "./style/images/play-circle-active.svg");
/* harmony import */ var _images_analytics_svg__WEBPACK_IMPORTED_MODULE_24__ = __webpack_require__(/*! ./images/analytics.svg */ "./style/images/analytics.svg");
/* harmony import */ var _images_team_svg__WEBPACK_IMPORTED_MODULE_25__ = __webpack_require__(/*! ./images/team.svg */ "./style/images/team.svg");
/* harmony import */ var _images_diagram_3_fill_svg__WEBPACK_IMPORTED_MODULE_26__ = __webpack_require__(/*! ./images/diagram-3-fill.svg */ "./style/images/diagram-3-fill.svg");
/* harmony import */ var _images_files_svg__WEBPACK_IMPORTED_MODULE_27__ = __webpack_require__(/*! ./images/files.svg */ "./style/images/files.svg");
/* harmony import */ var _images_up_and_down_arrows_svg__WEBPACK_IMPORTED_MODULE_28__ = __webpack_require__(/*! ./images/up-and-down-arrows.svg */ "./style/images/up-and-down-arrows.svg");
/* harmony import */ var _images_checkered_flag_svg__WEBPACK_IMPORTED_MODULE_29__ = __webpack_require__(/*! ./images/checkered-flag.svg */ "./style/images/checkered-flag.svg");
/* harmony import */ var _images_router_svg__WEBPACK_IMPORTED_MODULE_30__ = __webpack_require__(/*! ./images/router.svg */ "./style/images/router.svg");
/* harmony import */ var _images_lock_fill_svg__WEBPACK_IMPORTED_MODULE_31__ = __webpack_require__(/*! ./images/lock-fill.svg */ "./style/images/lock-fill.svg");
/* harmony import */ var _images_horizontal_rule_svg__WEBPACK_IMPORTED_MODULE_32__ = __webpack_require__(/*! ./images/horizontal-rule.svg */ "./style/images/horizontal-rule.svg");
/* harmony import */ var _images_turtle_png__WEBPACK_IMPORTED_MODULE_33__ = __webpack_require__(/*! ./images/turtle.png */ "./style/images/turtle.png");
/* harmony import */ var _images_logo_png__WEBPACK_IMPORTED_MODULE_34__ = __webpack_require__(/*! ./images/logo.png */ "./style/images/logo.png");
// Imports



































var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default()));
var ___CSS_LOADER_URL_REPLACEMENT_0___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_toolbar_folder_png__WEBPACK_IMPORTED_MODULE_3__.default);
var ___CSS_LOADER_URL_REPLACEMENT_1___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_toolbar_close_png__WEBPACK_IMPORTED_MODULE_4__.default);
var ___CSS_LOADER_URL_REPLACEMENT_2___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_toolbar_start_png__WEBPACK_IMPORTED_MODULE_5__.default);
var ___CSS_LOADER_URL_REPLACEMENT_3___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_toolbar_pause_png__WEBPACK_IMPORTED_MODULE_6__.default);
var ___CSS_LOADER_URL_REPLACEMENT_4___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_toolbar_info_png__WEBPACK_IMPORTED_MODULE_7__.default);
var ___CSS_LOADER_URL_REPLACEMENT_5___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_three_dots_vertical_svg__WEBPACK_IMPORTED_MODULE_8__.default);
var ___CSS_LOADER_URL_REPLACEMENT_6___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_blue_turtle_png__WEBPACK_IMPORTED_MODULE_9__.default);
var ___CSS_LOADER_URL_REPLACEMENT_7___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_chevron_up_svg__WEBPACK_IMPORTED_MODULE_10__.default);
var ___CSS_LOADER_URL_REPLACEMENT_8___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_chevron_down_svg__WEBPACK_IMPORTED_MODULE_11__.default);
var ___CSS_LOADER_URL_REPLACEMENT_9___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_audio_x_generic_png__WEBPACK_IMPORTED_MODULE_12__.default);
var ___CSS_LOADER_URL_REPLACEMENT_10___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_folder_png__WEBPACK_IMPORTED_MODULE_13__.default);
var ___CSS_LOADER_URL_REPLACEMENT_11___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_application_x_executable_png__WEBPACK_IMPORTED_MODULE_14__.default);
var ___CSS_LOADER_URL_REPLACEMENT_12___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_font_x_generic_png__WEBPACK_IMPORTED_MODULE_15__.default);
var ___CSS_LOADER_URL_REPLACEMENT_13___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_image_x_generic_png__WEBPACK_IMPORTED_MODULE_16__.default);
var ___CSS_LOADER_URL_REPLACEMENT_14___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_package_x_generic_png__WEBPACK_IMPORTED_MODULE_17__.default);
var ___CSS_LOADER_URL_REPLACEMENT_15___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_text_x_generic_png__WEBPACK_IMPORTED_MODULE_18__.default);
var ___CSS_LOADER_URL_REPLACEMENT_16___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_video_x_generic_png__WEBPACK_IMPORTED_MODULE_19__.default);
var ___CSS_LOADER_URL_REPLACEMENT_17___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_pause_circle_idle_svg__WEBPACK_IMPORTED_MODULE_20__.default);
var ___CSS_LOADER_URL_REPLACEMENT_18___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_pause_circle_active_svg__WEBPACK_IMPORTED_MODULE_21__.default);
var ___CSS_LOADER_URL_REPLACEMENT_19___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_play_circle_idle_svg__WEBPACK_IMPORTED_MODULE_22__.default);
var ___CSS_LOADER_URL_REPLACEMENT_20___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_play_circle_active_svg__WEBPACK_IMPORTED_MODULE_23__.default);
var ___CSS_LOADER_URL_REPLACEMENT_21___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_analytics_svg__WEBPACK_IMPORTED_MODULE_24__.default);
var ___CSS_LOADER_URL_REPLACEMENT_22___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_team_svg__WEBPACK_IMPORTED_MODULE_25__.default);
var ___CSS_LOADER_URL_REPLACEMENT_23___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_diagram_3_fill_svg__WEBPACK_IMPORTED_MODULE_26__.default);
var ___CSS_LOADER_URL_REPLACEMENT_24___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_files_svg__WEBPACK_IMPORTED_MODULE_27__.default);
var ___CSS_LOADER_URL_REPLACEMENT_25___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_up_and_down_arrows_svg__WEBPACK_IMPORTED_MODULE_28__.default);
var ___CSS_LOADER_URL_REPLACEMENT_26___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_checkered_flag_svg__WEBPACK_IMPORTED_MODULE_29__.default);
var ___CSS_LOADER_URL_REPLACEMENT_27___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_router_svg__WEBPACK_IMPORTED_MODULE_30__.default);
var ___CSS_LOADER_URL_REPLACEMENT_28___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_lock_fill_svg__WEBPACK_IMPORTED_MODULE_31__.default);
var ___CSS_LOADER_URL_REPLACEMENT_29___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_horizontal_rule_svg__WEBPACK_IMPORTED_MODULE_32__.default);
var ___CSS_LOADER_URL_REPLACEMENT_30___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_turtle_png__WEBPACK_IMPORTED_MODULE_33__.default);
var ___CSS_LOADER_URL_REPLACEMENT_31___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(_images_logo_png__WEBPACK_IMPORTED_MODULE_34__.default);
// Module
___CSS_LOADER_EXPORT___.push([module.id, ":root {\n  --color-bg-even: #fff;\n  --color-bg-hover: #f6f8fa;\n  --color-bg-menu: #e1e4e8;\n  --color-bg-odd: #f6f8fa;\n  --color-bg-selection-1: #e6dcfd;\n  --color-bg-selection-2: #f5f0ff;\n  --color-border: #959da5;\n  --color-fg-disabled: #959da5;\n  --color-fg-error: #d73a49;\n  --color-fg-name: #2f363d;\n  --color-fg-port-closed: #d73a49;\n  --color-fg-port-open: #28a745;\n  --color-progressbar-background-1: #f6f8fa;\n  --color-progressbar-background-2: #959da5;\n  --color-progressbar-verify-1: #f692ce;\n  --color-progressbar-verify-2: #b93a86;\n  --color-progressbar-magnet-1: #ffea7f;\n  --color-progressbar-magnet-2: #dbab09;\n  --color-progressbar-magnet-paused-1: #fff5b1;\n  --color-progressbar-magnet-paused-2: #6a737d;\n  --color-progressbar-leech-paused-1: #c8e1ff;\n  --color-progressbar-leech-paused-2: #6a737d;\n  --color-progressbar-leech-queued-1: #dbedff;\n  --color-progressbar-leech-queued-2: #79b8ff;\n  --color-progressbar-leech-1: #79b8ff;\n  --color-progressbar-leech-2: #044289;\n  --color-progressbar-seed-paused-1: #bef5cb;\n  --color-progressbar-seed-paused-2: #6a737d;\n  --color-progressbar-seed-queued-1: #dcffe4;\n  --color-progressbar-seed-queued-2: #85e89d;\n  --color-progressbar-seed-1: #85e89d;\n  --color-progressbar-seed-2: #176f2c;\n  --color-tab-deselected-1: #f6f8fa;\n  --color-tab-deselected-2: #959da5;\n  --color-tab-selected-1: #e6dcfd;\n  --color-tab-selected-2: #f5f0ff;\n  --color-toolbar-background: linear-gradient(#d1d5da, #959da5);\n}\n@media (prefers-color-scheme: dark) {\n  :root {\n    --color-fg-primary: #f6f8fa;\n    --color-fg-secondary: #959da5;\n    --color-fg-tertiary: #6a737d;\n  }\n}\n@media (prefers-color-scheme: light) {\n  :root {\n    --color-fg-primary: #2f363d;\n    --color-fg-secondary: #6a737d;\n    --color-fg-tertiary: #959da5;\n  }\n}\n\nhtml,\nbody {\n  color: var(--color-fg-primary);\n  font-family: Verdana, Arial, Helvetica, sans-serif;\n  height: 100%;\n  margin: 0;\n  overflow: hidden;\n}\n\nimg {\n  border: 0;\n}\n\na {\n  outline: 0;\n}\n\n.hidden {\n  display: none;\n}\n\n.even {\n  background-color: var(--color-bg-even);\n}\n\n.odd {\n  background-color: var(--color-bg-odd);\n}\n\n.mainwin {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  position: relative;\n}\n.mainwin > * {\n  margin: 0;\n  padding: 0;\n}\n\n.mainwin-workarea {\n  display: flex;\n  flex: 1;\n  flex-direction: row;\n  overflow: auto;\n}\n\n#mainwin-toolbar {\n  align-items: center;\n  background: var(--color-toolbar-background);\n  border-bottom: 1px solid var(--color-border);\n  display: flex;\n  flex-direction: row;\n  height: 36px;\n  margin: 0;\n  padding: 2px;\n  width: 100%;\n}\n#mainwin-toolbar button {\n  background-color: transparent;\n  background-position: center;\n  background-repeat: no-repeat;\n  border: 0;\n  cursor: pointer;\n  height: 36px;\n  margin-right: 6px;\n  user-select: none;\n  width: 36px;\n}\n#mainwin-toolbar button:hover:not(:disabled) {\n  background-color: var(--color-bg-hover);\n}\n#mainwin-toolbar button:disabled {\n  cursor: default;\n  opacity: 0.25;\n}\n#mainwin-toolbar > .toolbar-separator {\n  border-left: 1px solid var(--color-border);\n  height: 25px;\n  margin: 0 6px 0 0;\n}\n#mainwin-toolbar :nth-last-child(2) {\n  border: 0;\n  flex-grow: 1;\n}\n\n#toolbar-open {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_0___ + ");\n}\n\n#toolbar-remove {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_1___ + ");\n}\n\n#toolbar-start {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_2___ + ");\n}\n\n#toolbar-pause {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_3___ + ");\n}\n\n#toolbar-inspector {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_4___ + ");\n}\n\n#toolbar-overflow {\n  background-color: transparent;\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_5___ + ");\n  background-position: center;\n  background-size: 26px;\n  margin-right: 4px;\n  width: 36px;\n}\n#toolbar-overflow.alt-speed-enabled {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_6___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_5___ + ");\n  background-position: bottom left, center, center;\n  background-size: 16px 9px, 26px, 26px;\n}\n\n.mainwin-filterbar {\n  align-items: center;\n  background: var(--color-toolbar-background);\n  border-bottom: 1px solid var(--color-border);\n  display: flex;\n  flex-direction: row;\n  font-size: smaller;\n  width: 100%;\n}\n@media (max-width: 599px) {\n  .mainwin-filterbar > :not(select):not(input) {\n    display: none;\n  }\n  .mainwin-filterbar > input {\n    min-width: 50px;\n  }\n  .mainwin-filterbar > input,\n.mainwin-filterbar > select {\n    height: 100%;\n  }\n}\n.mainwin-filterbar > * {\n  padding: 0 5px;\n}\n\n#torrent-search.blur {\n  color: var(--color-fg-tertiary);\n}\n\n.speed-up-icon {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_7___ + ");\n  background-repeat: no-repeat;\n  background-size: 12px 12px;\n  height: 12px;\n  width: 12px;\n}\n\n.speed-dn-icon {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_8___ + ");\n  background-repeat: no-repeat;\n  background-size: 12px 12px;\n  height: 12px;\n  width: 12px;\n}\n\n.flex {\n  flex-grow: 1;\n}\n\n#torrent-container {\n  flex-grow: 1;\n  left: 0;\n  margin: 0;\n  overflow: auto;\n  -webkit-overflow-scrolling: touch;\n  padding: 0;\n  right: 0;\n}\n\n.torrent-container {\n  background-color: #fff;\n}\n\n.torrent-list {\n  cursor: pointer;\n  list-style-type: none;\n  margin: 0;\n  padding: 0;\n  text-align: left;\n  width: 100%;\n}\n.torrent-list .torrent {\n  border-bottom: 1px solid var(--color-border);\n  color: var(--color-fg-secondary);\n  user-select: none;\n}\n.torrent-list .torrent.paused {\n  color: var(--color-fg-disabled);\n}\n.torrent-list .torrent .icon {\n  background-position: center, top left;\n}\n.torrent-list .torrent .icon[data-icon-mime-type=audio] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_9___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=audio][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_9___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=application] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_11___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=application][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_11___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=font] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_12___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=font][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_12___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=image] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_13___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=image][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_13___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=model] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_14___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=model][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_14___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=text] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_15___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=text][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_15___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=video] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_16___ + ");\n}\n.torrent-list .torrent .icon[data-icon-mime-type=video][data-icon-multifile=true] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_16___ + "), url(" + ___CSS_LOADER_URL_REPLACEMENT_10___ + ");\n}\n.torrent-list .torrent.compact {\n  align-items: center;\n  display: flex;\n  flex-direction: row-reverse;\n}\n.torrent-list .torrent.compact .icon {\n  background-size: 16px, 8px;\n  flex-shrink: 0;\n  height: 16px;\n  width: 16px;\n}\n.torrent-list .torrent.compact > * {\n  margin: 4px 5px;\n}\n.torrent-list .torrent:not(.compact) {\n  align-items: center;\n  display: grid;\n  grid-column-gap: 12px;\n  grid-template-areas: \"icon name\" \"icon peers\" \"icon progressbar\" \"icon progress-text\";\n  grid-template-columns: 32px 1fr;\n  padding: 2px 12px;\n}\n.torrent-list .torrent:not(.compact) .icon {\n  background-size: 32px, 16px;\n  grid-area: icon;\n  height: 32px;\n  width: 32px;\n}\n.torrent-list .torrent:not(.compact) .torrent-name {\n  grid-area: name;\n}\n.torrent-list .torrent:not(.compact) .torrent-peer-details {\n  grid-area: peers;\n}\n.torrent-list .torrent:not(.compact) .torrent-progress {\n  display: flex;\n  flex-direction: row;\n  grid-area: progressbar;\n}\n.torrent-list .torrent:not(.compact) .torrent-progress-details {\n  grid-area: progress-text;\n}\n.torrent-list .torrent:not(.compact) > * {\n  margin: 1px 0;\n}\n.torrent-list .torrent.selected {\n  background-color: var(--color-bg-selection-1);\n}\n.torrent-list .torrent.selected .torrent-progress-details.error, .torrent-list .torrent.selected .torrent-peer-details.error {\n  color: #fff;\n}\n.torrent-list .icon {\n  background-position: center;\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-pauseresume-button {\n  background-position: center center;\n  background-repeat: no-repeat;\n  background-size: 14px;\n  margin-left: 7px;\n  width: 14px;\n}\n.torrent-list .torrent-pauseresume-button[data-action=pause] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_17___ + ");\n}\n.torrent-list .torrent-pauseresume-button[data-action=pause]:active, .torrent-list .torrent-pauseresume-button[data-action=pause]:hover {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_18___ + ");\n}\n.torrent-list .torrent-pauseresume-button[data-action=resume] {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_19___ + ");\n}\n.torrent-list .torrent-pauseresume-button[data-action=resume]:active, .torrent-list .torrent-pauseresume-button[data-action=resume]:hover {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_20___ + ");\n}\n.torrent-list .torrent-progress-details.error,\n.torrent-list .torrent-peer-details.error {\n  color: var(--color-fg-error);\n}\n.torrent-list .torrent-name {\n  font-weight: bold;\n  margin-bottom: 2px;\n  margin-top: 2px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.torrent-list .torrent-name.compact {\n  flex: 1;\n  font-size: 1em;\n  font-weight: normal;\n}\n.torrent-list .torrent-name:not(.paused) {\n  color: var(--color-fg-name);\n}\n.torrent-list .torrent-progress-details,\n.torrent-list .torrent-peer-details {\n  font-size: x-small;\n}\n.torrent-list .torrent-progress-bar {\n  background-repeat: no-repeat;\n  border-radius: 3px;\n  border-style: solid;\n  border-width: 1px;\n  height: 12px;\n}\n.torrent-list .torrent-progress-bar.full {\n  flex-grow: 1;\n  margin: 2px 0;\n}\n.torrent-list .torrent-progress-bar.compact {\n  min-width: 50px;\n  width: 50px;\n}\n.torrent-list .torrent-progress-bar.leech.paused {\n  background: linear-gradient(to bottom, var(--color-progressbar-leech-paused-1), var(--color-progressbar-leech-paused-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.leech.queued {\n  background: linear-gradient(to bottom, var(--color-progressbar-leech-queued-1), var(--color-progressbar-leech-queued-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.leech {\n  background: linear-gradient(to bottom, var(--color-progressbar-leech-1), var(--color-progressbar-leech-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.magnet.paused {\n  background: linear-gradient(to bottom, var(--color-progressbar-magnet-paused-1), var(--color-progressbar-magnet-paused-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.magnet {\n  background: linear-gradient(to bottom, var(--color-progressbar-magnet-1), var(--color-progressbar-magnet-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.seed.paused {\n  background: linear-gradient(to bottom, var(--color-progressbar-seed-paused-1), var(--color-progressbar-seed-paused-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.seed.queued {\n  background: linear-gradient(to bottom, var(--color-progressbar-seed-queued-1), var(--color-progressbar-seed-queued-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.seed {\n  background: linear-gradient(to bottom, var(--color-progressbar-seed-1), var(--color-progressbar-seed-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n.torrent-list .torrent-progress-bar.verify {\n  background: linear-gradient(to bottom, var(--color-progressbar-verify-1), var(--color-progressbar-verify-2) 80%), linear-gradient(to bottom, var(--color-progressbar-background-1), var(--color-progressbar-background-2) 80%);\n  background-repeat: no-repeat;\n}\n\n#inspector-tab-info {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_21___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#inspector-tab-info:active, #inspector-tab-info:checked, #inspector-tab-info.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_21___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#inspector-tab-peers {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_22___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#inspector-tab-peers:active, #inspector-tab-peers:checked, #inspector-tab-peers.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_22___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#inspector-tab-tiers {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_23___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#inspector-tab-tiers:active, #inspector-tab-tiers:checked, #inspector-tab-tiers.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_23___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#inspector-tab-files {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_24___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#inspector-tab-files:active, #inspector-tab-files:checked, #inspector-tab-files.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_24___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n@media (max-width: 599px) {\n  #inspector,\n#prefs-dialog {\n    left: 0;\n  }\n}\n@media (min-width: 600px) {\n  #inspector,\n#prefs-dialog {\n    border-left: 1px solid var(--color-fg-secondary);\n    width: 570px;\n  }\n}\n\n#prefs-dialog {\n  background: #fff;\n  bottom: 0;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  position: absolute;\n  right: 0;\n  top: 61px;\n  z-index: 2;\n}\n#prefs-dialog .tabs-page {\n  grid-column-gap: 8px;\n  grid-row-gap: 5px;\n  grid-template-columns: 1fr 1fr;\n  margin: 20px;\n}\n#prefs-dialog .tabs-page:not(.hidden) {\n  display: grid;\n}\n#prefs-dialog .tabs-page .section-label {\n  font-weight: bold;\n  grid-column: span 2;\n  margin-left: -20px;\n  padding-bottom: 5px;\n}\n#prefs-dialog .tabs-page .section-label:not(:first-of-type) {\n  margin-top: 20px;\n}\n#prefs-dialog .alt-speed-section-label {\n  background: transparent url(" + ___CSS_LOADER_URL_REPLACEMENT_6___ + ") no-repeat;\n  background-position: left 4px;\n  padding-left: 22px;\n}\n#prefs-dialog :disabled,\n#prefs-dialog .disabled {\n  color: var(--color-fg-disabled);\n}\n#prefs-dialog #alt-times-div,\n#prefs-dialog #autostart-div,\n#prefs-dialog #port-forwarding,\n#prefs-dialog #randomize-port,\n#prefs-dialog #suffix-div,\n#prefs-dialog #use-dht-div,\n#prefs-dialog #use-lpd-div,\n#prefs-dialog #use-pex-div,\n#prefs-dialog #utp-enabled,\n#prefs-dialog .alt-speed-label {\n  grid-column: span 2;\n}\n#prefs-dialog .blocklist-size-label,\n#prefs-dialog .blocklist-update-button,\n#prefs-dialog .port-status {\n  grid-column: 2/3;\n}\n#prefs-dialog .blocklist-size-label {\n  font-size: smaller;\n}\n#prefs-dialog .blocklist-size-number {\n  font-weight: bolder;\n}\n#prefs-dialog .port-status-label {\n  display: inline-block;\n  font-weight: bold;\n  margin-left: 5px;\n}\n#prefs-dialog .port-status-label[data-open=true] {\n  color: var(--color-fg-port-open);\n}\n#prefs-dialog .port-status-label[data-open=false] {\n  color: var(--color-fg-port-closed);\n}\n#prefs-dialog .alt-speed-label {\n  font-size: smaller;\n  font-style: lighter;\n  margin: -10px 0 10px -20px;\n}\n\n#prefs-dialog.ui-tabs .ui-tabs-panel {\n  padding: 0;\n  user-select: none;\n}\n\n.prefs-section {\n  text-align: left;\n}\n.prefs-section > * {\n  padding: 0 8px 8px;\n}\n.prefs-section .title {\n  font-size: larger;\n  font-weight: bold;\n  padding-left: 0;\n}\n.prefs-section .row .key {\n  float: left;\n  padding-top: 3px;\n}\n.prefs-section .row .key > * {\n  margin-left: 0;\n}\n.prefs-section .row .value {\n  margin-left: 150px;\n}\n.prefs-section .row .value > * {\n  width: 100%;\n}\n.prefs-section .checkbox-row > input {\n  margin: 0;\n}\n.prefs-section .checkbox-row > label {\n  margin-left: 5px;\n}\n.prefs-section #alternative-speed-limits-title {\n  background: transparent url(" + ___CSS_LOADER_URL_REPLACEMENT_6___ + ") no-repeat;\n  padding-left: 18px;\n}\n.prefs-section #alternative-speed-limits-desc {\n  font-size: smaller;\n  padding-bottom: 4px;\n}\n\n.tabs-container {\n  align-items: stretch;\n  background: #fff;\n  border-left: solid 1px var(--color-border);\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  position: absolute;\n  right: 0;\n  top: 61px;\n  z-index: 2;\n}\n@media (max-width: 599px) {\n  .tabs-container {\n    left: 0;\n  }\n}\n@media (min-width: 600px) {\n  .tabs-container {\n    width: 550px;\n  }\n}\n\n.tabs-buttons {\n  align-self: center;\n  display: flex;\n  padding: 10px;\n}\n.tabs-buttons button {\n  border: 1px solid var(--color-border);\n  cursor: pointer;\n  display: inline-block;\n  padding: 3px;\n  user-select: none;\n  appearance: none;\n  padding: 3px;\n}\n\n.file-priority-radiobox :first-child,\n.tabs-buttons :first-child {\n  border-bottom-left-radius: 8px;\n  border-top-left-radius: 8px;\n}\n.file-priority-radiobox :last-child,\n.tabs-buttons :last-child {\n  border-bottom-right-radius: 8px;\n  border-top-right-radius: 8px;\n}\n\n.tabs-pages {\n  box-sizing: border-box;\n  flex: 1;\n  overflow-x: hidden;\n  overflow-y: auto;\n}\n@media (max-width: 599px) {\n  .tabs-pages {\n    padding: 0 8px 20px;\n  }\n}\n@media (min-width: 600px) {\n  .tabs-pages {\n    padding: 0 20px 20px;\n  }\n}\n\n.tabs-button {\n  background-position: center;\n  background-repeat: no-repeat;\n}\n\n#inspector {\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n}\n\n.inspector-info-page {\n  grid-column-gap: 8px;\n  grid-row-gap: 5px;\n  grid-template-columns: auto 1fr;\n  margin: 20px;\n  padding: 12px;\n}\n.inspector-info-page:not(.hidden) {\n  display: grid;\n}\n.inspector-info-page .section-label {\n  font-weight: bold;\n  grid-column: span 2;\n  margin-left: -20px;\n  padding-bottom: 5px;\n}\n.inspector-info-page .section-label:not(:first-of-type) {\n  margin-top: 20px;\n}\n.inspector-info-page :not(.section-label) {\n  overflow-x: hidden;\n  text-overflow: ellipsis;\n}\n\n#inspector .tabs-button,\n#prefs-dialog .tabs-button {\n  background-size: 20px 20px, 40px 30px;\n  height: 30px;\n  margin: 0;\n  padding: 0;\n  width: 40px;\n}\n\n#prefs-tab-torrent {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_25___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#prefs-tab-torrent:active, #prefs-tab-torrent:checked, #prefs-tab-torrent.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_25___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#prefs-tab-speed {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_26___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#prefs-tab-speed:active, #prefs-tab-speed:checked, #prefs-tab-speed.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_26___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#prefs-tab-peers {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_22___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#prefs-tab-peers:active, #prefs-tab-peers:checked, #prefs-tab-peers.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_22___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#prefs-tab-network {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_27___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n#prefs-tab-network:active, #prefs-tab-network:checked, #prefs-tab-network.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_27___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n#inspector-file-list {\n  margin: 0;\n  padding: 0;\n}\n#inspector-file-list .inspector-torrent-file-list-entry {\n  display: grid;\n  grid-column-gap: 5px;\n  grid-template-areas: \"check   name  priority\" \"blank1  info  blank2\";\n  grid-template-columns: 20px 1fr 65px;\n  margin-bottom: 4px;\n  padding: 5px;\n}\n#inspector-file-list .inspector-torrent-file-list-entry.skip {\n  opacity: 0.5;\n}\n#inspector-file-list .file-wanted-control {\n  grid-area: check;\n}\n#inspector-file-list .inspector-torrent-file-list-entry-name {\n  color: var(--color-fg-name);\n  cursor: pointer;\n  grid-area: name;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n#inspector-file-list .inspector-torrent-file-list-entry.skip > .inspector-torrent-file-list-entry-name {\n  color: var(--color-fg-disabled);\n}\n#inspector-file-list .inspector-torrent-file-list-entry-progress {\n  color: var(--color-fg-secondary);\n  grid-area: info;\n}\n#inspector-file-list .single-file .inspector-torrent-file-list-entry > .file-wanted-control,\n#inspector-file-list .inspector-torrent-file-list-entry.complete > .file-wanted-control {\n  cursor: default;\n}\n\n#inspector-header {\n  margin: 8px 0;\n}\n\n#torrent-inspector-name {\n  font-size: large;\n  font-weight: bold;\n}\n\n#inspector-tiers-list {\n  color: var(--color-fg-secondary);\n  margin: 0 10px;\n  padding: 0 12px;\n}\n#inspector-tiers-list .tier-list-row {\n  display: grid;\n  grid-column-gap: 8px;\n  grid-row-gap: 4px;\n  margin-top: 8px;\n}\n#inspector-tiers-list .tier-list-torrent {\n  color: var(--color-fg-primary);\n  font-size: larger;\n  font-weight: bolder;\n  margin-left: -12px;\n  overflow-x: hidden;\n  padding-top: 20px;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n#inspector-tiers-list .tier-list-tracker {\n  font-weight: bolder;\n  grid-column: span 2;\n  margin-top: 8px;\n  padding-bottom: 4px;\n}\n#inspector-tiers-list .tier-announce,\n#inspector-tiers-list .tier-scrape,\n#inspector-tiers-list .tier-state {\n  text-align: left;\n}\n#inspector-tiers-list .tier-downloads,\n#inspector-tiers-list .tier-leechers,\n#inspector-tiers-list .tier-seeders {\n  text-align: right;\n}\n\n.peer-list {\n  border: 1px solid #f6f8fa;\n  border-collapse: collapse;\n  cursor: default;\n  table-layout: fixed;\n  text-align: left;\n  width: 100%;\n}\n.peer-list td,\n.peer-list th {\n  font-size: smaller;\n  padding: 5px;\n}\n.peer-list td {\n  border: 1px solid #f6f8fa;\n  color: var(--color-fg-secondary);\n}\n.peer-list .torrent-row td {\n  background: #f6f8fa;\n  color: var(--color-fg-primary);\n  font-size: normal;\n  font-weight: bolder;\n  overflow-x: hidden;\n  padding: 10px;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.peer-list .encryption {\n  width: 16px;\n}\n.peer-list .encryption[data-encrypted=true] {\n  background: transparent url(" + ___CSS_LOADER_URL_REPLACEMENT_28___ + ") center center no-repeat;\n  height: 16px;\n  width: 16px;\n}\n.peer-list .peer-address,\n.peer-list .percent-done,\n.peer-list .speed-down,\n.peer-list .speed-up {\n  text-align: right;\n}\n.peer-list .percent-done {\n  width: 10%;\n}\n.peer-list .speed-down,\n.peer-list .speed-up {\n  width: 15%;\n}\n.peer-list .peer-address {\n  width: 25%;\n}\n.peer-list .peer-app-name {\n  overflow-x: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n@media (max-width: 599px) {\n  .peer-list .peer-app-name {\n    display: none;\n  }\n}\n@media (min-width: 600px) {\n  .peer-list .peer-app-name {\n    width: 25%;\n  }\n}\n.peer-list .status {\n  cursor: pointer;\n}\n\n.file-priority-radiobox {\n  grid-area: priority;\n}\n.file-priority-radiobox > * {\n  border: 1px solid var(--color-border);\n  cursor: pointer;\n  display: inline-block;\n  padding: 3px;\n  user-select: none;\n  appearance: none;\n  background-position: center;\n  background-repeat: no-repeat;\n  background-size: 10px, 20px;\n  height: 20px;\n  margin: 0;\n  padding: 0;\n  width: 20px;\n}\n.file-priority-radiobox > .low {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_8___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n  border-right-width: 0;\n}\n.file-priority-radiobox > .low:active, .file-priority-radiobox > .low:checked, .file-priority-radiobox > .low.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_8___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n.file-priority-radiobox > .normal {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_29___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n}\n.file-priority-radiobox > .normal:active, .file-priority-radiobox > .normal:checked, .file-priority-radiobox > .normal.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_29___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n.file-priority-radiobox > .high {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_7___ + "), linear-gradient(var(--color-tab-deselected-1), var(--color-tab-deselected-2));\n  border-left-width: 0;\n}\n.file-priority-radiobox > .high:active, .file-priority-radiobox > .high:checked, .file-priority-radiobox > .high.selected {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_7___ + "), linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n}\n\n.context-menu {\n  background: var(--color-bg-menu);\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  position: absolute;\n  z-index: 2;\n}\n.context-menu .context-menuitem {\n  list-style: none;\n  padding: 5px 10px;\n}\n.context-menu .context-menuitem:hover:not(:disabled), .context-menu .context-menuitem:focus:not(:disabled) {\n  background-image: linear-gradient(#fff, var(--color-bg-menu));\n  cursor: pointer;\n}\n.context-menu .context-menuitem:disabled {\n  color: var(--color-fg-disabled);\n  cursor: none;\n}\n\n.context-menu-separator {\n  border-bottom: 1px solid var(--color-fg-secondary);\n  margin-bottom: 10px;\n  padding-bottom: 10px;\n}\n\n.overflow-menu {\n  background: linear-gradient(160deg, #d21, 1%, var(--color-bg-hover), 40%, var(--color-bg-menu));\n  border-radius: 8px;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  padding: 20px;\n  position: fixed;\n  width: 200px;\n  z-index: 2;\n}\n.overflow-menu fieldset {\n  border: 0;\n  margin-bottom: 8px;\n  padding: 0;\n}\n.overflow-menu > .actions,\n.overflow-menu > .info,\n.overflow-menu > .links {\n  display: flex;\n  flex-direction: column;\n}\n.overflow-menu legend {\n  font-weight: bolder;\n  margin-bottom: 4px;\n}\n.overflow-menu legend ~ * {\n  margin-left: 12px;\n}\n.overflow-menu a,\n.overflow-menu button,\n.overflow-menu label {\n  color: var(--color-fg-primary);\n  font-size: 1em;\n}\n.overflow-menu a:disabled,\n.overflow-menu button:disabled,\n.overflow-menu label:disabled {\n  color: var(--color-fg-disabled);\n  cursor: default;\n}\n.overflow-menu a:focus:not(:disabled), .overflow-menu a:hover:not(:disabled),\n.overflow-menu button:focus:not(:disabled),\n.overflow-menu button:hover:not(:disabled),\n.overflow-menu label:focus:not(:disabled),\n.overflow-menu label:hover:not(:disabled) {\n  background-color: var(--color-bg-hover);\n}\n.overflow-menu a,\n.overflow-menu button {\n  background: transparent;\n  border: 0;\n  cursor: pointer;\n  display: block;\n  padding: 4px 0;\n  text-align: left;\n  text-decoration: none;\n  width: 150px;\n}\n.overflow-menu .input-and-label {\n  align-items: center;\n  display: inline-flex;\n  flex-direction: row;\n}\n.overflow-menu #display-options > * {\n  align-items: center;\n  display: inline-flex;\n  flex-direction: row;\n}\n.overflow-menu #display-options > * > * {\n  margin: 4px;\n}\n.overflow-menu #display-options > * :first-child {\n  margin-left: 0;\n}\n.overflow-menu #display-options > * :last-child {\n  margin-right: 0;\n}\n.overflow-menu #speed-options {\n  display: flex;\n  flex-direction: column;\n}\n.overflow-menu #speed-options > .speed-up,\n.overflow-menu #speed-options > .speed-down {\n  align-items: center;\n  display: flex;\n  flex-direction: row;\n  padding: 4px 0;\n}\n.overflow-menu #speed-options > .speed-up > label,\n.overflow-menu #speed-options > .speed-down > label {\n  overflow: hidden;\n  width: 80px;\n}\n.overflow-menu #speed-options .alt-speed {\n  display: grid;\n  grid-column-gap: 5px;\n  grid-row-gap: 5px;\n  grid-template-areas: \"check   lb\" \"turtle  values\";\n  grid-template-columns: 20px 1fr;\n  margin-bottom: 4px;\n}\n.overflow-menu #speed-options #alt-speed-check {\n  grid-area: check;\n}\n.overflow-menu #speed-options #alt-speed-check:checked ~ #alt-speed-image {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_6___ + ");\n}\n.overflow-menu #speed-options #alt-speed-check:not(:checked) ~ #alt-speed-image {\n  background-image: url(" + ___CSS_LOADER_URL_REPLACEMENT_30___ + ");\n}\n.overflow-menu #speed-options #alt-speed-image {\n  background-position: center;\n  background-repeat: no-repeat;\n  grid-area: turtle;\n}\n.overflow-menu #speed-options #alt-speed-label {\n  grid-area: lb;\n}\n.overflow-menu #speed-options #alt-speed-values-label {\n  font-size: small;\n  grid-area: values;\n}\n\n@media (min-width: 600px) {\n  #display-fullscreen-check,\n#display-fullscreen-label {\n    display: none;\n  }\n}\n@media (max-width: 599px) {\n  .dialog-buttons {\n    padding-top: 13.3333333333px;\n  }\n\n  .dialog-container {\n    opacity: 96%;\n    position: absolute;\n    top: 61px;\n    width: 100%;\n  }\n\n  .dialog-logo {\n    padding-bottom: 13.3333333333px;\n  }\n\n  .dialog-window {\n    align-items: center;\n    display: flex;\n    flex-direction: column;\n  }\n}\n@media (min-width: 600px) {\n  .dialog-container {\n    left: 50%;\n    min-width: 400px;\n    position: absolute;\n    top: 122px;\n    transform: translateX(-50%);\n  }\n\n  .dialog-heading {\n    grid-area: heading;\n  }\n\n  .dialog-logo {\n    grid-area: icon;\n    padding-right: 13.3333333333px;\n  }\n\n  .dialog-window {\n    display: grid;\n    grid-column-gap: 12px;\n    grid-template-areas: \"icon heading\" \"icon message\" \"icon workarea\" \"icon buttons\";\n    grid-template-columns: 64px 1fr;\n    padding: 2px 12px;\n  }\n}\n.dialog-buttons {\n  display: flex;\n  float: right;\n  grid-area: buttons;\n  margin: 10px 0 0;\n  text-align: center;\n}\n.dialog-buttons button {\n  appearance: none;\n  background: transparent;\n  border: 1px solid var(--color-border);\n  border-radius: 5px;\n  color: var(--color-fg-name);\n  margin-left: 8px;\n  padding: 8px;\n  text-decoration: none;\n}\n.dialog-buttons a:hover,\n.dialog-buttons a:active {\n  background: linear-gradient(#fff, var(--color-bg-menu));\n}\n\n.dialog-buttons-begin {\n  flex-grow: 1;\n}\n\n.dialog-buttons-end {\n  display: none;\n}\n\n.dialog-container {\n  border: 0;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  color: var(--color-fg-primary);\n  display: block;\n  margin: 0;\n  padding: 0;\n  z-index: 2;\n}\n\n.dialog-heading {\n  align-items: center;\n  display: flex;\n  font-size: 1.2em;\n  font-weight: bold;\n}\n\n.dialog-logo {\n  background: transparent url(" + ___CSS_LOADER_URL_REPLACEMENT_31___ + ") top left no-repeat;\n  height: 64px;\n  width: 64px;\n}\n\n.dialog-message {\n  grid-area: message;\n  margin: 10px 0 0;\n}\n\n.dialog-window {\n  background: linear-gradient(160deg, #d21, 1%, #f7f7f7, 40%, #ccc);\n  height: 100%;\n  padding: 20px;\n}\n\n.dialog-workarea {\n  display: flex;\n  flex-direction: column;\n  grid-area: workarea;\n  margin: 10px 0 0;\n}\n.dialog-workarea > * {\n  margin-bottom: 5px;\n}\n\n.about-dialog .dialog-workarea > * {\n  margin-bottom: 10px;\n}\n\n.about-dialog-version-number {\n  margin-left: 4px;\n}\n\n.about-dialog-version-checksum {\n  font-size: small;\n  font-style: italic;\n  font-weight: normal;\n  margin-left: auto;\n  padding-left: 10px;\n}\n\n.shortcuts-dialog table {\n  border-collapse: collapse;\n  width: 100%;\n}\n.shortcuts-dialog th,\n.shortcuts-dialog td {\n  border: 1px solid var(--color-border);\n  padding: 5px 10px;\n}\n\n#torrent-rename-name {\n  min-width: 400px;\n}\n\n#move-container #torrent-path {\n  min-width: 400px;\n}\n\n.tabs-pages .content,\n.inspector-info-page .content,\n.statistics-dialog .content {\n  column-gap: 16px;\n  display: grid;\n  grid-row-gap: 6px;\n  grid-template-columns: auto 1fr;\n  margin-top: 4px;\n}\n@media (max-width: 599px) {\n  .tabs-pages .content,\n.inspector-info-page .content,\n.statistics-dialog .content {\n    margin: 4px 0 16px;\n  }\n}\n@media (min-width: 600px) {\n  .tabs-pages .content,\n.inspector-info-page .content,\n.statistics-dialog .content {\n    margin: 4px 16px 16px;\n  }\n}\n.tabs-pages .content div,\n.inspector-info-page .content div,\n.statistics-dialog .content div {\n  word-break: break-word;\n}\n.tabs-pages fieldset,\n.inspector-info-page fieldset,\n.statistics-dialog fieldset {\n  border: 0;\n  margin: 0;\n  padding: 0;\n}\n.tabs-pages legend,\n.inspector-info-page legend,\n.statistics-dialog legend {\n  font-weight: bolder;\n  margin-bottom: 4px;\n}\n\n#free-space-text {\n  display: inline-block;\n  float: right;\n  font-size: smaller;\n  font-style: italic;\n}\n\n#torrent-upload-frame {\n  border: 0;\n  display: block;\n  /* Don't change this : safari forms won't target hidden frames (they open a new window) */\n  height: 0;\n  left: -1000px;\n  margin: 0;\n  padding: 0;\n  position: absolute;\n  top: -1000px;\n  width: 0;\n}\n\n.ui-menu {\n  width: 200px;\n}", "",{"version":3,"sources":["webpack://./style/transmission-app.scss"],"names":[],"mappings":"AA6FA;EACE,qBAAA;EACA,yBAAA;EACA,wBAAA;EACA,uBAAA;EACA,+BAAA;EACA,+BAAA;EACA,uBAAA;EACA,4BAAA;EACA,yBAAA;EACA,wBAAA;EACA,+BAAA;EACA,6BAAA;EAWA,yCAAA;EACA,yCAAA;EACA,qCAAA;EACA,qCAAA;EACA,qCAAA;EACA,qCAAA;EACA,4CAAA;EACA,4CAAA;EACA,2CAAA;EACA,2CAAA;EACA,2CAAA;EACA,2CAAA;EACA,oCAAA;EACA,oCAAA;EACA,0CAAA;EACA,0CAAA;EACA,0CAAA;EACA,0CAAA;EACA,mCAAA;EACA,mCAAA;EACA,iCAAA;EACA,iCAAA;EACA,+BAAA;EACA,+BAAA;EACA,6DAAA;AAtGF;AAoEE;EAbF;IAcI,2BAAA;IACA,6BAAA;IACA,4BAAA;EAjEF;AACF;AAkEE;EAlBF;IAmBI,2BAAA;IACA,6BAAA;IACA,4BAAA;EA/DF;AACF;;AA2FA;;EAEE,8BAAA;EACA,kDAAA;EACA,YAAA;EACA,SAAA;EACA,gBAAA;AAxFF;;AA2FA;EACE,SAAA;AAxFF;;AA2FA;EACE,UAAA;AAxFF;;AA2FA;EACE,aAAA;AAxFF;;AA2FA;EACE,sCAAA;AAxFF;;AA0FA;EACE,qCAAA;AAvFF;;AA0FA;EACE,aAAA;EACA,sBAAA;EACA,YAAA;EACA,kBAAA;AAvFF;AAyFE;EACE,SAAA;EACA,UAAA;AAvFJ;;AA2FA;EACE,aAAA;EACA,OAAA;EACA,mBAAA;EACA,cAAA;AAxFF;;AAgGA;EACE,mBAAA;EACA,2CAAA;EACA,4CAAA;EACA,aAAA;EACA,mBAAA;EACA,YARe;EASf,SAAA;EACA,YAAA;EACA,WAAA;AA7FF;AA+FE;EACE,6BAAA;EACA,2BAAA;EACA,4BAAA;EACA,SAAA;EACA,eAAA;EACA,YAnBa;EAoBb,iBAAA;EACA,iBAAA;EACA,WAtBa;AAvEjB;AA+FI;EACE,uCAAA;AA7FN;AAgGI;EACE,eAAA;EACA,aAAA;AA9FN;AAkGE;EACE,0CAAA;EACA,YAAA;EACA,iBAAA;AAhGJ;AAmGE;EACE,SAAA;EACA,YAAA;AAjGJ;;AAqGA;EACE,yDAAA;AAlGF;;AAqGA;EACE,yDAAA;AAlGF;;AAqGA;EACE,yDAAA;AAlGF;;AAqGA;EACE,yDAAA;AAlGF;;AAqGA;EACE,yDAAA;AAlGF;;AAqGA;EACE,6BAAA;EACA,yDAAA;EACA,2BAAA;EACA,qBAAA;EACA,iBAAA;EACA,WAAA;AAlGF;AAoGE;EACE,kGAAA;EACA,gDAAA;EACA,qCAAA;AAlGJ;;AA2GA;EACE,mBAAA;EACA,2CAAA;EACA,4CAAA;EACA,aAAA;EACA,mBAAA;EACA,kBAAA;EAEA,WAAA;AAzGF;AA/HE;EA2OE;IACE,aAAA;EAzGJ;EA2GE;IACE,eAAA;EAzGJ;EA2GE;;IAEE,YAAA;EAzGJ;AACF;AA4GE;EACE,cAAA;AA1GJ;;AA+GE;EACE,+BAAA;AA5GJ;;AAkHA;EACE,yDAAA;EACA,4BAAA;EACA,0BAAA;EACA,YANgB;EAOhB,WAPgB;AAxGlB;;AAkHA;EACE,yDAAA;EACA,4BAAA;EACA,0BAAA;EACA,YAdgB;EAehB,WAfgB;AAhGlB;;AA0HA;EACE,YAAA;AAvHF;;AA0HA;EACE,YAAA;EACA,OAAA;EACA,SAAA;EACA,cAAA;EACA,iCAAA;EACA,UAAA;EACA,QAAA;AAvHF;;AA0HA;EACE,sBAxUM;AAiNR;;AA0HA;EAWE,eAAA;EACA,qBAAA;EACA,SAAA;EACA,UAAA;EACA,gBAAA;EACA,WAAA;AAjIF;AAmIE;EACE,4CAAA;EACA,gCAAA;EACA,iBAAA;AAjIJ;AAmII;EACE,+BAAA;AAjIN;AAoII;EACE,qCAAA;AAlIN;AA4IM;EAPE,yDAAA;AAlIR;AAoIQ;EACE,mGAAA;AAlIV;AA0IM;EAXE,0DAAA;AA5HR;AA8HQ;EACE,oGAAA;AA5HV;AAwIM;EAfE,0DAAA;AAtHR;AAwHQ;EACE,oGAAA;AAtHV;AAsIM;EAnBE,0DAAA;AAhHR;AAkHQ;EACE,oGAAA;AAhHV;AAoIM;EAvBE,0DAAA;AA1GR;AA4GQ;EACE,oGAAA;AA1GV;AAkIM;EA3BE,0DAAA;AApGR;AAsGQ;EACE,oGAAA;AApGV;AAgIM;EA/BE,0DAAA;AA9FR;AAgGQ;EACE,oGAAA;AA9FV;AA+HI;EAIE,mBAAA;EACA,aAAA;EACA,2BAAA;AAhIN;AAkIM;EACE,0BAAA;EACA,cAAA;EACA,YATU;EAUV,WAVU;AAtHlB;AAmIM;EACE,eAAA;AAjIR;AAqII;EAGE,mBAAA;EACA,aAAA;EACA,qBAAA;EACA,qFACE;EAIF,+BAAA;EACA,iBAAA;AAzIN;AA2IM;EACE,2BAAA;EACA,eAAA;EACA,YAfU;EAgBV,WAhBU;AAzHlB;AA4IM;EACE,eAAA;AA1IR;AA6IM;EACE,gBAAA;AA3IR;AA8IM;EACE,aAAA;EACA,mBAAA;EACA,sBAAA;AA5IR;AA+IM;EACE,wBAAA;AA7IR;AAgJM;EACE,aAAA;AA9IR;AAkJI;EACE,6CAAA;AAhJN;AAmJI;EAEE,WApdE;AAkUR;AAsJE;EACE,2BAAA;EACA,4BAAA;AApJJ;AAuJE;EACE,kCAAA;EACA,4BAAA;EACA,qBA5Ke;EA6Kf,gBAAA;EACA,WA9Ke;AAyBnB;AAuJI;EACE,0DAAA;AArJN;AAsJM;EAEE,0DAAA;AArJR;AAwJI;EACE,0DAAA;AAtJN;AAuJM;EAEE,0DAAA;AAtJR;AA2JE;;EAEE,4BAAA;AAzJJ;AA4JE;EACE,iBAAA;EACA,kBAAA;EACA,eAAA;EACA,gBAAA;EACA,uBAAA;EACA,mBAAA;AA1JJ;AA4JI;EACE,OAAA;EACA,cAAA;EACA,mBAAA;AA1JN;AA6JI;EACE,2BAAA;AA3JN;AA+JE;;EAEE,kBAAA;AA7JJ;AAgKE;EACE,4BAAA;EACA,kBAAA;EACA,mBAAA;EACA,iBAAA;EACA,YAAA;AA9JJ;AAiKI;EACE,YAAA;EACA,aAAA;AA/JN;AAiKI;EACE,eAAA;EACA,WAAA;AA/JN;AA+KI;EARE,0OAAA;EAKA,4BAAA;AAxKN;AAiLI;EAdE,0OAAA;EAKA,4BAAA;AApKN;AAmLI;EApBE,4NAAA;EAKA,4BAAA;AAhKN;AAqLI;EA1BE,4OAAA;EAKA,4BAAA;AA5JN;AAuLI;EAhCE,8NAAA;EAKA,4BAAA;AAxJN;AAyLI;EAtCE,wOAAA;EAKA,4BAAA;AApJN;AA2LI;EA5CE,wOAAA;EAKA,4BAAA;AAhJN;AA6LI;EAlDE,0NAAA;EAKA,4BAAA;AA5IN;AA+LI;EAxDE,8NAAA;EAKA,4BAAA;AAxIN;;AAqNA;EAdE,yIAAA;AAnMF;AAyME;EAGE,qIAAA;AAzMJ;;AAkNA;EAlBE,yIAAA;AA5LF;AAkME;EAGE,qIAAA;AAlMJ;;AA+MA;EAtBE,yIAAA;AArLF;AA2LE;EAGE,qIAAA;AA3LJ;;AA4MA;EA1BE,yIAAA;AA9KF;AAoLE;EAGE,qIAAA;AApLJ;;AAnaE;EA4mBF;;IAGI,OAAA;EAtMF;AACF;AAraE;EAumBF;;IAMI,gDAAA;IACA,YAAA;EAnMF;AACF;;AAsMA;EAGE,gBAvpBM;EAwpBN,SAAA;EACA,wCAAA;EACA,kBAAA;EACA,QAAA;EACA,SArWU;EAsWV,UAtpBc;AAidhB;AAuME;EACE,oBAAA;EACA,iBAAA;EACA,8BAAA;EACA,YAAA;AArMJ;AAuMI;EACE,aAAA;AArMN;AAwMI;EACE,iBAAA;EACA,mBAAA;EACA,kBAAA;EACA,mBAAA;AAtMN;AAwMM;EACE,gBAAA;AAtMR;AA2ME;EACE,yEAAA;EACA,6BAAA;EACA,kBAAA;AAzMJ;AA4ME;;EAEE,+BAAA;AA1MJ;AA6ME;;;;;;;;;;EAUE,mBAAA;AA3MJ;AA8ME;;;EAGE,gBAAA;AA5MJ;AA+ME;EACE,kBAAA;AA7MJ;AA+ME;EACE,mBAAA;AA7MJ;AAgNE;EACE,qBAAA;EACA,iBAAA;EACA,gBAAA;AA9MJ;AA+MI;EACE,gCAAA;AA7MN;AA+MI;EACE,kCAAA;AA7MN;AAiNE;EACE,kBAAA;EACA,mBAAA;EAEA,0BAAA;AAhNJ;;AAoNA;EACE,UAAA;EACA,iBAAA;AAjNF;;AAoNA;EACE,gBAAA;AAjNF;AAmNE;EACE,kBAAA;AAjNJ;AAoNE;EACE,iBAAA;EACA,iBAAA;EACA,eAAA;AAlNJ;AAsNI;EACE,WAAA;EACA,gBAAA;AApNN;AAsNM;EACE,cAAA;AApNR;AAwNI;EACE,kBAAA;AAtNN;AAwNM;EACE,WAAA;AAtNR;AA4NI;EACE,SAAA;AA1NN;AA6NI;EACE,gBAAA;AA3NN;AA+NE;EACE,yEAAA;EACA,kBAAA;AA7NJ;AAgOE;EACE,kBAAA;EACA,mBAAA;AA9NJ;;AAoOA;EACE,oBAAA;EACA,gBA5yBM;EA6yBN,0CAAA;EACA,SAAA;EACA,aAAA;EACA,sBAAA;EACA,kBAAA;EACA,QAAA;EACA,SA5fU;EA6fV,UA7yBc;AA4kBhB;AAtjBE;EA6wBF;IAYI,OAAA;EA/NF;AACF;AAtjBE;EAwwBF;IAeI,YAAA;EA7NF;AACF;;AAgOA;EACE,kBAAA;EACA,aAAA;EACA,aAAA;AA7NF;AA+NE;EAjxBA,qCAAA;EACA,eAAA;EACA,qBAAA;EACA,YAAA;EACA,iBAAA;EA+wBE,gBAAA;EACA,YAAA;AAzNJ;;AAgOE;;EACE,8BAFO;EAGP,2BAHO;AAzNX;AA8NE;;EACE,+BANO;EAOP,4BAPO;AApNX;;AA+NA;EACE,sBAAA;EACA,OAAA;EACA,kBAAA;EACA,gBAAA;AA5NF;AAjmBE;EAyzBF;IAMI,mBAAA;EA1NF;AACF;AAjmBE;EAozBF;IASI,oBAAA;EAxNF;AACF;;AA2NA;EACE,2BAAA;EACA,4BAAA;AAxNF;;AA6NA;EACE,wCAAA;AA1NF;;AA6NA;EAIE,oBAAA;EACA,iBAAA;EACA,+BAAA;EACA,YAAA;EACA,aAAA;AA7NF;AA+NE;EACE,aAAA;AA7NJ;AAgOE;EACE,iBAAA;EACA,mBAAA;EACA,kBAAA;EACA,mBAAA;AA9NJ;AAgOI;EACE,gBAAA;AA9NN;AAkOE;EAEE,kBAAA;EACA,uBAAA;AAjOJ;;AAqOA;;EAEE,qCAAA;EACA,YAAA;EACA,SAAA;EACA,UAAA;EACA,WAAA;AAlOF;;AAqOA;EA5SE,yIAAA;AA2EF;AArEE;EAGE,qIAAA;AAqEJ;;AAkOA;EAhTE,yIAAA;AAkFF;AA5EE;EAGE,qIAAA;AA4EJ;;AA+NA;EApTE,yIAAA;AAyFF;AAnFE;EAGE,qIAAA;AAmFJ;;AA4NA;EAxTE,yIAAA;AAgGF;AA1FE;EAGE,qIAAA;AA0FJ;;AAyNA;EACE,SAAA;EACA,UAAA;AAtNF;AAwNE;EACE,aAAA;EACA,oBAAA;EACA,oEACE;EAEF,oCAAA;EACA,kBAAA;EACA,YAAA;AAxNJ;AA0NI;EACE,YAAA;AAxNN;AA4NE;EACE,gBAAA;AA1NJ;AA6NE;EACE,2BAAA;EACA,eAAA;EACA,eAAA;EACA,gBAAA;EACA,uBAAA;EACA,mBAAA;AA3NJ;AA8NE;EAEE,+BAAA;AA7NJ;AAgOE;EACE,gCAAA;EACA,eAAA;AA9NJ;AAiOE;;EAEE,eAAA;AA/NJ;;AAmOA;EACE,aAAA;AAhOF;;AAmOA;EACE,gBAAA;EACA,iBAAA;AAhOF;;AAmOA;EACE,gCAAA;EACA,cAAA;EACA,eAAA;AAhOF;AAkOE;EACE,aAAA;EACA,oBAAA;EACA,iBAAA;EACA,eAAA;AAhOJ;AAmOE;EACE,8BAAA;EACA,iBAAA;EACA,mBAAA;EACA,kBAAA;EACA,kBAAA;EACA,iBAAA;EACA,uBAAA;EACA,mBAAA;AAjOJ;AAoOE;EACE,mBAAA;EACA,mBAAA;EACA,eAAA;EACA,mBAAA;AAlOJ;AAqOE;;;EAGE,gBAAA;AAnOJ;AAsOE;;;EAGE,iBAAA;AApOJ;;AA0OA;EAIE,yBAFe;EAGf,yBAAA;EACA,eAAA;EACA,mBAAA;EACA,gBAAA;EACA,WAAA;AA1OF;AA4OE;;EAEE,kBAAA;EACA,YAAA;AA1OJ;AA6OE;EACE,yBAhBa;EAiBb,gCAAA;AA3OJ;AA+OE;EACE,mBAhjCO;EAijCP,8BAAA;EACA,iBAAA;EACA,mBAAA;EACA,kBAAA;EACA,aAAA;EACA,uBAAA;EACA,mBAAA;AA7OJ;AAgPE;EACE,WAAA;AA9OJ;AA+OI;EACE,wFAAA;EACA,YAAA;EACA,WAAA;AA7ON;AAiPE;;;;EAIE,iBAAA;AA/OJ;AAkPE;EACE,UAAA;AAhPJ;AAkPE;;EAEE,UAAA;AAhPJ;AAkPE;EACE,UAAA;AAhPJ;AAmPE;EACE,kBAAA;EACA,uBAAA;EACA,mBAAA;AAjPJ;AA/zBE;EA6iCA;IAKI,aAAA;EA/OJ;AACF;AA/zBE;EAwiCA;IAQI,UAAA;EA7OJ;AACF;AAgPE;EACE,eAAA;AA9OJ;;AAoPA;EAKE,mBAAA;AArPF;AAuPE;EAnjCA,qCAAA;EACA,eAAA;EACA,qBAAA;EACA,YAAA;EACA,iBAAA;EAijCE,gBAAA;EACA,2BAAA;EACA,4BAAA;EACA,2BAAA;EACA,YAXK;EAYL,SAAA;EACA,UAAA;EACA,WAdK;AAnOT;AAoPE;EArgBA,wIAAA;EAugBE,qBAAA;AAlPJ;AA/QE;EAGE,oIAAA;AA+QJ;AAkPE;EA1gBA,yIAAA;AA2RF;AArRE;EAGE,qIAAA;AAqRJ;AAgPE;EA9gBA,wIAAA;EAghBE,oBAAA;AA9OJ;AA5RE;EAGE,oIAAA;AA4RJ;;AAiPA;EAIE,gCAHmB;EAInB,wCAAA;EACA,kBAAA;EACA,UAjoCc;AAg5BhB;AAmPE;EACE,gBAAA;EACA,iBAAA;AAjPJ;AAmPI;EAEE,6DAAA;EACA,eAAA;AAlPN;AAqPI;EACE,+BAlBa;EAmBb,YAAA;AAnPN;;AAwPA;EACE,kDAAA;EACA,mBAAA;EACA,oBAAA;AArPF;;AA0PA;EACE,+FAAA;EAQA,kBAAA;EACA,wCAAA;EACA,aAAA;EACA,eAAA;EACA,YAAA;EACA,UA1qCc;AA46BhB;AAgQE;EACE,SAAA;EACA,kBAAA;EACA,UAAA;AA9PJ;AAiQE;;;EAGE,aAAA;EACA,sBAAA;AA/PJ;AAkQE;EACE,mBAAA;EACA,kBAAA;AAhQJ;AAmQE;EACE,iBAAA;AAjQJ;AAoQE;;;EAGE,8BAAA;EACA,cAAA;AAlQJ;AAoQI;;;EACE,+BAAA;EACA,eAAA;AAhQN;AAmQI;;;;;EAEE,uCAAA;AA9PN;AAkQE;;EAEE,uBAAA;EACA,SAAA;EACA,eAAA;EACA,cAAA;EACA,cAAA;EACA,gBAAA;EACA,qBAAA;EACA,YAAA;AAhQJ;AAmQE;EACE,mBAAA;EACA,oBAAA;EACA,mBAAA;AAjQJ;AAqQI;EACE,mBAAA;EACA,oBAAA;EACA,mBAAA;AAnQN;AAqQM;EACE,WAAA;AAnQR;AAsQM;EACE,cAAA;AApQR;AAuQM;EACE,eAAA;AArQR;AA0QE;EACE,aAAA;EACA,sBAAA;AAxQJ;AA0QI;;EAEE,mBAAA;EACA,aAAA;EACA,mBAAA;EACA,cAAA;AAxQN;AA0QM;;EACE,gBAAA;EACA,WAAA;AAvQR;AA2QI;EACE,aAAA;EACA,oBAAA;EACA,iBAAA;EACA,kDACE;EAEF,+BAAA;EACA,kBAAA;AA3QN;AA8QI;EACE,gBAAA;AA5QN;AA8QM;EACE,yDAAA;AA5QR;AA+QM;EACE,0DAAA;AA7QR;AAiRI;EACE,2BAAA;EACA,4BAAA;EACA,iBAAA;AA/QN;AAkRI;EACE,aAAA;AAhRN;AAmRI;EACE,gBAAA;EACA,iBAAA;AAjRN;;AAjgCE;EAyxCA;;IAEE,aAAA;EApRF;AACF;AA7gCE;EA2yCA;IACE,4BAAA;EA3RF;;EA8RA;IACE,YAAA;IACA,kBAAA;IACA,SAxhCQ;IAyhCR,WAAA;EA3RF;;EA8RA;IACE,+BAAA;EA3RF;;EA8RA;IACE,mBAAA;IACA,aAAA;IACA,sBAAA;EA3RF;AACF;AA9hCE;EA4zCA;IACE,SAAA;IACA,gBAAA;IACA,kBAAA;IACA,UAAA;IACA,2BAAA;EA3RF;;EA8RA;IACE,kBAAA;EA3RF;;EA8RA;IACE,eAAA;IACA,8BAAA;EA3RF;;EA8RA;IACE,aAAA;IACA,qBAAA;IACA,iFACE;IAIF,+BAAA;IACA,iBAAA;EA/RF;AACF;AAkSA;EACE,aAAA;EACA,YAAA;EACA,kBAAA;EACA,gBAAA;EACA,kBAAA;AAhSF;AAkSE;EACE,gBAAA;EACA,uBAAA;EACA,qCAAA;EACA,kBAAA;EACA,2BAAA;EACA,gBAAA;EACA,YAAA;EACA,qBAAA;AAhSJ;AAmSE;;EAEE,uDAAA;AAjSJ;;AAqSA;EACE,YAAA;AAlSF;;AAqSA;EACE,aAAA;AAlSF;;AAqSA;EACE,SAAA;EACA,wCAAA;EACA,8BAAA;EACA,cAAA;EACA,SAAA;EACA,UAAA;EACA,UA55Cc;AA0nChB;;AAqSA;EACE,mBAAA;EACA,aAAA;EACA,gBAAA;EACA,iBAAA;AAlSF;;AAqSA;EACE,mFAAA;EACA,YA1GU;EA2GV,WA3GU;AAvLZ;;AAqSA;EACE,kBAAA;EACA,gBAAA;AAlSF;;AAqSA;EACE,iEAAA;EACA,YAAA;EACA,aAAA;AAlSF;;AAqSA;EACE,aAAA;EACA,sBAAA;EACA,mBAAA;EACA,gBAAA;AAlSF;AAoSE;EACE,kBAAA;AAlSJ;;AAwSA;EACE,mBAAA;AArSF;;AAwSA;EACE,gBAAA;AArSF;;AAwSA;EACE,gBAAA;EACA,kBAAA;EACA,mBAAA;EACA,iBAAA;EACA,kBAAA;AArSF;;AA2SE;EACE,yBAAA;EACA,WAAA;AAxSJ;AA2SE;;EAEE,qCAAA;EACA,iBAAA;AAzSJ;;AA+SA;EACE,gBAAA;AA5SF;;AAiTA;EACE,gBAAA;AA9SF;;AAsTE;;;EACE,gBAAA;EACA,aAAA;EACA,iBAAA;EACA,+BAAA;EACA,eAAA;AAjTJ;AAlrCE;EA89CA;;;IAQI,kBAAA;EA9SJ;AACF;AAprCE;EAy9CA;;;IAWI,qBAAA;EA1SJ;AACF;AA4SI;;;EACE,sBAAA;AAxSN;AA4SE;;;EACE,SAAA;EACA,SAAA;EACA,UAAA;AAxSJ;AA2SE;;;EACE,mBAAA;EACA,kBAAA;AAvSJ;;AA6SA;EACE,qBAAA;EACA,YAAA;EACA,kBAAA;EACA,kBAAA;AA1SF;;AA6SA;EACE,SAAA;EACA,cAAA;EAAgB,yFAAA;EAChB,SAAA;EACA,aAAA;EACA,SAAA;EACA,UAAA;EACA,kBAAA;EACA,YAAA;EACA,QAAA;AAzSF;;AA4SA;EACE,YAAA;AAzSF","sourcesContent":["@use 'sass:color';\n@use 'sass:math';\n\n// Color palette recycled from GitHub Primer.\n// Please use those values if you need to add colors.\n// https://primer.style/css/support/color-system\n$blue-100: #dbedff;\n$blue-200: #c8e1ff;\n$blue-300: #79b8ff;\n$blue-700: #044289;\n$green-100: #dcffe4;\n$green-200: #bef5cb;\n$green-300: #85e89d;\n$green-500: #28a745;\n$green-700: #176f2c;\n$grey-100: #f6f8fa;\n$grey-200: #e1e4e8;\n$grey-300: #d1d5da;\n$grey-400: #959da5;\n$grey-500: #6a737d;\n$grey-800: #2f363d;\n$pink-300: #f692ce;\n$pink-700: #b93a86;\n$purple-000: #f5f0ff;\n$purple-100: #e6dcfd;\n$red-500: #d73a49;\n$white: #fff;\n$yellow-200: #fff5b1;\n$yellow-300: #ffea7f;\n$yellow-700: #dbab09;\n\n// Z-INDEX ENUM\n// $z-index-progressbar: 1;\n$z-index-popup: 2;\n\n// SVG ICONS -- see images/README.md for sources and license details\n// Are you a designer? New icon PRs welcomed!\n$image-analytics: 'images/analytics.svg';\n$image-baseline: 'images/horizontal-rule.svg';\n$image-files: 'images/files.svg';\n$image-chevron-dn: 'images/chevron-down.svg';\n$image-chevron-up: 'images/chevron-up.svg';\n$image-lock-fill: 'images/lock-fill.svg';\n$image-network: 'images/router.svg';\n$image-overflow: 'images/three-dots-vertical.svg';\n$image-pause-circle-active: 'images/pause-circle-active.svg';\n$image-pause-circle-idle: 'images/pause-circle-idle.svg';\n$image-peers: 'images/team.svg';\n$image-play-circle-active: 'images/play-circle-active.svg';\n$image-play-circle-idle: 'images/play-circle-idle.svg';\n$image-speed: 'images/checkered-flag.svg';\n$image-tiers: 'images/diagram-3-fill.svg';\n$image-upload-download: 'images/up-and-down-arrows.svg';\n\n@mixin for-phone-only {\n  @media (max-width: 599px) {\n    @content;\n  }\n}\n@mixin for-tablet-portrait-up {\n  @media (min-width: 600px) {\n    @content;\n  }\n}\n// @mixin for-tablet-landscape-up {media (min-width: 900px) { @content; }}\n// @mixin for-desktop-up {@media (min-width: 1200px) { @content; }}\n// @mixin for-big-desktop-up {@media (min-width: 1800px) { @content; }}\n\n/// MIXINS\n\n@mixin image-on-vertical-gradient($src, $topColor, $bottomColor) {\n  background-image: url($src), linear-gradient($topColor, $bottomColor);\n}\n\n@mixin button {\n  border: 1px solid var(--color-border);\n  cursor: pointer;\n  display: inline-block;\n  padding: 3px;\n  user-select: none;\n}\n\n@mixin rounded-box($radius) {\n  border-radius: $radius;\n}\n\n@mixin rounded-button($radius) {\n  @include button;\n  border-radius: $radius;\n}\n\n/// GLOBAL\n\n:root {\n  --color-bg-even: #{$white};\n  --color-bg-hover: #{$grey-100};\n  --color-bg-menu: #{$grey-200};\n  --color-bg-odd: #{$grey-100};\n  --color-bg-selection-1: #{$purple-100};\n  --color-bg-selection-2: #{$purple-000};\n  --color-border: #{$grey-400};\n  --color-fg-disabled: #{$grey-400};\n  --color-fg-error: #{$red-500};\n  --color-fg-name: #{$grey-800};\n  --color-fg-port-closed: #{$red-500};\n  --color-fg-port-open: #{$green-500};\n  @media (prefers-color-scheme: dark) {\n    --color-fg-primary: #{$grey-100};\n    --color-fg-secondary: #{$grey-400};\n    --color-fg-tertiary: #{$grey-500};\n  }\n  @media (prefers-color-scheme: light) {\n    --color-fg-primary: #{$grey-800};\n    --color-fg-secondary: #{$grey-500};\n    --color-fg-tertiary: #{$grey-400};\n  }\n  --color-progressbar-background-1: #{$grey-100};\n  --color-progressbar-background-2: #{$grey-400};\n  --color-progressbar-verify-1: #{$pink-300};\n  --color-progressbar-verify-2: #{$pink-700};\n  --color-progressbar-magnet-1: #{$yellow-300};\n  --color-progressbar-magnet-2: #{$yellow-700};\n  --color-progressbar-magnet-paused-1: #{$yellow-200};\n  --color-progressbar-magnet-paused-2: #{$grey-500};\n  --color-progressbar-leech-paused-1: #{$blue-200};\n  --color-progressbar-leech-paused-2: #{$grey-500};\n  --color-progressbar-leech-queued-1: #{$blue-100};\n  --color-progressbar-leech-queued-2: #{$blue-300};\n  --color-progressbar-leech-1: #{$blue-300};\n  --color-progressbar-leech-2: #{$blue-700};\n  --color-progressbar-seed-paused-1: #{$green-200};\n  --color-progressbar-seed-paused-2: #{$grey-500};\n  --color-progressbar-seed-queued-1: #{$green-100};\n  --color-progressbar-seed-queued-2: #{$green-300};\n  --color-progressbar-seed-1: #{$green-300};\n  --color-progressbar-seed-2: #{$green-700};\n  --color-tab-deselected-1: #{$grey-100};\n  --color-tab-deselected-2: #{$grey-400};\n  --color-tab-selected-1: #{$purple-100};\n  --color-tab-selected-2: #{$purple-000};\n  --color-toolbar-background: linear-gradient(#{$grey-300}, #{$grey-400});\n}\n\nhtml,\nbody {\n  color: var(--color-fg-primary);\n  font-family: Verdana, Arial, Helvetica, sans-serif;\n  height: 100%;\n  margin: 0;\n  overflow: hidden;\n}\n\nimg {\n  border: 0;\n}\n\na {\n  outline: 0;\n}\n\n.hidden {\n  display: none;\n}\n\n.even {\n  background-color: var(--color-bg-even);\n}\n.odd {\n  background-color: var(--color-bg-odd);\n}\n\n.mainwin {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  position: relative;\n\n  > * {\n    margin: 0;\n    padding: 0;\n  }\n}\n\n.mainwin-workarea {\n  display: flex;\n  flex: 1;\n  flex-direction: row;\n  overflow: auto;\n}\n\n/// TOOLBAR\n\n$toolbar-height-number: 36;\n$toolbar-height: $toolbar-height-number * 1px;\n\n#mainwin-toolbar {\n  align-items: center;\n  background: var(--color-toolbar-background);\n  border-bottom: 1px solid var(--color-border);\n  display: flex;\n  flex-direction: row;\n  height: $toolbar-height;\n  margin: 0;\n  padding: 2px;\n  width: 100%;\n\n  button {\n    background-color: transparent;\n    background-position: center;\n    background-repeat: no-repeat;\n    border: 0;\n    cursor: pointer;\n    height: $toolbar-height;\n    margin-right: 6px;\n    user-select: none;\n    width: $toolbar-height;\n\n    &:hover:not(:disabled) {\n      background-color: var(--color-bg-hover);\n    }\n\n    &:disabled {\n      cursor: default;\n      opacity: 0.25;\n    }\n  }\n\n  > .toolbar-separator {\n    border-left: 1px solid var(--color-border);\n    height: 25px;\n    margin: 0 6px 0 0;\n  }\n\n  :nth-last-child(2) {\n    border: 0;\n    flex-grow: 1;\n  }\n}\n\n#toolbar-open {\n  background-image: url('images/toolbar-folder.png');\n}\n\n#toolbar-remove {\n  background-image: url('images/toolbar-close.png');\n}\n\n#toolbar-start {\n  background-image: url('images/toolbar-start.png');\n}\n\n#toolbar-pause {\n  background-image: url('images/toolbar-pause.png');\n}\n\n#toolbar-inspector {\n  background-image: url('images/toolbar-info.png');\n}\n\n#toolbar-overflow {\n  background-color: transparent;\n  background-image: url($image-overflow);\n  background-position: center;\n  background-size: 26px;\n  margin-right: 4px;\n  width: 36px;\n\n  &.alt-speed-enabled {\n    background-image: url('images/blue-turtle.png'), url($image-overflow);\n    background-position: bottom left, center, center;\n    background-size: 16px 9px, 26px, 26px;\n  }\n}\n\n/// FILTERBAR\n\n// $filterbar-height-number: 30;\n// $filterbar-height: $filterbar-height-number * 1px;\n\n.mainwin-filterbar {\n  align-items: center;\n  background: var(--color-toolbar-background);\n  border-bottom: 1px solid var(--color-border);\n  display: flex;\n  flex-direction: row;\n  font-size: smaller;\n  // height: $filterbar-height;\n  width: 100%;\n\n  @include for-phone-only {\n    > :not(select):not(input) {\n      display: none;\n    }\n    > input {\n      min-width: 50px;\n    }\n    > input,\n    > select {\n      height: 100%;\n    }\n  }\n\n  > * {\n    padding: 0 5px;\n  }\n}\n\n#torrent-search {\n  &.blur {\n    color: var(--color-fg-tertiary);\n  }\n}\n\n$speed-icon-size: 12px;\n\n.speed-up-icon {\n  background-image: url($image-chevron-up);\n  background-repeat: no-repeat;\n  background-size: $speed-icon-size $speed-icon-size;\n  height: $speed-icon-size;\n  width: $speed-icon-size;\n}\n\n.speed-dn-icon {\n  background-image: url($image-chevron-dn);\n  background-repeat: no-repeat;\n  background-size: $speed-icon-size $speed-icon-size;\n  height: $speed-icon-size;\n  width: $speed-icon-size;\n}\n\n/// TORRENT CONTAINER\n\n$pauseresume-size-number: 14;\n$pauseresume-size: $pauseresume-size-number * 1px;\n$progressbar-border-number: 1;\n$progressbar-height-number: 14;\n$popup-top: 61px; // TODO: ugly that this is hardcoded\n\n.flex {\n  flex-grow: 1;\n}\n\n#torrent-container {\n  flex-grow: 1;\n  left: 0;\n  margin: 0;\n  overflow: auto;\n  -webkit-overflow-scrolling: touch;\n  padding: 0;\n  right: 0;\n}\n\n.torrent-container {\n  background-color: $white;\n}\n\n.torrent-list {\n  $app-image: 'images/application-x-executable.png';\n  $audio-image: 'images/audio-x-generic.png';\n  $folder-image: 'images/folder.png';\n  $font-image: 'images/font-x-generic.png';\n  $image-image: 'images/image-x-generic.png';\n  $package-image: 'images/package-x-generic.png';\n  $text-image: 'images/folder.png';\n  $text-image: 'images/text-x-generic.png';\n  $video-image: 'images/video-x-generic.png';\n\n  cursor: pointer;\n  list-style-type: none;\n  margin: 0;\n  padding: 0;\n  text-align: left;\n  width: 100%;\n\n  .torrent {\n    border-bottom: 1px solid var(--color-border);\n    color: var(--color-fg-secondary);\n    user-select: none;\n\n    &.paused {\n      color: var(--color-fg-disabled);\n    }\n\n    .icon {\n      background-position: center, top left;\n\n      @mixin mime-icon-compact($mime-icon) {\n        background-image: url($mime-icon);\n\n        &[data-icon-multifile='true'] {\n          background-image: url($mime-icon), url($folder-image);\n        }\n      }\n\n      &[data-icon-mime-type='audio'] {\n        @include mime-icon-compact($audio-image);\n      }\n\n      &[data-icon-mime-type='application'] {\n        @include mime-icon-compact($app-image);\n      }\n\n      &[data-icon-mime-type='font'] {\n        @include mime-icon-compact($font-image);\n      }\n\n      &[data-icon-mime-type='image'] {\n        @include mime-icon-compact($image-image);\n      }\n\n      &[data-icon-mime-type='model'] {\n        @include mime-icon-compact($package-image);\n      }\n\n      &[data-icon-mime-type='text'] {\n        @include mime-icon-compact($text-image);\n      }\n\n      &[data-icon-mime-type='video'] {\n        @include mime-icon-compact($video-image);\n      }\n    }\n\n    &.compact {\n      $icon-size-num: 16;\n      $icon-size: $icon-size-num * 1px;\n\n      align-items: center;\n      display: flex;\n      flex-direction: row-reverse;\n\n      .icon {\n        background-size: $icon-size, $icon-size-num * 0.5px;\n        flex-shrink: 0;\n        height: $icon-size;\n        width: $icon-size;\n      }\n\n      > * {\n        margin: 4px 5px;\n      }\n    }\n\n    &:not(.compact) {\n      $icon-size-num: 32;\n      $icon-size: $icon-size-num * 1px;\n      align-items: center;\n      display: grid;\n      grid-column-gap: 12px;\n      grid-template-areas:\n        'icon name'\n        'icon peers'\n        'icon progressbar'\n        'icon progress-text';\n      grid-template-columns: $icon-size 1fr;\n      padding: 2px 12px;\n\n      .icon {\n        background-size: $icon-size, $icon-size-num * 0.5px;\n        grid-area: icon;\n        height: $icon-size;\n        width: $icon-size;\n      }\n\n      .torrent-name {\n        grid-area: name;\n      }\n\n      .torrent-peer-details {\n        grid-area: peers;\n      }\n\n      .torrent-progress {\n        display: flex;\n        flex-direction: row;\n        grid-area: progressbar;\n      }\n\n      .torrent-progress-details {\n        grid-area: progress-text;\n      }\n\n      > * {\n        margin: 1px 0;\n      }\n    }\n\n    &.selected {\n      background-color: var(--color-bg-selection-1);\n    }\n\n    &.selected .torrent-progress-details.error,\n    &.selected .torrent-peer-details.error {\n      color: $white;\n    }\n  }\n\n  .icon {\n    background-position: center;\n    background-repeat: no-repeat;\n  }\n\n  .torrent-pauseresume-button {\n    background-position: center center;\n    background-repeat: no-repeat;\n    background-size: $pauseresume-size;\n    margin-left: math.floor($pauseresume-size-number / 2) * 1px;\n    width: $pauseresume-size;\n\n    &[data-action='pause'] {\n      background-image: url($image-pause-circle-idle);\n      &:active,\n      &:hover {\n        background-image: url($image-pause-circle-active);\n      }\n    }\n    &[data-action='resume'] {\n      background-image: url($image-play-circle-idle);\n      &:active,\n      &:hover {\n        background-image: url($image-play-circle-active);\n      }\n    }\n  }\n\n  .torrent-progress-details.error,\n  .torrent-peer-details.error {\n    color: var(--color-fg-error);\n  }\n\n  .torrent-name {\n    font-weight: bold;\n    margin-bottom: 2px;\n    margin-top: 2px;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n\n    &.compact {\n      flex: 1;\n      font-size: 1em;\n      font-weight: normal;\n    }\n\n    &:not(.paused) {\n      color: var(--color-fg-name);\n    }\n  }\n\n  .torrent-progress-details,\n  .torrent-peer-details {\n    font-size: x-small;\n  }\n\n  .torrent-progress-bar {\n    background-repeat: no-repeat;\n    border-radius: 3px;\n    border-style: solid;\n    border-width: 1px;\n    height: ($progressbar-height-number - ($progressbar-border-number * 2)) *\n      1px;\n\n    &.full {\n      flex-grow: 1;\n      margin: 2px 0;\n    }\n    &.compact {\n      min-width: 50px;\n      width: 50px;\n    }\n\n    @function progressbar-gradient($color1, $color2) {\n      @return linear-gradient(to bottom, $color1, $color2 80%);\n    }\n\n    @mixin progressbar-background($color1, $color2) {\n      background: progressbar-gradient($color1, $color2),\n        progressbar-gradient(\n          var(--color-progressbar-background-1),\n          var(--color-progressbar-background-2)\n        );\n      background-repeat: no-repeat;\n    }\n\n    &.leech.paused {\n      @include progressbar-background(\n        var(--color-progressbar-leech-paused-1),\n        var(--color-progressbar-leech-paused-2)\n      );\n    }\n    &.leech.queued {\n      @include progressbar-background(\n        var(--color-progressbar-leech-queued-1),\n        var(--color-progressbar-leech-queued-2)\n      );\n    }\n    &.leech {\n      @include progressbar-background(\n        var(--color-progressbar-leech-1),\n        var(--color-progressbar-leech-2)\n      );\n    }\n    &.magnet.paused {\n      @include progressbar-background(\n        var(--color-progressbar-magnet-paused-1),\n        var(--color-progressbar-magnet-paused-2)\n      );\n    }\n    &.magnet {\n      @include progressbar-background(\n        var(--color-progressbar-magnet-1),\n        var(--color-progressbar-magnet-2)\n      );\n    }\n    &.seed.paused {\n      @include progressbar-background(\n        var(--color-progressbar-seed-paused-1),\n        var(--color-progressbar-seed-paused-2)\n      );\n    }\n    &.seed.queued {\n      @include progressbar-background(\n        var(--color-progressbar-seed-queued-1),\n        var(--color-progressbar-seed-queued-2)\n      );\n    }\n    &.seed {\n      @include progressbar-background(\n        var(--color-progressbar-seed-1),\n        var(--color-progressbar-seed-2)\n      );\n    }\n    &.verify {\n      @include progressbar-background(\n        var(--color-progressbar-verify-1),\n        var(--color-progressbar-verify-2)\n      );\n    }\n  }\n}\n\n/// PREFERENCES DIALOG\n\n@mixin tab-image($image-url) {\n  background-image: url($image-url),\n    linear-gradient(\n      var(--color-tab-deselected-1),\n      var(--color-tab-deselected-2)\n    );\n\n  &:active,\n  &:checked,\n  &.selected {\n    background-image: url($image-url),\n      linear-gradient(var(--color-tab-selected-1), var(--color-tab-selected-2));\n  }\n}\n\n#inspector-tab-info {\n  @include tab-image($image-analytics);\n}\n\n#inspector-tab-peers {\n  @include tab-image($image-peers);\n}\n\n#inspector-tab-tiers {\n  @include tab-image($image-tiers);\n}\n\n#inspector-tab-files {\n  @include tab-image($image-files);\n}\n\n#inspector,\n#prefs-dialog {\n  @include for-phone-only {\n    left: 0;\n  }\n  @include for-tablet-portrait-up {\n    border-left: 1px solid var(--color-fg-secondary);\n    width: 570px;\n  }\n}\n\n#prefs-dialog {\n  $background-color: $white;\n\n  background: $background-color;\n  bottom: 0;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  position: absolute;\n  right: 0;\n  top: $popup-top;\n  z-index: $z-index-popup;\n\n  .tabs-page {\n    grid-column-gap: 8px;\n    grid-row-gap: 5px;\n    grid-template-columns: 1fr 1fr;\n    margin: 20px;\n\n    &:not(.hidden) {\n      display: grid;\n    }\n\n    .section-label {\n      font-weight: bold;\n      grid-column: span 2;\n      margin-left: -20px;\n      padding-bottom: 5px;\n\n      &:not(:first-of-type) {\n        margin-top: 20px;\n      }\n    }\n  }\n\n  .alt-speed-section-label {\n    background: transparent url('images/blue-turtle.png') no-repeat;\n    background-position: left 4px;\n    padding-left: 22px;\n  }\n\n  :disabled,\n  .disabled {\n    color: var(--color-fg-disabled);\n  }\n\n  #alt-times-div,\n  #autostart-div,\n  #port-forwarding,\n  #randomize-port,\n  #suffix-div,\n  #use-dht-div,\n  #use-lpd-div,\n  #use-pex-div,\n  #utp-enabled,\n  .alt-speed-label {\n    grid-column: span 2;\n  }\n\n  .blocklist-size-label,\n  .blocklist-update-button,\n  .port-status {\n    grid-column: 2 / 3;\n  }\n\n  .blocklist-size-label {\n    font-size: smaller;\n  }\n  .blocklist-size-number {\n    font-weight: bolder;\n  }\n\n  .port-status-label {\n    display: inline-block;\n    font-weight: bold;\n    margin-left: 5px;\n    &[data-open='true'] {\n      color: var(--color-fg-port-open);\n    }\n    &[data-open='false'] {\n      color: var(--color-fg-port-closed);\n    }\n  }\n\n  .alt-speed-label {\n    font-size: smaller;\n    font-style: lighter;\n    // nudge it up next to the section label\n    margin: -10px 0 10px -20px;\n  }\n}\n\n#prefs-dialog.ui-tabs .ui-tabs-panel {\n  padding: 0;\n  user-select: none;\n}\n\n.prefs-section {\n  text-align: left;\n\n  > * {\n    padding: 0 8px 8px;\n  }\n\n  .title {\n    font-size: larger;\n    font-weight: bold;\n    padding-left: 0;\n  }\n\n  .row {\n    .key {\n      float: left;\n      padding-top: 3px;\n\n      > * {\n        margin-left: 0;\n      }\n    }\n\n    .value {\n      margin-left: 150px;\n\n      > * {\n        width: 100%;\n      }\n    }\n  }\n\n  .checkbox-row {\n    > input {\n      margin: 0;\n    }\n\n    > label {\n      margin-left: 5px;\n    }\n  }\n\n  #alternative-speed-limits-title {\n    background: transparent url('images/blue-turtle.png') no-repeat;\n    padding-left: 18px;\n  }\n\n  #alternative-speed-limits-desc {\n    font-size: smaller;\n    padding-bottom: 4px;\n  }\n}\n\n/// TABS\n\n.tabs-container {\n  align-items: stretch;\n  background: $white;\n  border-left: solid 1px var(--color-border);\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  position: absolute;\n  right: 0;\n  top: $popup-top;\n  z-index: $z-index-popup;\n  @include for-phone-only {\n    left: 0;\n  }\n  @include for-tablet-portrait-up {\n    width: 550px;\n  }\n}\n\n.tabs-buttons {\n  align-self: center;\n  display: flex;\n  padding: 10px;\n\n  button {\n    @include button;\n    appearance: none;\n    padding: 3px;\n  }\n}\n\n.file-priority-radiobox,\n.tabs-buttons {\n  $radius: 8px;\n  :first-child {\n    border-bottom-left-radius: $radius;\n    border-top-left-radius: $radius;\n  }\n  :last-child {\n    border-bottom-right-radius: $radius;\n    border-top-right-radius: $radius;\n  }\n}\n\n.tabs-pages {\n  box-sizing: border-box;\n  flex: 1;\n  overflow-x: hidden;\n  overflow-y: auto;\n  @include for-phone-only {\n    padding: 0 8px 20px;\n  }\n  @include for-tablet-portrait-up {\n    padding: 0 20px 20px;\n  }\n}\n\n.tabs-button {\n  background-position: center;\n  background-repeat: no-repeat;\n}\n\n/// INSPECTOR\n\n#inspector {\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n}\n\n.inspector-info-page {\n  // TODO: this is (mostly) copied from prefs-dialog.\n  // can it be folded into a single set of rules?\n\n  grid-column-gap: 8px;\n  grid-row-gap: 5px;\n  grid-template-columns: auto 1fr;\n  margin: 20px;\n  padding: 12px;\n\n  &:not(.hidden) {\n    display: grid;\n  }\n\n  .section-label {\n    font-weight: bold;\n    grid-column: span 2;\n    margin-left: -20px;\n    padding-bottom: 5px;\n\n    &:not(:first-of-type) {\n      margin-top: 20px;\n    }\n  }\n\n  :not(.section-label) {\n    // this section is not copied\n    overflow-x: hidden;\n    text-overflow: ellipsis;\n  }\n}\n\n#inspector .tabs-button,\n#prefs-dialog .tabs-button {\n  background-size: 20px 20px, 40px 30px;\n  height: 30px;\n  margin: 0;\n  padding: 0;\n  width: 40px;\n}\n\n#prefs-tab-torrent {\n  @include tab-image($image-upload-download);\n}\n\n#prefs-tab-speed {\n  @include tab-image($image-speed);\n}\n\n#prefs-tab-peers {\n  @include tab-image($image-peers);\n}\n\n#prefs-tab-network {\n  @include tab-image($image-network);\n}\n\n#inspector-file-list {\n  margin: 0;\n  padding: 0;\n\n  .inspector-torrent-file-list-entry {\n    display: grid;\n    grid-column-gap: 5px;\n    grid-template-areas:\n      'check   name  priority'\n      'blank1  info  blank2';\n    grid-template-columns: 20px 1fr 65px;\n    margin-bottom: 4px;\n    padding: 5px;\n\n    &.skip {\n      opacity: 0.5;\n    }\n  }\n\n  .file-wanted-control {\n    grid-area: check;\n  }\n\n  .inspector-torrent-file-list-entry-name {\n    color: var(--color-fg-name);\n    cursor: pointer;\n    grid-area: name;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n  }\n\n  .inspector-torrent-file-list-entry.skip\n    > .inspector-torrent-file-list-entry-name {\n    color: var(--color-fg-disabled);\n  }\n\n  .inspector-torrent-file-list-entry-progress {\n    color: var(--color-fg-secondary);\n    grid-area: info;\n  }\n\n  .single-file .inspector-torrent-file-list-entry > .file-wanted-control,\n  .inspector-torrent-file-list-entry.complete > .file-wanted-control {\n    cursor: default;\n  }\n}\n\n#inspector-header {\n  margin: 8px 0;\n}\n\n#torrent-inspector-name {\n  font-size: large;\n  font-weight: bold;\n}\n\n#inspector-tiers-list {\n  color: var(--color-fg-secondary);\n  margin: 0 10px;\n  padding: 0 12px;\n\n  .tier-list-row {\n    display: grid;\n    grid-column-gap: 8px;\n    grid-row-gap: 4px;\n    margin-top: 8px;\n  }\n\n  .tier-list-torrent {\n    color: var(--color-fg-primary);\n    font-size: larger;\n    font-weight: bolder;\n    margin-left: -12px;\n    overflow-x: hidden;\n    padding-top: 20px;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n  }\n\n  .tier-list-tracker {\n    font-weight: bolder;\n    grid-column: span 2;\n    margin-top: 8px;\n    padding-bottom: 4px;\n  }\n\n  .tier-announce,\n  .tier-scrape,\n  .tier-state {\n    text-align: left;\n  }\n\n  .tier-downloads,\n  .tier-leechers,\n  .tier-seeders {\n    text-align: right;\n  }\n}\n\n// Peers Tab\n\n.peer-list {\n  $table-border-color: $grey-100;\n  $table-border: 1px solid $table-border-color;\n\n  border: $table-border;\n  border-collapse: collapse;\n  cursor: default;\n  table-layout: fixed;\n  text-align: left;\n  width: 100%;\n\n  td,\n  th {\n    font-size: smaller;\n    padding: 5px;\n  }\n\n  td {\n    border: $table-border;\n    color: var(--color-fg-secondary);\n    // padding: 5px 10px;\n  }\n\n  .torrent-row td {\n    background: $table-border-color;\n    color: var(--color-fg-primary);\n    font-size: normal;\n    font-weight: bolder;\n    overflow-x: hidden;\n    padding: 10px;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n  }\n\n  .encryption {\n    width: 16px;\n    &[data-encrypted='true'] {\n      background: transparent url($image-lock-fill) center center no-repeat;\n      height: 16px;\n      width: 16px;\n    }\n  }\n\n  .peer-address,\n  .percent-done,\n  .speed-down,\n  .speed-up {\n    text-align: right;\n  }\n\n  .percent-done {\n    width: 10%;\n  }\n  .speed-down,\n  .speed-up {\n    width: 15%;\n  }\n  .peer-address {\n    width: 25%;\n  }\n\n  .peer-app-name {\n    overflow-x: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n    @include for-phone-only {\n      display: none;\n    }\n    @include for-tablet-portrait-up {\n      width: 25%;\n    }\n  }\n\n  .status {\n    cursor: pointer;\n  }\n}\n\n/// FILE PRIORITY BUTTONS\n\n.file-priority-radiobox {\n  $size-number: 20;\n  $size: $size-number * 1px;\n  $halfsize: math.floor($size-number / 2) * 1px;\n\n  grid-area: priority;\n\n  > * {\n    @include button;\n    appearance: none;\n    background-position: center;\n    background-repeat: no-repeat;\n    background-size: $halfsize, $size;\n    height: $size;\n    margin: 0;\n    padding: 0;\n    width: $size;\n  }\n\n  > .low {\n    @include tab-image($image-chevron-dn);\n    border-right-width: 0;\n  }\n\n  > .normal {\n    @include tab-image($image-baseline);\n  }\n\n  > .high {\n    @include tab-image($image-chevron-up);\n    border-left-width: 0;\n  }\n}\n\n/// CONTEXT MENU\n\n.context-menu {\n  $background-color: var(--color-bg-menu);\n  $disabled-color: var(--color-fg-disabled);\n\n  background: $background-color;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  position: absolute;\n  z-index: $z-index-popup;\n\n  .context-menuitem {\n    list-style: none;\n    padding: 5px 10px;\n\n    &:hover:not(:disabled),\n    &:focus:not(:disabled) {\n      background-image: linear-gradient($white, var(--color-bg-menu));\n      cursor: pointer;\n    }\n\n    &:disabled {\n      color: $disabled-color;\n      cursor: none;\n    }\n  }\n}\n\n.context-menu-separator {\n  border-bottom: 1px solid var(--color-fg-secondary);\n  margin-bottom: 10px;\n  padding-bottom: 10px;\n}\n\n/// OVERFLOW MENU\n\n.overflow-menu {\n  background: linear-gradient(\n    160deg,\n    #d21,\n    1%,\n    var(--color-bg-hover),\n    40%,\n    var(--color-bg-menu)\n  );\n  border-radius: 8px;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  padding: 20px;\n  position: fixed;\n  width: 200px;\n  z-index: $z-index-popup;\n\n  fieldset {\n    border: 0;\n    margin-bottom: 8px;\n    padding: 0;\n  }\n\n  > .actions,\n  > .info,\n  > .links {\n    display: flex;\n    flex-direction: column;\n  }\n\n  legend {\n    font-weight: bolder;\n    margin-bottom: 4px;\n  }\n\n  legend ~ * {\n    margin-left: 12px;\n  }\n\n  a,\n  button,\n  label {\n    color: var(--color-fg-primary);\n    font-size: 1em;\n\n    &:disabled {\n      color: var(--color-fg-disabled);\n      cursor: default;\n    }\n\n    &:focus:not(:disabled),\n    &:hover:not(:disabled) {\n      background-color: var(--color-bg-hover);\n    }\n  }\n\n  a,\n  button {\n    background: transparent;\n    border: 0;\n    cursor: pointer;\n    display: block;\n    padding: 4px 0;\n    text-align: left;\n    text-decoration: none;\n    width: 150px;\n  }\n\n  .input-and-label {\n    align-items: center;\n    display: inline-flex;\n    flex-direction: row;\n  }\n\n  #display-options {\n    > * {\n      align-items: center;\n      display: inline-flex;\n      flex-direction: row;\n\n      > * {\n        margin: 4px;\n      }\n\n      :first-child {\n        margin-left: 0;\n      }\n\n      :last-child {\n        margin-right: 0;\n      }\n    }\n  }\n\n  #speed-options {\n    display: flex;\n    flex-direction: column;\n\n    > .speed-up,\n    > .speed-down {\n      align-items: center;\n      display: flex;\n      flex-direction: row;\n      padding: 4px 0;\n\n      > label {\n        overflow: hidden;\n        width: 80px;\n      }\n    }\n\n    .alt-speed {\n      display: grid;\n      grid-column-gap: 5px;\n      grid-row-gap: 5px;\n      grid-template-areas:\n        'check   lb'\n        'turtle  values';\n      grid-template-columns: 20px 1fr;\n      margin-bottom: 4px;\n    }\n\n    #alt-speed-check {\n      grid-area: check;\n\n      &:checked ~ #alt-speed-image {\n        background-image: url('images/blue-turtle.png');\n      }\n\n      &:not(:checked) ~ #alt-speed-image {\n        background-image: url('images/turtle.png');\n      }\n    }\n\n    #alt-speed-image {\n      background-position: center;\n      background-repeat: no-repeat;\n      grid-area: turtle;\n    }\n\n    #alt-speed-label {\n      grid-area: lb;\n    }\n\n    #alt-speed-values-label {\n      font-size: small;\n      grid-area: values;\n    }\n  }\n}\n\n// hide the fullscreen button unless we're on mobile\n@include for-tablet-portrait-up {\n  #display-fullscreen-check,\n  #display-fullscreen-label {\n    display: none;\n  }\n}\n\n/// DIALOGS\n\n$dialog-padding-number: 20;\n$dialog-logo-padding-number: $dialog-padding-number * 2 / 3;\n$logo-size: 64px;\n\n@include for-phone-only {\n  .dialog-buttons {\n    padding-top: $dialog-logo-padding-number * 1px;\n  }\n\n  .dialog-container {\n    opacity: 96%;\n    position: absolute;\n    top: $popup-top;\n    width: 100%;\n  }\n\n  .dialog-logo {\n    padding-bottom: $dialog-logo-padding-number * 1px;\n  }\n\n  .dialog-window {\n    align-items: center;\n    display: flex;\n    flex-direction: column;\n  }\n}\n@include for-tablet-portrait-up {\n  .dialog-container {\n    left: 50%;\n    min-width: 400px;\n    position: absolute;\n    top: $popup-top * 2;\n    transform: translateX(-50%);\n  }\n\n  .dialog-heading {\n    grid-area: heading;\n  }\n\n  .dialog-logo {\n    grid-area: icon;\n    padding-right: $dialog-logo-padding-number * 1px;\n  }\n\n  .dialog-window {\n    display: grid;\n    grid-column-gap: 12px;\n    grid-template-areas:\n      'icon heading'\n      'icon message'\n      'icon workarea'\n      'icon buttons';\n    grid-template-columns: $logo-size 1fr;\n    padding: 2px 12px;\n  }\n}\n\n.dialog-buttons {\n  display: flex;\n  float: right;\n  grid-area: buttons;\n  margin: 10px 0 0;\n  text-align: center;\n\n  button {\n    appearance: none;\n    background: transparent;\n    border: 1px solid var(--color-border);\n    border-radius: 5px;\n    color: var(--color-fg-name);\n    margin-left: 8px;\n    padding: 8px;\n    text-decoration: none;\n  }\n\n  a:hover,\n  a:active {\n    background: linear-gradient($white, var(--color-bg-menu));\n  }\n}\n\n.dialog-buttons-begin {\n  flex-grow: 1;\n}\n\n.dialog-buttons-end {\n  display: none;\n}\n\n.dialog-container {\n  border: 0;\n  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.7);\n  color: var(--color-fg-primary);\n  display: block;\n  margin: 0;\n  padding: 0;\n  z-index: $z-index-popup;\n}\n\n.dialog-heading {\n  align-items: center;\n  display: flex;\n  font-size: 1.2em;\n  font-weight: bold;\n}\n\n.dialog-logo {\n  background: transparent url('images/logo.png') top left no-repeat;\n  height: $logo-size;\n  width: $logo-size;\n}\n\n.dialog-message {\n  grid-area: message;\n  margin: 10px 0 0;\n}\n\n.dialog-window {\n  background: linear-gradient(160deg, #d21, 1%, #f7f7f7, 40%, #ccc);\n  height: 100%;\n  padding: $dialog-padding-number * 1px;\n}\n\n.dialog-workarea {\n  display: flex;\n  flex-direction: column;\n  grid-area: workarea;\n  margin: 10px 0 0;\n\n  > * {\n    margin-bottom: 5px;\n  }\n}\n\n/// ABOUT DIALOG\n\n.about-dialog .dialog-workarea > * {\n  margin-bottom: 10px;\n}\n\n.about-dialog-version-number {\n  margin-left: 4px;\n}\n\n.about-dialog-version-checksum {\n  font-size: small;\n  font-style: italic;\n  font-weight: normal;\n  margin-left: auto;\n  padding-left: 10px;\n}\n\n/// HOTKEYS DIALOG\n\n.shortcuts-dialog {\n  table {\n    border-collapse: collapse;\n    width: 100%;\n  }\n\n  th,\n  td {\n    border: 1px solid var(--color-border);\n    padding: 5px 10px;\n  }\n}\n\n/// RENAME DIALOG\n\n#torrent-rename-name {\n  min-width: 400px;\n}\n\n/// SET-LOCATION DIALOG\n\n#move-container #torrent-path {\n  min-width: 400px;\n}\n\n/// STATISTICS DIALOG\n\n.tabs-pages,\n.inspector-info-page,\n.statistics-dialog {\n  .content {\n    column-gap: 16px;\n    display: grid;\n    grid-row-gap: 6px;\n    grid-template-columns: auto 1fr;\n    margin-top: 4px;\n\n    @include for-phone-only {\n      margin: 4px 0 16px;\n    }\n    @include for-tablet-portrait-up {\n      margin: 4px 16px 16px;\n    }\n\n    div {\n      word-break: break-word;\n    }\n  }\n\n  fieldset {\n    border: 0;\n    margin: 0;\n    padding: 0;\n  }\n\n  legend {\n    font-weight: bolder;\n    margin-bottom: 4px;\n  }\n}\n\n/// UPLOAD DIALOG\n\n#free-space-text {\n  display: inline-block;\n  float: right;\n  font-size: smaller;\n  font-style: italic;\n}\n\n#torrent-upload-frame {\n  border: 0;\n  display: block; /* Don't change this : safari forms won't target hidden frames (they open a new window) */\n  height: 0;\n  left: -1000px;\n  margin: 0;\n  padding: 0;\n  position: absolute;\n  top: -1000px;\n  width: 0;\n}\n\n.ui-menu {\n  width: 200px;\n}\n"],"sourceRoot":""}]);
// Exports
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/api.js":
/*!*****************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/api.js ***!
  \*****************************************************/
/***/ ((module) => {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (cssWithMappingToString) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join("");
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === "string") {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, ""]];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/cssWithMappingToString.js":
/*!************************************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/cssWithMappingToString.js ***!
  \************************************************************************/
/***/ ((module) => {

"use strict";


function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return; var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

module.exports = function cssWithMappingToString(item) {
  var _item = _slicedToArray(item, 4),
      content = _item[1],
      cssMapping = _item[3];

  if (typeof btoa === "function") {
    // eslint-disable-next-line no-undef
    var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(cssMapping))));
    var data = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(base64);
    var sourceMapping = "/*# ".concat(data, " */");
    var sourceURLs = cssMapping.sources.map(function (source) {
      return "/*# sourceURL=".concat(cssMapping.sourceRoot || "").concat(source, " */");
    });
    return [content].concat(sourceURLs).concat([sourceMapping]).join("\n");
  }

  return [content].join("\n");
};

/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/getUrl.js":
/*!********************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/getUrl.js ***!
  \********************************************************/
/***/ ((module) => {

"use strict";


module.exports = function (url, options) {
  if (!options) {
    // eslint-disable-next-line no-param-reassign
    options = {};
  } // eslint-disable-next-line no-underscore-dangle, no-param-reassign


  url = url && url.__esModule ? url.default : url;

  if (typeof url !== "string") {
    return url;
  } // If url is already wrapped in quotes, remove them


  if (/^['"].*['"]$/.test(url)) {
    // eslint-disable-next-line no-param-reassign
    url = url.slice(1, -1);
  }

  if (options.hash) {
    // eslint-disable-next-line no-param-reassign
    url += options.hash;
  } // Should url be wrapped?
  // See https://drafts.csswg.org/css-values-3/#urls


  if (/["'() \t\n]/.test(url) || options.needQuotes) {
    return "\"".concat(url.replace(/"/g, '\\"').replace(/\n/g, "\\n"), "\"");
  }

  return url;
};

/***/ }),

/***/ "./node_modules/lodash.isequal/index.js":
/*!**********************************************!*\
  !*** ./node_modules/lodash.isequal/index.js ***!
  \**********************************************/
/***/ ((module, exports, __webpack_require__) => {

/* module decorator */ module = __webpack_require__.nmd(module);
/**
 * Lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright JS Foundation and other contributors <https://js.foundation/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the size to enable large array optimizations. */
var LARGE_ARRAY_SIZE = 200;

/** Used to stand-in for `undefined` hash values. */
var HASH_UNDEFINED = '__lodash_hash_undefined__';

/** Used to compose bitmasks for value comparisons. */
var COMPARE_PARTIAL_FLAG = 1,
    COMPARE_UNORDERED_FLAG = 2;

/** Used as references for various `Number` constants. */
var MAX_SAFE_INTEGER = 9007199254740991;

/** `Object#toString` result references. */
var argsTag = '[object Arguments]',
    arrayTag = '[object Array]',
    asyncTag = '[object AsyncFunction]',
    boolTag = '[object Boolean]',
    dateTag = '[object Date]',
    errorTag = '[object Error]',
    funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]',
    mapTag = '[object Map]',
    numberTag = '[object Number]',
    nullTag = '[object Null]',
    objectTag = '[object Object]',
    promiseTag = '[object Promise]',
    proxyTag = '[object Proxy]',
    regexpTag = '[object RegExp]',
    setTag = '[object Set]',
    stringTag = '[object String]',
    symbolTag = '[object Symbol]',
    undefinedTag = '[object Undefined]',
    weakMapTag = '[object WeakMap]';

var arrayBufferTag = '[object ArrayBuffer]',
    dataViewTag = '[object DataView]',
    float32Tag = '[object Float32Array]',
    float64Tag = '[object Float64Array]',
    int8Tag = '[object Int8Array]',
    int16Tag = '[object Int16Array]',
    int32Tag = '[object Int32Array]',
    uint8Tag = '[object Uint8Array]',
    uint8ClampedTag = '[object Uint8ClampedArray]',
    uint16Tag = '[object Uint16Array]',
    uint32Tag = '[object Uint32Array]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Used to detect unsigned integer values. */
var reIsUint = /^(?:0|[1-9]\d*)$/;

/** Used to identify `toStringTag` values of typed arrays. */
var typedArrayTags = {};
typedArrayTags[float32Tag] = typedArrayTags[float64Tag] =
typedArrayTags[int8Tag] = typedArrayTags[int16Tag] =
typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] =
typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] =
typedArrayTags[uint32Tag] = true;
typedArrayTags[argsTag] = typedArrayTags[arrayTag] =
typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] =
typedArrayTags[dataViewTag] = typedArrayTags[dateTag] =
typedArrayTags[errorTag] = typedArrayTags[funcTag] =
typedArrayTags[mapTag] = typedArrayTags[numberTag] =
typedArrayTags[objectTag] = typedArrayTags[regexpTag] =
typedArrayTags[setTag] = typedArrayTags[stringTag] =
typedArrayTags[weakMapTag] = false;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof __webpack_require__.g == 'object' && __webpack_require__.g && __webpack_require__.g.Object === Object && __webpack_require__.g;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Detect free variable `exports`. */
var freeExports =  true && exports && !exports.nodeType && exports;

/** Detect free variable `module`. */
var freeModule = freeExports && "object" == 'object' && module && !module.nodeType && module;

/** Detect the popular CommonJS extension `module.exports`. */
var moduleExports = freeModule && freeModule.exports === freeExports;

/** Detect free variable `process` from Node.js. */
var freeProcess = moduleExports && freeGlobal.process;

/** Used to access faster Node.js helpers. */
var nodeUtil = (function() {
  try {
    return freeProcess && freeProcess.binding && freeProcess.binding('util');
  } catch (e) {}
}());

/* Node.js helper references. */
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;

/**
 * A specialized version of `_.filter` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param {Array} [array] The array to iterate over.
 * @param {Function} predicate The function invoked per iteration.
 * @returns {Array} Returns the new filtered array.
 */
function arrayFilter(array, predicate) {
  var index = -1,
      length = array == null ? 0 : array.length,
      resIndex = 0,
      result = [];

  while (++index < length) {
    var value = array[index];
    if (predicate(value, index, array)) {
      result[resIndex++] = value;
    }
  }
  return result;
}

/**
 * Appends the elements of `values` to `array`.
 *
 * @private
 * @param {Array} array The array to modify.
 * @param {Array} values The values to append.
 * @returns {Array} Returns `array`.
 */
function arrayPush(array, values) {
  var index = -1,
      length = values.length,
      offset = array.length;

  while (++index < length) {
    array[offset + index] = values[index];
  }
  return array;
}

/**
 * A specialized version of `_.some` for arrays without support for iteratee
 * shorthands.
 *
 * @private
 * @param {Array} [array] The array to iterate over.
 * @param {Function} predicate The function invoked per iteration.
 * @returns {boolean} Returns `true` if any element passes the predicate check,
 *  else `false`.
 */
function arraySome(array, predicate) {
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) {
    if (predicate(array[index], index, array)) {
      return true;
    }
  }
  return false;
}

/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param {number} n The number of times to invoke `iteratee`.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the array of results.
 */
function baseTimes(n, iteratee) {
  var index = -1,
      result = Array(n);

  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}

/**
 * The base implementation of `_.unary` without support for storing metadata.
 *
 * @private
 * @param {Function} func The function to cap arguments for.
 * @returns {Function} Returns the new capped function.
 */
function baseUnary(func) {
  return function(value) {
    return func(value);
  };
}

/**
 * Checks if a `cache` value for `key` exists.
 *
 * @private
 * @param {Object} cache The cache to query.
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function cacheHas(cache, key) {
  return cache.has(key);
}

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Converts `map` to its key-value pairs.
 *
 * @private
 * @param {Object} map The map to convert.
 * @returns {Array} Returns the key-value pairs.
 */
function mapToArray(map) {
  var index = -1,
      result = Array(map.size);

  map.forEach(function(value, key) {
    result[++index] = [key, value];
  });
  return result;
}

/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/**
 * Converts `set` to an array of its values.
 *
 * @private
 * @param {Object} set The set to convert.
 * @returns {Array} Returns the values.
 */
function setToArray(set) {
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) {
    result[++index] = value;
  });
  return result;
}

/** Used for built-in method references. */
var arrayProto = Array.prototype,
    funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var Buffer = moduleExports ? root.Buffer : undefined,
    Symbol = root.Symbol,
    Uint8Array = root.Uint8Array,
    propertyIsEnumerable = objectProto.propertyIsEnumerable,
    splice = arrayProto.splice,
    symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeGetSymbols = Object.getOwnPropertySymbols,
    nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined,
    nativeKeys = overArg(Object.keys, Object);

/* Built-in method references that are verified to be native. */
var DataView = getNative(root, 'DataView'),
    Map = getNative(root, 'Map'),
    Promise = getNative(root, 'Promise'),
    Set = getNative(root, 'Set'),
    WeakMap = getNative(root, 'WeakMap'),
    nativeCreate = getNative(Object, 'create');

/** Used to detect maps, sets, and weakmaps. */
var dataViewCtorString = toSource(DataView),
    mapCtorString = toSource(Map),
    promiseCtorString = toSource(Promise),
    setCtorString = toSource(Set),
    weakMapCtorString = toSource(WeakMap);

/** Used to convert symbols to primitives and strings. */
var symbolProto = Symbol ? Symbol.prototype : undefined,
    symbolValueOf = symbolProto ? symbolProto.valueOf : undefined;

/**
 * Creates a hash object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Hash(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the hash.
 *
 * @private
 * @name clear
 * @memberOf Hash
 */
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
  this.size = 0;
}

/**
 * Removes `key` and its value from the hash.
 *
 * @private
 * @name delete
 * @memberOf Hash
 * @param {Object} hash The hash to modify.
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function hashDelete(key) {
  var result = this.has(key) && delete this.__data__[key];
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the hash value for `key`.
 *
 * @private
 * @name get
 * @memberOf Hash
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED ? undefined : result;
  }
  return hasOwnProperty.call(data, key) ? data[key] : undefined;
}

/**
 * Checks if a hash value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Hash
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? (data[key] !== undefined) : hasOwnProperty.call(data, key);
}

/**
 * Sets the hash `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Hash
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the hash instance.
 */
function hashSet(key, value) {
  var data = this.__data__;
  this.size += this.has(key) ? 0 : 1;
  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;
  return this;
}

// Add methods to `Hash`.
Hash.prototype.clear = hashClear;
Hash.prototype['delete'] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;

/**
 * Creates an list cache object.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function ListCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the list cache.
 *
 * @private
 * @name clear
 * @memberOf ListCache
 */
function listCacheClear() {
  this.__data__ = [];
  this.size = 0;
}

/**
 * Removes `key` and its value from the list cache.
 *
 * @private
 * @name delete
 * @memberOf ListCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function listCacheDelete(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index, 1);
  }
  --this.size;
  return true;
}

/**
 * Gets the list cache value for `key`.
 *
 * @private
 * @name get
 * @memberOf ListCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function listCacheGet(key) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  return index < 0 ? undefined : data[index][1];
}

/**
 * Checks if a list cache value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf ListCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}

/**
 * Sets the list cache `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf ListCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the list cache instance.
 */
function listCacheSet(key, value) {
  var data = this.__data__,
      index = assocIndexOf(data, key);

  if (index < 0) {
    ++this.size;
    data.push([key, value]);
  } else {
    data[index][1] = value;
  }
  return this;
}

// Add methods to `ListCache`.
ListCache.prototype.clear = listCacheClear;
ListCache.prototype['delete'] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;

/**
 * Creates a map cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function MapCache(entries) {
  var index = -1,
      length = entries == null ? 0 : entries.length;

  this.clear();
  while (++index < length) {
    var entry = entries[index];
    this.set(entry[0], entry[1]);
  }
}

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

/**
 * Removes `key` and its value from the map.
 *
 * @private
 * @name delete
 * @memberOf MapCache
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function mapCacheDelete(key) {
  var result = getMapData(this, key)['delete'](key);
  this.size -= result ? 1 : 0;
  return result;
}

/**
 * Gets the map value for `key`.
 *
 * @private
 * @name get
 * @memberOf MapCache
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}

/**
 * Checks if a map value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf MapCache
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}

/**
 * Sets the map `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf MapCache
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the map cache instance.
 */
function mapCacheSet(key, value) {
  var data = getMapData(this, key),
      size = data.size;

  data.set(key, value);
  this.size += data.size == size ? 0 : 1;
  return this;
}

// Add methods to `MapCache`.
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype['delete'] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;

/**
 *
 * Creates an array cache object to store unique values.
 *
 * @private
 * @constructor
 * @param {Array} [values] The values to cache.
 */
function SetCache(values) {
  var index = -1,
      length = values == null ? 0 : values.length;

  this.__data__ = new MapCache;
  while (++index < length) {
    this.add(values[index]);
  }
}

/**
 * Adds `value` to the array cache.
 *
 * @private
 * @name add
 * @memberOf SetCache
 * @alias push
 * @param {*} value The value to cache.
 * @returns {Object} Returns the cache instance.
 */
function setCacheAdd(value) {
  this.__data__.set(value, HASH_UNDEFINED);
  return this;
}

/**
 * Checks if `value` is in the array cache.
 *
 * @private
 * @name has
 * @memberOf SetCache
 * @param {*} value The value to search for.
 * @returns {number} Returns `true` if `value` is found, else `false`.
 */
function setCacheHas(value) {
  return this.__data__.has(value);
}

// Add methods to `SetCache`.
SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
SetCache.prototype.has = setCacheHas;

/**
 * Creates a stack cache object to store key-value pairs.
 *
 * @private
 * @constructor
 * @param {Array} [entries] The key-value pairs to cache.
 */
function Stack(entries) {
  var data = this.__data__ = new ListCache(entries);
  this.size = data.size;
}

/**
 * Removes all key-value entries from the stack.
 *
 * @private
 * @name clear
 * @memberOf Stack
 */
function stackClear() {
  this.__data__ = new ListCache;
  this.size = 0;
}

/**
 * Removes `key` and its value from the stack.
 *
 * @private
 * @name delete
 * @memberOf Stack
 * @param {string} key The key of the value to remove.
 * @returns {boolean} Returns `true` if the entry was removed, else `false`.
 */
function stackDelete(key) {
  var data = this.__data__,
      result = data['delete'](key);

  this.size = data.size;
  return result;
}

/**
 * Gets the stack value for `key`.
 *
 * @private
 * @name get
 * @memberOf Stack
 * @param {string} key The key of the value to get.
 * @returns {*} Returns the entry value.
 */
function stackGet(key) {
  return this.__data__.get(key);
}

/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param {string} key The key of the entry to check.
 * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) {
  return this.__data__.has(key);
}

/**
 * Sets the stack `key` to `value`.
 *
 * @private
 * @name set
 * @memberOf Stack
 * @param {string} key The key of the value to set.
 * @param {*} value The value to set.
 * @returns {Object} Returns the stack cache instance.
 */
function stackSet(key, value) {
  var data = this.__data__;
  if (data instanceof ListCache) {
    var pairs = data.__data__;
    if (!Map || (pairs.length < LARGE_ARRAY_SIZE - 1)) {
      pairs.push([key, value]);
      this.size = ++data.size;
      return this;
    }
    data = this.__data__ = new MapCache(pairs);
  }
  data.set(key, value);
  this.size = data.size;
  return this;
}

// Add methods to `Stack`.
Stack.prototype.clear = stackClear;
Stack.prototype['delete'] = stackDelete;
Stack.prototype.get = stackGet;
Stack.prototype.has = stackHas;
Stack.prototype.set = stackSet;

/**
 * Creates an array of the enumerable property names of the array-like `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @param {boolean} inherited Specify returning inherited property names.
 * @returns {Array} Returns the array of property names.
 */
function arrayLikeKeys(value, inherited) {
  var isArr = isArray(value),
      isArg = !isArr && isArguments(value),
      isBuff = !isArr && !isArg && isBuffer(value),
      isType = !isArr && !isArg && !isBuff && isTypedArray(value),
      skipIndexes = isArr || isArg || isBuff || isType,
      result = skipIndexes ? baseTimes(value.length, String) : [],
      length = result.length;

  for (var key in value) {
    if ((inherited || hasOwnProperty.call(value, key)) &&
        !(skipIndexes && (
           // Safari 9 has enumerable `arguments.length` in strict mode.
           key == 'length' ||
           // Node.js 0.10 has enumerable non-index properties on buffers.
           (isBuff && (key == 'offset' || key == 'parent')) ||
           // PhantomJS 2 has enumerable non-index properties on typed arrays.
           (isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset')) ||
           // Skip index properties.
           isIndex(key, length)
        ))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array} array The array to inspect.
 * @param {*} key The key to search for.
 * @returns {number} Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    }
  }
  return -1;
}

/**
 * The base implementation of `getAllKeys` and `getAllKeysIn` which uses
 * `keysFunc` and `symbolsFunc` to get the enumerable property names and
 * symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {Function} keysFunc The function to get the keys of `object`.
 * @param {Function} symbolsFunc The function to get the symbols of `object`.
 * @returns {Array} Returns the array of property names and symbols.
 */
function baseGetAllKeys(object, keysFunc, symbolsFunc) {
  var result = keysFunc(object);
  return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
}

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

/**
 * The base implementation of `_.isArguments`.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 */
function baseIsArguments(value) {
  return isObjectLike(value) && baseGetTag(value) == argsTag;
}

/**
 * The base implementation of `_.isEqual` which supports partial comparisons
 * and tracks traversed objects.
 *
 * @private
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @param {boolean} bitmask The bitmask flags.
 *  1 - Unordered comparison
 *  2 - Partial comparison
 * @param {Function} [customizer] The function to customize comparisons.
 * @param {Object} [stack] Tracks traversed `value` and `other` objects.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 */
function baseIsEqual(value, other, bitmask, customizer, stack) {
  if (value === other) {
    return true;
  }
  if (value == null || other == null || (!isObjectLike(value) && !isObjectLike(other))) {
    return value !== value && other !== other;
  }
  return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
}

/**
 * A specialized version of `baseIsEqual` for arrays and objects which performs
 * deep comparisons and tracks traversed objects enabling objects with circular
 * references to be compared.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} [stack] Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
  var objIsArr = isArray(object),
      othIsArr = isArray(other),
      objTag = objIsArr ? arrayTag : getTag(object),
      othTag = othIsArr ? arrayTag : getTag(other);

  objTag = objTag == argsTag ? objectTag : objTag;
  othTag = othTag == argsTag ? objectTag : othTag;

  var objIsObj = objTag == objectTag,
      othIsObj = othTag == objectTag,
      isSameTag = objTag == othTag;

  if (isSameTag && isBuffer(object)) {
    if (!isBuffer(other)) {
      return false;
    }
    objIsArr = true;
    objIsObj = false;
  }
  if (isSameTag && !objIsObj) {
    stack || (stack = new Stack);
    return (objIsArr || isTypedArray(object))
      ? equalArrays(object, other, bitmask, customizer, equalFunc, stack)
      : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
  }
  if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
    var objIsWrapped = objIsObj && hasOwnProperty.call(object, '__wrapped__'),
        othIsWrapped = othIsObj && hasOwnProperty.call(other, '__wrapped__');

    if (objIsWrapped || othIsWrapped) {
      var objUnwrapped = objIsWrapped ? object.value() : object,
          othUnwrapped = othIsWrapped ? other.value() : other;

      stack || (stack = new Stack);
      return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
    }
  }
  if (!isSameTag) {
    return false;
  }
  stack || (stack = new Stack);
  return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * The base implementation of `_.isTypedArray` without Node.js optimizations.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 */
function baseIsTypedArray(value) {
  return isObjectLike(value) &&
    isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
}

/**
 * The base implementation of `_.keys` which doesn't treat sparse arrays as dense.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function baseKeys(object) {
  if (!isPrototype(object)) {
    return nativeKeys(object);
  }
  var result = [];
  for (var key in Object(object)) {
    if (hasOwnProperty.call(object, key) && key != 'constructor') {
      result.push(key);
    }
  }
  return result;
}

/**
 * A specialized version of `baseIsEqualDeep` for arrays with support for
 * partial deep comparisons.
 *
 * @private
 * @param {Array} array The array to compare.
 * @param {Array} other The other array to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `array` and `other` objects.
 * @returns {boolean} Returns `true` if the arrays are equivalent, else `false`.
 */
function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG,
      arrLength = array.length,
      othLength = other.length;

  if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
    return false;
  }
  // Assume cyclic values are equal.
  var stacked = stack.get(array);
  if (stacked && stack.get(other)) {
    return stacked == other;
  }
  var index = -1,
      result = true,
      seen = (bitmask & COMPARE_UNORDERED_FLAG) ? new SetCache : undefined;

  stack.set(array, other);
  stack.set(other, array);

  // Ignore non-index properties.
  while (++index < arrLength) {
    var arrValue = array[index],
        othValue = other[index];

    if (customizer) {
      var compared = isPartial
        ? customizer(othValue, arrValue, index, other, array, stack)
        : customizer(arrValue, othValue, index, array, other, stack);
    }
    if (compared !== undefined) {
      if (compared) {
        continue;
      }
      result = false;
      break;
    }
    // Recursively compare arrays (susceptible to call stack limits).
    if (seen) {
      if (!arraySome(other, function(othValue, othIndex) {
            if (!cacheHas(seen, othIndex) &&
                (arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
              return seen.push(othIndex);
            }
          })) {
        result = false;
        break;
      }
    } else if (!(
          arrValue === othValue ||
            equalFunc(arrValue, othValue, bitmask, customizer, stack)
        )) {
      result = false;
      break;
    }
  }
  stack['delete'](array);
  stack['delete'](other);
  return result;
}

/**
 * A specialized version of `baseIsEqualDeep` for comparing objects of
 * the same `toStringTag`.
 *
 * **Note:** This function only supports comparing values with tags of
 * `Boolean`, `Date`, `Error`, `Number`, `RegExp`, or `String`.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {string} tag The `toStringTag` of the objects to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
  switch (tag) {
    case dataViewTag:
      if ((object.byteLength != other.byteLength) ||
          (object.byteOffset != other.byteOffset)) {
        return false;
      }
      object = object.buffer;
      other = other.buffer;

    case arrayBufferTag:
      if ((object.byteLength != other.byteLength) ||
          !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
        return false;
      }
      return true;

    case boolTag:
    case dateTag:
    case numberTag:
      // Coerce booleans to `1` or `0` and dates to milliseconds.
      // Invalid dates are coerced to `NaN`.
      return eq(+object, +other);

    case errorTag:
      return object.name == other.name && object.message == other.message;

    case regexpTag:
    case stringTag:
      // Coerce regexes to strings and treat strings, primitives and objects,
      // as equal. See http://www.ecma-international.org/ecma-262/7.0/#sec-regexp.prototype.tostring
      // for more details.
      return object == (other + '');

    case mapTag:
      var convert = mapToArray;

    case setTag:
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
      convert || (convert = setToArray);

      if (object.size != other.size && !isPartial) {
        return false;
      }
      // Assume cyclic values are equal.
      var stacked = stack.get(object);
      if (stacked) {
        return stacked == other;
      }
      bitmask |= COMPARE_UNORDERED_FLAG;

      // Recursively compare objects (susceptible to call stack limits).
      stack.set(object, other);
      var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
      stack['delete'](object);
      return result;

    case symbolTag:
      if (symbolValueOf) {
        return symbolValueOf.call(object) == symbolValueOf.call(other);
      }
  }
  return false;
}

/**
 * A specialized version of `baseIsEqualDeep` for objects with support for
 * partial deep comparisons.
 *
 * @private
 * @param {Object} object The object to compare.
 * @param {Object} other The other object to compare.
 * @param {number} bitmask The bitmask flags. See `baseIsEqual` for more details.
 * @param {Function} customizer The function to customize comparisons.
 * @param {Function} equalFunc The function to determine equivalents of values.
 * @param {Object} stack Tracks traversed `object` and `other` objects.
 * @returns {boolean} Returns `true` if the objects are equivalent, else `false`.
 */
function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG,
      objProps = getAllKeys(object),
      objLength = objProps.length,
      othProps = getAllKeys(other),
      othLength = othProps.length;

  if (objLength != othLength && !isPartial) {
    return false;
  }
  var index = objLength;
  while (index--) {
    var key = objProps[index];
    if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
      return false;
    }
  }
  // Assume cyclic values are equal.
  var stacked = stack.get(object);
  if (stacked && stack.get(other)) {
    return stacked == other;
  }
  var result = true;
  stack.set(object, other);
  stack.set(other, object);

  var skipCtor = isPartial;
  while (++index < objLength) {
    key = objProps[index];
    var objValue = object[key],
        othValue = other[key];

    if (customizer) {
      var compared = isPartial
        ? customizer(othValue, objValue, key, other, object, stack)
        : customizer(objValue, othValue, key, object, other, stack);
    }
    // Recursively compare objects (susceptible to call stack limits).
    if (!(compared === undefined
          ? (objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack))
          : compared
        )) {
      result = false;
      break;
    }
    skipCtor || (skipCtor = key == 'constructor');
  }
  if (result && !skipCtor) {
    var objCtor = object.constructor,
        othCtor = other.constructor;

    // Non `Object` object instances with different constructors are not equal.
    if (objCtor != othCtor &&
        ('constructor' in object && 'constructor' in other) &&
        !(typeof objCtor == 'function' && objCtor instanceof objCtor &&
          typeof othCtor == 'function' && othCtor instanceof othCtor)) {
      result = false;
    }
  }
  stack['delete'](object);
  stack['delete'](other);
  return result;
}

/**
 * Creates an array of own enumerable property names and symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names and symbols.
 */
function getAllKeys(object) {
  return baseGetAllKeys(object, keys, getSymbols);
}

/**
 * Gets the data for `map`.
 *
 * @private
 * @param {Object} map The map to query.
 * @param {string} key The reference key.
 * @returns {*} Returns the map data.
 */
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key)
    ? data[typeof key == 'string' ? 'string' : 'hash']
    : data.map;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/**
 * Creates an array of the own enumerable symbols of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of symbols.
 */
var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
  if (object == null) {
    return [];
  }
  object = Object(object);
  return arrayFilter(nativeGetSymbols(object), function(symbol) {
    return propertyIsEnumerable.call(object, symbol);
  });
};

/**
 * Gets the `toStringTag` of `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
var getTag = baseGetTag;

// Fallback for data views, maps, sets, and weak maps in IE 11 and promises in Node.js < 6.
if ((DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag) ||
    (Map && getTag(new Map) != mapTag) ||
    (Promise && getTag(Promise.resolve()) != promiseTag) ||
    (Set && getTag(new Set) != setTag) ||
    (WeakMap && getTag(new WeakMap) != weakMapTag)) {
  getTag = function(value) {
    var result = baseGetTag(value),
        Ctor = result == objectTag ? value.constructor : undefined,
        ctorString = Ctor ? toSource(Ctor) : '';

    if (ctorString) {
      switch (ctorString) {
        case dataViewCtorString: return dataViewTag;
        case mapCtorString: return mapTag;
        case promiseCtorString: return promiseTag;
        case setCtorString: return setTag;
        case weakMapCtorString: return weakMapTag;
      }
    }
    return result;
  };
}

/**
 * Checks if `value` is a valid array-like index.
 *
 * @private
 * @param {*} value The value to check.
 * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
 * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
 */
function isIndex(value, length) {
  length = length == null ? MAX_SAFE_INTEGER : length;
  return !!length &&
    (typeof value == 'number' || reIsUint.test(value)) &&
    (value > -1 && value % 1 == 0 && value < length);
}

/**
 * Checks if `value` is suitable for use as unique object key.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is suitable, else `false`.
 */
function isKeyable(value) {
  var type = typeof value;
  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')
    ? (value !== '__proto__')
    : (value === null);
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Checks if `value` is likely a prototype object.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
 */
function isPrototype(value) {
  var Ctor = value && value.constructor,
      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;

  return value === proto;
}

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to convert.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is likely an `arguments` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 *  else `false`.
 * @example
 *
 * _.isArguments(function() { return arguments; }());
 * // => true
 *
 * _.isArguments([1, 2, 3]);
 * // => false
 */
var isArguments = baseIsArguments(function() { return arguments; }()) ? baseIsArguments : function(value) {
  return isObjectLike(value) && hasOwnProperty.call(value, 'callee') &&
    !propertyIsEnumerable.call(value, 'callee');
};

/**
 * Checks if `value` is classified as an `Array` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array, else `false`.
 * @example
 *
 * _.isArray([1, 2, 3]);
 * // => true
 *
 * _.isArray(document.body.children);
 * // => false
 *
 * _.isArray('abc');
 * // => false
 *
 * _.isArray(_.noop);
 * // => false
 */
var isArray = Array.isArray;

/**
 * Checks if `value` is array-like. A value is considered array-like if it's
 * not a function and has a `value.length` that's an integer greater than or
 * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
 * @example
 *
 * _.isArrayLike([1, 2, 3]);
 * // => true
 *
 * _.isArrayLike(document.body.children);
 * // => true
 *
 * _.isArrayLike('abc');
 * // => true
 *
 * _.isArrayLike(_.noop);
 * // => false
 */
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}

/**
 * Checks if `value` is a buffer.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.
 * @example
 *
 * _.isBuffer(new Buffer(2));
 * // => true
 *
 * _.isBuffer(new Uint8Array(2));
 * // => false
 */
var isBuffer = nativeIsBuffer || stubFalse;

/**
 * Performs a deep comparison between two values to determine if they are
 * equivalent.
 *
 * **Note:** This method supports comparing arrays, array buffers, booleans,
 * date objects, error objects, maps, numbers, `Object` objects, regexes,
 * sets, strings, symbols, and typed arrays. `Object` objects are compared
 * by their own, not inherited, enumerable properties. Functions and DOM
 * nodes are compared by strict equality, i.e. `===`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.isEqual(object, other);
 * // => true
 *
 * object === other;
 * // => false
 */
function isEqual(value, other) {
  return baseIsEqual(value, other);
}

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  if (!isObject(value)) {
    return false;
  }
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 9 which returns 'object' for typed arrays and other constructors.
  var tag = baseGetTag(value);
  return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
}

/**
 * Checks if `value` is a valid array-like length.
 *
 * **Note:** This method is loosely based on
 * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
 * @example
 *
 * _.isLength(3);
 * // => true
 *
 * _.isLength(Number.MIN_VALUE);
 * // => false
 *
 * _.isLength(Infinity);
 * // => false
 *
 * _.isLength('3');
 * // => false
 */
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/**
 * Checks if `value` is classified as a typed array.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 * @example
 *
 * _.isTypedArray(new Uint8Array);
 * // => true
 *
 * _.isTypedArray([]);
 * // => false
 */
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;

/**
 * Creates an array of the own enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects. See the
 * [ES spec](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
 * for more details.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keys(new Foo);
 * // => ['a', 'b'] (iteration order is not guaranteed)
 *
 * _.keys('hi');
 * // => ['0', '1']
 */
function keys(object) {
  return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
}

/**
 * This method returns a new empty array.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {Array} Returns the new empty array.
 * @example
 *
 * var arrays = _.times(2, _.stubArray);
 *
 * console.log(arrays);
 * // => [[], []]
 *
 * console.log(arrays[0] === arrays[1]);
 * // => false
 */
function stubArray() {
  return [];
}

/**
 * This method returns `false`.
 *
 * @static
 * @memberOf _
 * @since 4.13.0
 * @category Util
 * @returns {boolean} Returns `false`.
 * @example
 *
 * _.times(2, _.stubFalse);
 * // => [false, false]
 */
function stubFalse() {
  return false;
}

module.exports = isEqual;


/***/ }),

/***/ "./style/transmission-app.scss":
/*!*************************************!*\
  !*** ./style/transmission-app.scss ***!
  \*************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! !../node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js */ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js");
/* harmony import */ var _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_cjs_js_node_modules_sass_loader_dist_cjs_js_transmission_app_scss__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! !!../node_modules/css-loader/dist/cjs.js!../node_modules/sass-loader/dist/cjs.js!./transmission-app.scss */ "./node_modules/css-loader/dist/cjs.js!./node_modules/sass-loader/dist/cjs.js!./style/transmission-app.scss");

            

var options = {};

options.insert = "head";
options.singleton = false;

var update = _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default()(_node_modules_css_loader_dist_cjs_js_node_modules_sass_loader_dist_cjs_js_transmission_app_scss__WEBPACK_IMPORTED_MODULE_1__.default, options);



/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_node_modules_css_loader_dist_cjs_js_node_modules_sass_loader_dist_cjs_js_transmission_app_scss__WEBPACK_IMPORTED_MODULE_1__.default.locals || {});

/***/ }),

/***/ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js":
/*!****************************************************************************!*\
  !*** ./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js ***!
  \****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ "./style/images/analytics.svg":
/*!************************************!*\
  !*** ./style/images/analytics.svg ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/checkered-flag.svg":
/*!*****************************************!*\
  !*** ./style/images/checkered-flag.svg ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/chevron-down.svg":
/*!***************************************!*\
  !*** ./style/images/chevron-down.svg ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/chevron-up.svg":
/*!*************************************!*\
  !*** ./style/images/chevron-up.svg ***!
  \*************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/diagram-3-fill.svg":
/*!*****************************************!*\
  !*** ./style/images/diagram-3-fill.svg ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/files.svg":
/*!********************************!*\
  !*** ./style/images/files.svg ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/horizontal-rule.svg":
/*!******************************************!*\
  !*** ./style/images/horizontal-rule.svg ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIGhlaWdodD0iMjQiIHdpZHRoPSIyNCI+PHBhdGggZmlsbD0ibm9uZSIgZD0iTTAgMGgyNHYyNEgweiIvPjxwYXRoIGZpbGwtcnVsZT0iZXZlbm9kZCIgZD0iTTQgMTFoMTZ2Mkg0eiIvPjwvc3ZnPg==");

/***/ }),

/***/ "./style/images/lock-fill.svg":
/*!************************************!*\
  !*** ./style/images/lock-fill.svg ***!
  \************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/pause-circle-active.svg":
/*!**********************************************!*\
  !*** ./style/images/pause-circle-active.svg ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/pause-circle-idle.svg":
/*!********************************************!*\
  !*** ./style/images/pause-circle-idle.svg ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/play-circle-active.svg":
/*!*********************************************!*\
  !*** ./style/images/play-circle-active.svg ***!
  \*********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/play-circle-idle.svg":
/*!*******************************************!*\
  !*** ./style/images/play-circle-idle.svg ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/router.svg":
/*!*********************************!*\
  !*** ./style/images/router.svg ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/team.svg":
/*!*******************************!*\
  !*** ./style/images/team.svg ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/three-dots-vertical.svg":
/*!**********************************************!*\
  !*** ./style/images/three-dots-vertical.svg ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/up-and-down-arrows.svg":
/*!*********************************************!*\
  !*** ./style/images/up-and-down-arrows.svg ***!
  \*********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/svg+xml;base64,");

/***/ }),

/***/ "./style/images/application-x-executable.png":
/*!***************************************************!*\
  !*** ./style/images/application-x-executable.png ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/audio-x-generic.png":
/*!******************************************!*\
  !*** ./style/images/audio-x-generic.png ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/blue-turtle.png":
/*!**************************************!*\
  !*** ./style/images/blue-turtle.png ***!
  \**************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/folder.png":
/*!*********************************!*\
  !*** ./style/images/folder.png ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/font-x-generic.png":
/*!*****************************************!*\
  !*** ./style/images/font-x-generic.png ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/image-x-generic.png":
/*!******************************************!*\
  !*** ./style/images/image-x-generic.png ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/logo.png":
/*!*******************************!*\
  !*** ./style/images/logo.png ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/package-x-generic.png":
/*!********************************************!*\
  !*** ./style/images/package-x-generic.png ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/text-x-generic.png":
/*!*****************************************!*\
  !*** ./style/images/text-x-generic.png ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/toolbar-close.png":
/*!****************************************!*\
  !*** ./style/images/toolbar-close.png ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/toolbar-folder.png":
/*!*****************************************!*\
  !*** ./style/images/toolbar-folder.png ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/toolbar-info.png":
/*!***************************************!*\
  !*** ./style/images/toolbar-info.png ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/toolbar-pause.png":
/*!****************************************!*\
  !*** ./style/images/toolbar-pause.png ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/toolbar-start.png":
/*!****************************************!*\
  !*** ./style/images/toolbar-start.png ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./style/images/turtle.png":
/*!*********************************!*\
  !*** ./style/images/turtle.png ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABAAAAAICAQAAABaf7ccAAAAfklEQVR4Xm3LMQ4BcRhE8R8aG6VGJdFtsokItVK5p9DqKETPRSRbuIY7bKmQcAmJT0H+WeK9ajIzEj1rlaOVv0w8xMeLDHQNAPrCUyRvCgsh7IBrs26OHRiq0z/pbq80Qju3df79t7JovYO6U1MJS1NjhbmbkGviZKbJRgnwArtVQeM0F/R2AAAAAElFTkSuQmCC");

/***/ }),

/***/ "./style/images/video-x-generic.png":
/*!******************************************!*\
  !*** ./style/images/video-x-generic.png ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = ("data:image/png;base64,");

/***/ }),

/***/ "./src/about-dialog.js":
/*!*****************************!*\
  !*** ./src/about-dialog.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "AboutDialog": () => (/* binding */ AboutDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class AboutDialog extends EventTarget {
  constructor(version_info) {
    super();

    this.elements = AboutDialog._create(version_info);
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    document.body.append(this.elements.root);
    this.elements.dismiss.focus();
  }

  close() {
    this.elements.root.remove();
    this.dispatchEvent(new Event('close'));
    delete this.elements;
  }

  _onDismiss() {
    this.close();
  }

  static _create(version_info) {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('about-dialog');
    elements.root.setAttribute('aria-label', 'About transmission');
    elements.heading.textContent = 'Transmission';
    elements.dismiss.textContent = 'Close';

    let e = document.createElement('div');
    e.classList.add('about-dialog-version-number');
    e.textContent = version_info.version;
    elements.heading.append(e);

    e = document.createElement('div');
    e.classList.add('about-dialog-version-checksum');
    e.textContent = version_info.checksum;
    elements.heading.append(e);

    e = document.createElement('div');
    e.textContent = 'A fast and easy bitTorrent client';
    elements.workarea.append(e);
    e = document.createElement('div');
    e.textContent = 'Copyright © The Transmission Project';
    elements.workarea.append(e);

    elements.confirm.remove();
    delete elements.confirm;

    return elements;
  }
}


/***/ }),

/***/ "./src/action-manager.js":
/*!*******************************!*\
  !*** ./src/action-manager.js ***!
  \*******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ActionManager": () => (/* binding */ ActionManager)
/* harmony export */ });
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */

class ActionManager extends EventTarget {
  constructor() {
    super();
    this.actions = Object.seal({
      'deselect-all': {
        enabled: false,
        shortcut: 'Control+A',
        text: 'Deselect all',
      },
      'move-bottom': { enabled: false, text: 'Move to the back of the queue' },
      'move-down': { enabled: false, text: 'Move down in the queue' },
      'move-top': { enabled: false, text: 'Move to the front of the queue' },
      'move-up': { enabled: false, text: 'Move up in the queue' },
      'open-torrent': {
        enabled: true,
        shortcut: 'Alt+O',
        text: 'Open torrent…',
      },
      'pause-all-torrents': { enabled: false, text: 'Pause all' },
      'pause-selected-torrents': {
        enabled: false,
        shortcut: 'Alt+U',
        text: 'Pause',
      },
      'reannounce-selected-torrents': {
        enabled: false,
        text: 'Ask tracker for more peers',
      },
      'remove-selected-torrents': { enabled: false, text: 'Remove from list…' },
      'resume-selected-torrents': {
        enabled: false,
        shortcut: 'Alt+R',
        text: 'Resume',
      },
      'resume-selected-torrents-now': { enabled: false, text: 'Resume now' },
      'select-all': { enabled: false, shortcut: 'Alt+A', text: 'Select all' },
      'show-about-dialog': { enabled: true, text: 'About' },
      'show-inspector': {
        enabled: true,
        shortcut: 'Alt+I',
        text: 'Torrent Inspector',
      },
      'show-move-dialog': {
        enabled: false,
        shortcut: 'Alt+L',
        text: 'Set location…',
      },
      'show-overflow-menu': { enabled: true, text: 'More options…' },
      'show-preferences-dialog': {
        enabled: true,
        shortcut: 'Alt+P',
        text: 'Edit preferences',
      },
      'show-rename-dialog': {
        enabled: false,
        shortcut: 'Alt+N',
        text: 'Rename…',
      },
      'show-shortcuts-dialog': { enabled: true, text: 'Keyboard shortcuts' },
      'show-statistics-dialog': {
        enabled: true,
        shortcut: 'Alt+S',
        text: 'Statistics',
      },
      'start-all-torrents': { enabled: false, text: 'Start all' },
      'toggle-compact-rows': { enabled: true, text: 'Compact rows' },
      'trash-selected-torrents': {
        enabled: false,
        text: 'Trash data and remove from list…',
      },
      'verify-selected-torrents': {
        enabled: false,
        shortcut: 'Alt+V',
        text: 'Verify local data',
      },
    });
  }

  click(name) {
    if (this.isEnabled(name)) {
      const event_ = new Event('click');
      event_.action = name;
      this.dispatchEvent(event_);
    }
  }

  getActionForShortcut(shortcut) {
    for (const [name, properties] of Object.entries(this.actions)) {
      if (shortcut === properties.shortcut) {
        return name;
      }
    }
    return null;
  }

  // return a map of shortcuts to action names
  allShortcuts() {
    return new Map(
      Object.entries(this.actions)
        .filter(([, properties]) => properties.shortcut)
        .map(([name, properties]) => [properties.shortcut, name])
    );
  }

  isEnabled(name) {
    return this._getAction(name).enabled;
  }

  text(name) {
    return this._getAction(name).text;
  }

  keyshortcuts(name) {
    return this._getAction(name).shortcut;
  }

  update(event_) {
    const counts = ActionManager._recount(event_.selected, event_.nonselected);
    this._updateStates(counts);
  }

  _getAction(name) {
    const action = this.actions[name];
    if (!action) {
      throw new Error(`no such action: ${name}`);
    }
    return action;
  }

  static _recount(selected, nonselected) {
    const total = selected.length + nonselected.length;
    const selected_paused = selected.filter((tor) => tor.isStopped()).length;
    const selected_active = selected.length - selected_paused;
    const nonselected_paused = nonselected.filter((tor) =>
      tor.isStopped()
    ).length;
    const nonselected_active = nonselected.length - nonselected_paused;
    const paused = selected_paused + nonselected_paused;
    const active = selected_active + nonselected_active;
    const selected_queued = selected.filter((tor) => tor.isQueued()).length;

    return {
      active,
      nonselected_active,
      nonselected_paused,
      paused,
      selected: selected.length,
      selected_active,
      selected_paused,
      selected_queued,
      total,
    };
  }

  _updateStates(counts) {
    const set_enabled = (enabled, actions) => {
      for (const action of actions) {
        this._updateActionState(action, enabled);
      }
    };

    set_enabled(counts.selected_paused > 0, ['resume-selected-torrents']);

    set_enabled(counts.paused > 0, ['start-all-torrents']);

    set_enabled(counts.active > 0, ['pause-all-torrents']);

    set_enabled(counts.selected_paused > 0 || counts.selected_queued > 0, [
      'resume-selected-torrents-now',
    ]);

    set_enabled(counts.selected_active > 0, [
      'pause-selected-torrents',
      'reannounce-selected-torrents',
    ]);

    set_enabled(counts.selected > 0, [
      'deselect-all',
      'move-bottom',
      'move-down',
      'move-top',
      'move-up',
      'show-inspector',
      'show-move-dialog',
      'remove-selected-torrents',
      'trash-selected-torrents',
      'verify-selected-torrents',
    ]);

    set_enabled(counts.selected === 1, ['show-rename-dialog']);

    set_enabled(counts.selected < counts.total, ['select-all']);
  }

  _updateActionState(name, enabled) {
    const action = this.actions[name];
    if (!action) {
      throw new Error(`no such action: ${name}`);
    }

    if (action.enabled !== enabled) {
      action.enabled = enabled;

      const event = new Event('change');
      event.action = name;
      event.enabled = enabled;
      this.dispatchEvent(event);
    }
  }
}


/***/ }),

/***/ "./src/alert-dialog.js":
/*!*****************************!*\
  !*** ./src/alert-dialog.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "AlertDialog": () => (/* binding */ AlertDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class AlertDialog extends EventTarget {
  constructor(options) {
    super();

    // options: heading, message
    this.elements = AlertDialog._create(options);
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    this.options = options;
    document.body.append(this.elements.root);
    this.elements.dismiss.focus();
  }

  close() {
    if (!this.closed) {
      this.elements.root.remove();
      this.dispatchEvent(new Event('close'));
      for (const key of Object.keys(this)) {
        delete this[key];
      }
      this.closed = true;
    }
  }

  _onDismiss() {
    this.close();
  }

  static _create(options) {
    const { heading, message } = options;
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('confirm-dialog');
    elements.confirm.remove();
    delete elements.confirm;
    elements.heading.textContent = heading;
    elements.workarea.textContent = message;
    return elements;
  }
}


/***/ }),

/***/ "./src/context-menu.js":
/*!*****************************!*\
  !*** ./src/context-menu.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ContextMenu": () => (/* binding */ ContextMenu)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class ContextMenu extends EventTarget {
  constructor(action_manager) {
    super();

    this.action_listener = this._update.bind(this);
    this.action_manager = action_manager;
    this.action_manager.addEventListener('change', this.action_listener);

    Object.assign(this, this._create());
    this.show();
  }

  show() {
    for (const [action, item] of Object.entries(this.actions)) {
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.setEnabled)(item, this.action_manager.isEnabled(action));
    }
    document.body.append(this.root);
  }

  close() {
    if (!this.closed) {
      this.action_manager.removeEventListener('change', this.action_listener);
      this.root.remove();
      this.dispatchEvent(new Event('close'));
      for (const key of Object.keys(this)) {
        delete this[key];
      }
      this.closed = true;
    }
  }

  _update(event_) {
    const e = this.actions[event_.action];
    if (e) {
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.setEnabled)(e, event_.enabled);
    }
  }

  _create() {
    const root = document.createElement('div');
    root.role = 'menu';
    root.classList.add('context-menu', 'popup');

    const actions = {};
    const add_item = (action) => {
      const item = document.createElement('div');
      const text = this.action_manager.text(action);
      item.role = 'menuitem';
      item.classList.add('context-menuitem');
      item.dataset.action = action;
      item.textContent = text;
      const keyshortcuts = this.action_manager.keyshortcuts(action);
      if (keyshortcuts) {
        item.setAttribute('aria-keyshortcuts', keyshortcuts);
      }
      item.addEventListener('click', () => {
        this.action_manager.click(action);
        this.close();
      });
      actions[action] = item;
      root.append(item);
    };

    const add_separator = () => {
      const item = document.createElement('div');
      item.classList.add('context-menu-separator');
      root.append(item);
    };

    add_item('resume-selected-torrents');
    add_item('resume-selected-torrents-now');
    add_item('pause-selected-torrents');
    add_separator();
    add_item('move-top');
    add_item('move-up');
    add_item('move-down');
    add_item('move-bottom');
    add_separator();
    add_item('remove-selected-torrents');
    add_item('trash-selected-torrents');
    add_separator();
    add_item('verify-selected-torrents');
    add_item('show-move-dialog');
    add_item('show-rename-dialog');
    add_separator();
    add_item('reannounce-selected-torrents');
    add_separator();
    add_item('select-all');
    add_item('deselect-all');

    return { actions, root };
  }
}


/***/ }),

/***/ "./src/file-row.js":
/*!*************************!*\
  !*** ./src/file-row.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "FileRow": () => (/* binding */ FileRow)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */




class FileRow extends EventTarget {
  isDone() {
    return this.fields.have >= this.fields.size;
  }

  isEditable() {
    return this.fields.torrent.getFileCount() > 1 && !this.isDone();
  }

  refreshWantedHTML() {
    const e = this.elements.root;
    e.classList.toggle('skip', !this.fields.isWanted);
    e.classList.toggle('complete', this.isDone());
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setEnabled)(e.checkbox, this.isEditable());
    e.checkbox.checked = this.fields.isWanted;
  }

  refreshProgressHTML() {
    const { size, have } = this.fields;
    const pct = 100 * (size ? have / size : 1);
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter;
    const c = `${fmt.size(have)} of ${fmt.size(size)} (${fmt.percentString(
      pct
    )}%)`;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.progress, c);
  }

  refresh() {
    let have = 0;
    let high = false;
    let low = false;
    let normal = false;
    let size = 0;
    let wanted = false;

    // loop through the file_indices that affect this row
    const files = this.fields.torrent.getFiles();
    for (const index of this.fields.indices) {
      const file = files[index];
      have += file.bytesCompleted;
      size += file.length;
      wanted |= file.wanted;
      switch (file.priority) {
        case -1:
          low = true;
          break;
        case 1:
          high = true;
          break;
        default:
          normal = true;
          break;
      }
    }

    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setChecked)(this.elements.priority_low_button, low);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setChecked)(this.elements.priority_normal_button, normal);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setChecked)(this.elements.priority_high_button, high);

    if (this.fields.have !== have || this.fields.size !== size) {
      this.fields.have = have;
      this.fields.size = size;
      this.refreshProgressHTML();
    }

    if (this.fields.isWanted !== wanted) {
      this.fields.isWanted = wanted;
      this.refreshWantedHTML();
    }
  }

  fireWantedChanged(wanted) {
    const e = new Event('wantedToggled');
    e.indices = [...this.fields.indices];
    e.wanted = wanted;
    this.dispatchEvent(e);
  }

  firePriorityChanged(priority) {
    const e = new Event('priorityToggled');
    e.indices = [...this.fields.indices];
    e.priority = priority;
    this.dispatchEvent(e);
  }

  createRow(torrent, depth, name, even) {
    const root = document.createElement('li');
    root.classList.add(
      'inspector-torrent-file-list-entry',
      even ? 'even' : 'odd'
    );

    this.elements.root = root;

    let e = document.createElement('input');
    const check_id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    e.type = 'checkbox';
    e.className = 'file-wanted-control';
    e.title = 'Download file';
    e.id = check_id;
    e.addEventListener('change', (event_) =>
      this.fireWantedChanged(event_.target.checked)
    );
    root.checkbox = e;
    root.append(e);

    e = document.createElement('label');
    e.className = 'inspector-torrent-file-list-entry-name';
    e.setAttribute('for', check_id);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(e, name);
    root.append(e);

    e = document.createElement('div');
    e.className = 'inspector-torrent-file-list-entry-progress';
    root.append(e);
    this.elements.progress = e;

    e = document.createElement('div');
    e.className = 'file-priority-radiobox';
    const box = e;

    const priority_click_listener = (event_) =>
      this.firePriorityChanged(event_.target.value);

    e = document.createElement('input');
    e.type = 'radio';
    e.value = -1;
    e.className = 'low';
    e.title = 'Low Priority';
    e.addEventListener('click', priority_click_listener);
    this.elements.priority_low_button = e;
    box.append(e);

    e = document.createElement('input');
    e.type = 'radio';
    e.value = 0;
    e.className = 'normal';
    e.title = 'Normal Priority';
    e.addEventListener('click', priority_click_listener);
    this.elements.priority_normal_button = e;
    box.append(e);

    e = document.createElement('input');
    e.type = 'radio';
    e.value = 1;
    e.title = 'High Priority';
    e.className = 'high';
    e.addEventListener('click', priority_click_listener);
    this.elements.priority_high_button = e;
    box.append(e);

    root.append(box);

    root.style.paddingLeft = `${depth * 20}px`;

    this.refresh();
  }

  /// PUBLIC

  getElement() {
    return this.elements.root;
  }

  constructor(torrent, depth, name, indices, even) {
    super();

    this.fields = {
      have: 0,
      indices,
      isWanted: true,
      // priorityHigh: false,
      // priorityLow: false,
      // priorityNormal: false,
      size: 0,
      torrent,
    };
    this.elements = {
      priority_high_button: null,
      priority_low_button: null,
      priority_normal_button: null,
      progress: null,
      root: null,
    };
    this.createRow(torrent, depth, name, even);
  }
}


/***/ }),

/***/ "./src/formatter.js":
/*!**************************!*\
  !*** ./src/formatter.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Formatter": () => (/* binding */ Formatter)
/* harmony export */ });
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */

const plural_rules = new Intl.PluralRules();
const current_locale = plural_rules.resolvedOptions().locale;
const number_format = new Intl.NumberFormat(current_locale);

const kilo = 1000;
const mem_formatters = [
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'byte' }),
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'kilobyte' }),
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'megabyte' }),
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'gigabyte' }),
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'terabyte' }),
  new Intl.NumberFormat(current_locale, { style: 'unit', unit: 'petabyte' }),
];

const fmt_kBps = new Intl.NumberFormat(current_locale, {
  style: 'unit',
  unit: 'kilobyte-per-second',
});
const fmt_MBps = new Intl.NumberFormat(current_locale, {
  style: 'unit',
  unit: 'megabyte-per-second',
});

const Formatter = {
  /** Round a string of a number to a specified number of decimal places */
  _toTruncFixed(number, places) {
    const returnValue = Math.floor(number * 10 ** places) / 10 ** places;
    return returnValue.toFixed(places);
  },

  countString(msgid, msgid_plural, n) {
    return `${this.number(n)} ${this.ngettext(msgid, msgid_plural, n)}`;
  },

  // Formats the a memory size into a human-readable string
  // @param {Number} bytes the filesize in bytes
  // @return {String} human-readable string
  mem(bytes) {
    if (bytes < 0) {
      return 'Unknown';
    }
    if (bytes === 0) {
      return 'None';
    }

    let size = bytes;
    for (const nf of mem_formatters) {
      if (size < kilo) {
        return nf.format(Math.floor(size));
      }
      size /= kilo;
    }

    return 'E2BIG';
  },

  ngettext(msgid, msgid_plural, n) {
    return plural_rules.select(n) === 'one' ? msgid : msgid_plural;
  },

  number(number) {
    return number_format.format(number);
  },

  // format a percentage to a string
  percentString(x) {
    const decimal_places = x < 100 ? 1 : 0;
    return this._toTruncFixed(x, decimal_places);
  },

  /*
   *   Format a ratio to a string
   */
  ratioString(x) {
    if (x === -1) {
      return 'None';
    }
    if (x === -2) {
      return '&infin;';
    }
    return this.percentString(x);
  },

  /**
   * Formats the a disk capacity or file size into a human-readable string
   * @param {Number} bytes the filesize in bytes
   * @return {String} human-readable string
   */
  size(bytes) {
    return this.mem(bytes);
  },

  speed(KBps) {
    return KBps < 999.95 ? fmt_kBps.format(KBps) : fmt_MBps.format(KBps / 1000);
  },

  speedBps(Bps) {
    return this.speed(this.toKBps(Bps));
  },

  timeInterval(seconds) {
    const days = Math.floor(seconds / 86_400);
    if (days) {
      return this.countString('day', 'days', days);
    }

    const hours = Math.floor((seconds % 86_400) / 3600);
    if (hours) {
      return this.countString('hour', 'hours', hours);
    }

    const minutes = Math.floor((seconds % 3600) / 60);
    if (minutes) {
      return this.countString('minute', 'minutes', minutes);
    }

    seconds = Math.floor(seconds % 60);
    return this.countString('second', 'seconds', seconds);
  },

  timestamp(seconds) {
    if (!seconds) {
      return 'N/A';
    }

    const myDate = new Date(seconds * 1000);
    const now = new Date();

    let date = '';
    let time = '';

    const sameYear = now.getFullYear() === myDate.getFullYear();
    const sameMonth = now.getMonth() === myDate.getMonth();

    const dateDiff = now.getDate() - myDate.getDate();
    if (sameYear && sameMonth && Math.abs(dateDiff) <= 1) {
      if (dateDiff === 0) {
        date = 'Today';
      } else if (dateDiff === 1) {
        date = 'Yesterday';
      } else {
        date = 'Tomorrow';
      }
    } else {
      date = myDate.toDateString();
    }

    let hours = myDate.getHours();
    let period = 'AM';
    if (hours > 12) {
      hours = hours - 12;
      period = 'PM';
    }
    if (hours === 0) {
      hours = 12;
    }
    if (hours < 10) {
      hours = `0${hours}`;
    }
    let minutes = myDate.getMinutes();
    if (minutes < 10) {
      minutes = `0${minutes}`;
    }
    seconds = myDate.getSeconds();
    if (seconds < 10) {
      seconds = `0${seconds}`;
    }

    time = [hours, minutes, seconds].join(':');

    return [date, time, period].join(' ');
  },

  toKBps(Bps) {
    return Math.floor(Bps / kilo);
  },
};


/***/ }),

/***/ "./src/inspector.js":
/*!**************************!*\
  !*** ./src/inspector.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Inspector": () => (/* binding */ Inspector)
/* harmony export */ });
/* harmony import */ var _file_row_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./file-row.js */ "./src/file-row.js");
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _torrent_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./torrent.js */ "./src/torrent.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */






const peer_column_classes = [
  'encryption',
  'speed-up',
  'speed-down',
  'percent-done',
  'status',
  'peer-address',
  'peer-app-name',
];

class Inspector extends EventTarget {
  constructor(controller) {
    super();

    this.closed = false;
    this.controller = controller;
    this.elements = this._create();
    this.current_page = this.elements.info.root;
    this.interval = setInterval(this._refreshTorrents.bind(this), 3000);
    this.name = 'inspector';
    this.selection_listener = (event_) => this._setTorrents(event_.selected);
    this.torrent_listener = () => this._updateCurrentPage();
    this.torrents = [];
    this.file_torrent = null;
    this.file_torrent_n = null;
    this.file_rows = null;
    this.outside = new _utils_js__WEBPACK_IMPORTED_MODULE_3__.OutsideClickListener(this.elements.root);
    this.outside.addEventListener('click', () => this.close());
    Object.seal(this);

    controller.addEventListener(
      'torrent-selection-changed',
      this.selection_listener
    );
    this._setTorrents(this.controller.getSelectedTorrents());

    document.body.append(this.elements.root);
  }

  close() {
    if (!this.closed) {
      this.outside.stop();
      clearInterval(this.interval);
      this._setTorrents([]);
      this.elements.root.remove();
      this.controller.removeEventListener(
        'torrent-selection-changed',
        this.selection_listener
      );
      this.dispatchEvent(new Event('close'));
      for (const property of Object.keys(this)) {
        this[property] = null;
      }
      this.closed = true;
    }
  }

  static _createInfoPage() {
    const root = document.createElement('div');
    root.classList.add('inspector-info-page');
    const elements = { root };

    const append_section_title = (text) => {
      const label = document.createElement('div');
      label.textContent = text;
      label.classList.add('section-label');
      root.append(label);
    };

    const append_row = (text) => {
      const lhs = document.createElement('label');
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(lhs, text);
      root.append(lhs);

      const rhs = document.createElement('label');
      root.append(rhs);
      return rhs;
    };

    append_section_title('Activity');
    let rows = [
      ['have', 'Have:'],
      ['availability', 'Availability:'],
      ['uploaded', 'Uploaded:'],
      ['downloaded', 'Downloaded:'],
      ['state', 'State:'],
      ['running_time', 'Running time:'],
      ['remaining_time', 'Remaining:'],
      ['last_activity', 'Last activity:'],
      ['error', 'Error:'],
    ];
    for (const [name, text] of rows) {
      elements[name] = append_row(text);
    }

    append_section_title('Details');
    rows = [
      ['size', 'Size:'],
      ['location', 'Location:'],
      ['hash', 'Hash:'],
      ['privacy', 'Privacy:'],
      ['origin', 'Origin:'],
      ['comment', 'Comment:'],
    ];
    for (const [name, text] of rows) {
      elements[name] = append_row(text);
    }

    return elements;
  }

  static _createListPage(list_type, list_id) {
    const root = document.createElement('div');
    const list = document.createElement(list_type);
    list.id = list_id;
    root.append(list);
    return { list, root };
  }

  static _createTiersPage() {
    return Inspector._createListPage('div', 'inspector-tiers-list');
  }

  static _createFilesPage() {
    return Inspector._createListPage('ul', 'inspector-file-list');
  }

  static _createPeersPage() {
    const table = document.createElement('table');
    table.classList.add('peer-list');
    const thead = document.createElement('thead');
    const tr = document.createElement('tr');
    const names = ['', 'Up', 'Down', 'Done', 'Status', 'Address', 'Client'];
    for (const [index, name] of names.entries()) {
      const th = document.createElement('th');
      const classname = peer_column_classes[index];
      if (classname === 'encryption') {
        th.dataset.encrypted = true;
      }
      th.classList.add(classname);
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(th, name);
      tr.append(th);
    }
    const tbody = document.createElement('tbody');
    thead.append(tr);
    table.append(thead);
    table.append(tbody);
    return {
      root: table,
      tbody,
    };
  }

  _create() {
    const pages = {
      files: Inspector._createFilesPage(),
      info: Inspector._createInfoPage(),
      peers: Inspector._createPeersPage(),
      tiers: Inspector._createTiersPage(),
    };

    const on_activated = (page) => {
      this.current_page = page;
      this._updateCurrentPage();
    };

    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.createTabsContainer)(
      'inspector',
      [
        ['inspector-tab-info', pages.info.root],
        ['inspector-tab-peers', pages.peers.root],
        ['inspector-tab-tiers', pages.tiers.root],
        ['inspector-tab-files', pages.files.root],
      ],
      on_activated.bind(this)
    );

    return { ...elements, ...pages };
  }

  _setTorrents(torrents) {
    // update the inspector when a selected torrent's data changes.
    const key = 'dataChanged';
    const callback = this.torrent_listener;
    for (const t of this.torrents) {
      t.removeEventListener(key, callback);
    }
    this.torrents = [...torrents];
    for (const t of this.torrents) {
      t.addEventListener(key, callback);
    }

    this._refreshTorrents();
    this._updateCurrentPage();
  }

  static _needsExtraInfo(torrents) {
    return torrents.some((tor) => !tor.hasExtraInfo());
  }

  _refreshTorrents() {
    const { controller, torrents } = this;
    const ids = torrents.map((t) => t.getId());

    if (ids && ids.length > 0) {
      const fields = ['id', ..._torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent.Fields.StatsExtra];
      if (Inspector._needsExtraInfo(torrents)) {
        fields.push(..._torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent.Fields.InfoExtra);
      }

      controller.updateTorrents(ids, fields);
    }
  }

  _updateCurrentPage() {
    const { current_page, elements } = this;
    switch (current_page) {
      case elements.files.root:
        this._updateFiles();
        break;
      case elements.info.root:
        this._updateInfo();
        break;
      case elements.peers.root:
        this._updatePeers();
        break;
      case elements.tiers.root:
        this._updateTiers();
        break;
      default:
        console.warn('unexpected page');
        console.log(current_page);
    }
  }

  _updateInfo() {
    const none = 'None';
    const mixed = 'Mixed';
    const unknown = 'Unknown';
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter;
    const now = Date.now();
    const { elements: e, torrents } = this;
    const sizeWhenDone = torrents.reduce(
      (accumulator, t) => accumulator + t.getSizeWhenDone(),
      0
    );

    // state
    let string = null;
    if (torrents.length === 0) {
      string = none;
    } else if (torrents.every((t) => t.isFinished())) {
      string = 'Finished';
    } else if (torrents.every((t) => t.isStopped())) {
      string = 'Paused';
    } else {
      const get = (t) => t.getStateString();
      const first = get(torrents[0]);
      string = torrents.every((t) => get(t) === first) ? first : mixed;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.state, string);
    const stateString = string;

    // have
    if (torrents.length === 0) {
      string = none;
    } else {
      const verified = torrents.reduce(
        (accumulator, t) => accumulator + t.getHaveValid(),
        0
      );
      const unverified = torrents.reduce(
        (accumulator, t) => accumulator + t.getHaveUnchecked(),
        0
      );
      const leftUntilDone = torrents.reduce(
        (accumulator, t) => accumulator + t.getLeftUntilDone(),
        0
      );

      const d =
        100 *
        (sizeWhenDone ? (sizeWhenDone - leftUntilDone) / sizeWhenDone : 1);
      string = fmt.percentString(d);

      if (!unverified && !leftUntilDone) {
        string = `${fmt.size(verified)} (100%)`;
      } else if (!unverified) {
        string = `${fmt.size(verified)} of ${fmt.size(
          sizeWhenDone
        )} (${string}%)`;
      } else {
        string = `${fmt.size(verified)} of ${fmt.size(
          sizeWhenDone
        )} (${string}%), ${fmt.size(unverified)} Unverified`;
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.have, string);

    // availability
    if (torrents.length === 0) {
      string = none;
    } else if (sizeWhenDone === 0) {
      string = none;
    } else {
      const available = torrents.reduce(
        (accumulator, t) => t.getHave() + t.getDesiredAvailable(),
        0
      );
      string = `${fmt.percentString((100 * available) / sizeWhenDone)}%`;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.availability, string);

    //  downloaded
    if (torrents.length === 0) {
      string = none;
    } else {
      const d = torrents.reduce(
        (accumulator, t) => accumulator + t.getDownloadedEver(),
        0
      );
      const f = torrents.reduce(
        (accumulator, t) => accumulator + t.getFailedEver(),
        0
      );
      string = f ? `${fmt.size(d)} (${fmt.size(f)} corrupt)` : fmt.size(d);
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.downloaded, string);

    // uploaded
    if (torrents.length === 0) {
      string = none;
    } else {
      const u = torrents.reduce(
        (accumulator, t) => accumulator + t.getUploadedEver(),
        0
      );
      const d =
        torrents.reduce(
          (accumulator, t) => accumulator + t.getDownloadedEver(),
          0
        ) ||
        torrents.reduce((accumulator, t) => accumulator + t.getHaveValid(), 0);
      string = `${fmt.size(u)} (Ratio: ${fmt.ratioString(_utils_js__WEBPACK_IMPORTED_MODULE_3__.Utils.ratio(u, d))})`;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.uploaded, string);

    // running time
    if (torrents.length === 0) {
      string = none;
    } else if (torrents.every((t) => t.isStopped())) {
      string = stateString; // paused || finished}
    } else {
      const get = (t) => t.getStartDate();
      const first = get(torrents[0]);
      string = !torrents.every((t) => get(t) === first)
        ? mixed
        : fmt.timeInterval(now / 1000 - first);
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.running_time, string);

    // remaining time
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getETA();
      const first = get(torrents[0]);
      if (!torrents.every((t) => get(t) === first)) {
        string = mixed;
      } else if (first < 0) {
        string = unknown;
      } else {
        string = fmt.timeInterval(first);
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.remaining_time, string);

    // last active at
    if (torrents.length === 0) {
      string = none;
    } else {
      const latest = torrents.reduce(
        (accumulator, t) => Math.max(accumulator, t.getLastActivity()),
        -1
      );
      const now_seconds = Math.floor(now / 1000);
      if (0 < latest && latest <= now_seconds) {
        const idle_secs = now_seconds - latest;
        string =
          idle_secs < 5 ? 'Active now' : `${fmt.timeInterval(idle_secs)} ago`;
      } else {
        string = none;
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.last_activity, string);

    // error
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getErrorString();
      const first = get(torrents[0]);
      string = torrents.every((t) => get(t) === first) ? first : mixed;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.error, string || none);

    // size
    if (torrents.length === 0) {
      string = none;
    } else {
      const size = torrents.reduce(
        (accumulator, t) => accumulator + t.getTotalSize(),
        0
      );
      if (!size) {
        string = 'None';
      } else {
        const get = (t) => t.getPieceSize();
        const pieceCount = torrents.reduce(
          (accumulator, t) => accumulator + t.getPieceCount(),
          0
        );
        const pieceString = fmt.number(pieceCount);
        const pieceSize = get(torrents[0]);
        string = torrents.every((t) => get(t) === pieceSize)
          ? `${fmt.size(size)} (${pieceString} pieces @ ${fmt.mem(pieceSize)})`
          : `${fmt.size(size)} (${pieceString} pieces)`;
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.size, string);

    // hash
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getHashString();
      const first = get(torrents[0]);
      string = torrents.every((t) => get(t) === first) ? first : mixed;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.hash, string);

    // privacy
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getPrivateFlag();
      const first = get(torrents[0]);
      if (!torrents.every((t) => get(t) === first)) {
        string = mixed;
      } else if (first) {
        string = 'Private to this tracker -- DHT and PEX disabled';
      } else {
        string = 'Public torrent';
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.privacy, string);

    // comment
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getComment();
      const first = get(torrents[0]);
      string = torrents.every((t) => get(t) === first) ? first : mixed;
    }
    string = string || none;
    if (string.startsWith('https://') || string.startsWith('http://')) {
      string = encodeURI(string);
      _utils_js__WEBPACK_IMPORTED_MODULE_3__.Utils.setInnerHTML(
        e.info.comment,
        `<a href="${string}" target="_blank" >${string}</a>`
      );
    } else {
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.comment, string);
    }

    // origin
    if (torrents.length === 0) {
      string = none;
    } else {
      let get = (t) => t.getCreator();
      const creator = get(torrents[0]);
      const mixed_creator = !torrents.every((t) => get(t) === creator);

      get = (t) => t.getDateCreated();
      const date = get(torrents[0]);
      const mixed_date = !torrents.every((t) => get(t) === date);

      const empty_creator = !creator || creator.length === 0;
      const empty_date = !date;
      if (mixed_creator || mixed_date) {
        string = mixed;
      } else if (empty_creator && empty_date) {
        string = unknown;
      } else if (empty_date && !empty_creator) {
        string = `Created by ${creator}`;
      } else if (empty_creator && !empty_date) {
        string = `Created on ${new Date(date * 1000).toDateString()}`;
      } else {
        string = `Created by ${creator} on ${new Date(
          date * 1000
        ).toDateString()}`;
      }
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.origin, string);

    // location
    if (torrents.length === 0) {
      string = none;
    } else {
      const get = (t) => t.getDownloadDir();
      const first = get(torrents[0]);
      string = torrents.every((t) => get(t) === first) ? first : mixed;
    }
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(e.info.location, string);
  }

  ///  PEERS PAGE

  static _peerStatusTitle(flag_string) {
    const texts = Object.seal({
      '?': "We unchoked this peer, but they're not interested",
      D: 'Downloading from this peer',
      E: 'Encrypted Connection',
      H: 'Peer was discovered through Distributed Hash Table (DHT)',
      I: 'Peer is an incoming connection',
      K: "Peer has unchoked us, but we're not interested",
      O: 'Optimistic unchoke',
      T: 'Peer is connected via uTP',
      U: 'Uploading to peer',
      X: 'Peer was discovered through Peer Exchange (PEX)',
      d: "We would download from this peer if they'd let us",
      u: "We would upload to this peer if they'd ask",
    });

    return [...flag_string]
      .filter((ch) => texts[ch])
      .map((ch) => `${ch}: ${texts[ch]}`)
      .join('\n');
  }

  _updatePeers() {
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter;
    const { elements, torrents } = this;
    const { tbody } = elements.peers;

    const cell_setters = [
      (peer, td) => {
        td.dataset.encrypted = peer.isEncrypted;
      },
      (peer, td) =>
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          td,
          peer.rateToPeer ? fmt.speedBps(peer.rateToPeer) : ''
        ),
      (peer, td) =>
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          td,
          peer.rateToClient ? fmt.speedBps(peer.rateToClient) : ''
        ),
      (peer, td) => (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(td, `${Math.floor(peer.progress * 100)}%`),
      (peer, td) => {
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(td, peer.flagStr);
        td.setAttribute('title', Inspector._peerStatusTitle(peer.flagStr));
      },
      (peer, td) => (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(td, peer.address),
      (peer, td) => (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(td, peer.clientName),
    ];

    const rows = [];
    for (const tor of torrents) {
      // torrent name
      const tortr = document.createElement('tr');
      tortr.classList.add('torrent-row');
      const tortd = document.createElement('td');
      tortd.setAttribute('colspan', cell_setters.length);
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(tortd, tor.getName());
      tortr.append(tortd);
      rows.push(tortr);

      // peers
      for (const peer of tor.getPeers()) {
        const tr = document.createElement('tr');
        tr.classList.add('peer-row');
        for (const [index, setter] of cell_setters.entries()) {
          const td = document.createElement('td');
          td.classList.add(peer_column_classes[index]);
          setter(peer, td);
          tr.append(td);
        }
        rows.push(tr);
      }

      // TODO: modify instead of rebuilding wholesale?
      while (tbody.firstChild) {
        tbody.firstChild.remove();
      }
      tbody.append(...rows);
    }
  }

  /// TRACKERS PAGE

  static getAnnounceState(tracker) {
    switch (tracker.announceState) {
      case _torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent._TrackerActive:
        return 'Announce in progress';
      case _torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent._TrackerWaiting: {
        const timeUntilAnnounce = Math.max(
          0,
          tracker.nextAnnounceTime - Date.now() / 1000
        );
        return `Next announce in ${_formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter.timeInterval(timeUntilAnnounce)}`;
      }
      case _torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent._TrackerQueued:
        return 'Announce is queued';
      case _torrent_js__WEBPACK_IMPORTED_MODULE_2__.Torrent._TrackerInactive:
        return tracker.isBackup
          ? 'Tracker will be used as a backup'
          : 'Announce not scheduled';
      default:
        return `unknown announce state: ${tracker.announceState}`;
    }
  }

  static lastAnnounceStatus(tracker) {
    let lastAnnounceLabel = 'Last Announce';
    let lastAnnounce = ['N/A'];

    if (tracker.hasAnnounced) {
      const lastAnnounceTime = _formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter.timestamp(tracker.lastAnnounceTime);
      if (tracker.lastAnnounceSucceeded) {
        lastAnnounce = [
          lastAnnounceTime,
          ' (got ',
          _formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter.countString('peer', 'peers', tracker.lastAnnouncePeerCount),
          ')',
        ];
      } else {
        lastAnnounceLabel = 'Announce error';
        lastAnnounce = [
          tracker.lastAnnounceResult ? `${tracker.lastAnnounceResult} - ` : '',
          lastAnnounceTime,
        ];
      }
    }
    return {
      label: lastAnnounceLabel,
      value: lastAnnounce.join(''),
    };
  }

  static lastScrapeStatus(tracker) {
    let lastScrapeLabel = 'Last Scrape';
    let lastScrape = 'N/A';

    if (tracker.hasScraped) {
      const lastScrapeTime = _formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter.timestamp(tracker.lastScrapeTime);
      if (tracker.lastScrapeSucceeded) {
        lastScrape = lastScrapeTime;
      } else {
        lastScrapeLabel = 'Scrape error';
        lastScrape =
          (tracker.lastScrapeResult ? `${tracker.lastScrapeResult} - ` : '') +
          lastScrapeTime;
      }
    }
    return {
      label: lastScrapeLabel,
      value: lastScrape,
    };
  }

  _updateTiers() {
    const na = 'N/A';
    const { list } = this.elements.tiers;
    const { torrents } = this;

    const rows = [];
    for (const tor of torrents) {
      const group = document.createElement('div');
      group.classList.add('inspector-group');
      rows.push(group);

      // if >1 torrent to be shown, give a title
      if (torrents.length > 1) {
        const title = document.createElement('div');
        title.classList.add('tier-list-torrent');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(title, tor.getName());
        rows.push(title);
      }

      for (const [index, tracker] of tor.getTrackers().entries()) {
        const announceState = Inspector.getAnnounceState(tracker);
        const lastAnnounceStatusHash = Inspector.lastAnnounceStatus(tracker);
        const lastScrapeStatusHash = Inspector.lastScrapeStatus(tracker);

        const tier_div = document.createElement('div');
        tier_div.classList.add('tier-list-row', index % 2 ? 'odd' : 'even');

        let element = document.createElement('div');
        element.classList.add('tier-list-tracker');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `${tracker.domain || tracker.host || tracker.announce} - tier ${
            tracker.tier + 1
          }`
        );
        element.setAttribute('title', tracker.announce);
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-announce');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `${lastAnnounceStatusHash.label}: ${lastAnnounceStatusHash.value}`
        );
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-seeders');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `Seeders: ${tracker.seederCount > -1 ? tracker.seederCount : na}`
        );
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-state');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(element, announceState);
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-leechers');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `Leechers: ${tracker.leecherCount > -1 ? tracker.leecherCount : na}`
        );
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-scrape');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `${lastScrapeStatusHash.label}: ${lastScrapeStatusHash.value}`
        );
        tier_div.append(element);

        element = document.createElement('div');
        element.classList.add('tier-downloads');
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setTextContent)(
          element,
          `Downloads: ${
            tracker.downloadCount > -1 ? tracker.downloadCount : na
          }`
        );
        tier_div.append(element);

        rows.push(tier_div);
      }
    }

    // TODO: modify instead of rebuilding wholesale?
    while (list.firstChild) {
      list.firstChild.remove();
    }
    list.append(...rows);
  }

  ///  FILES PAGE

  _changeFileCommand(fileIndices, command) {
    const { controller, file_torrent } = this;
    const torrentId = file_torrent.getId();
    controller.changeFileCommand(torrentId, fileIndices, command);
  }

  _onFileWantedToggled(event_) {
    const { indices, wanted } = event_;
    this._changeFileCommand(
      indices,
      wanted ? 'files-wanted' : 'files-unwanted'
    );
  }

  _onFilePriorityToggled(event_) {
    const { indices, priority } = event_;

    let command = null;
    switch (priority) {
      case -1:
        command = 'priority-low';
        break;
      case 1:
        command = 'priority-high';
        break;
      default:
        command = 'priority-normal';
        break;
    }

    this._changeFileCommand(indices, command);
  }

  _clearFileList() {
    const { list } = this.elements.files;
    while (list.firstChild) {
      list.firstChild.remove();
    }

    this.file_torrent = null;
    this.file_torrent_n = null;
    this.file_rows = null;
  }

  static createFileTreeModel(tor) {
    const leaves = [];
    const tree = {
      children: {},
      file_indices: [],
    };

    for (const [index, file] of tor.getFiles().entries()) {
      const { name } = file;
      const tokens = name.split('/');
      let walk = tree;
      for (const [index_, token] of tokens.entries()) {
        let sub = walk.children[token];
        if (!sub) {
          walk.children[token] = sub = {
            children: {},
            depth: index_,
            file_indices: [],
            name: token,
            parent: walk,
          };
        }
        walk = sub;
      }
      walk.file_index = index;
      delete walk.children;
      leaves.push(walk);
    }

    for (const leaf of leaves) {
      const { file_index } = leaf;
      let walk = leaf;
      do {
        walk.file_indices.push(file_index);
        walk = walk.parent;
      } while (walk);
    }

    return tree;
  }

  addNodeToView(tor, parent, sub, index) {
    const row = new _file_row_js__WEBPACK_IMPORTED_MODULE_0__.FileRow(
      tor,
      sub.depth,
      sub.name,
      sub.file_indices,
      index % 2
    );
    row.addEventListener('wantedToggled', this._onFileWantedToggled.bind(this));
    row.addEventListener(
      'priorityToggled',
      this._onFilePriorityToggled.bind(this)
    );
    this.file_rows.push(row);
    parent.append(row.getElement());
  }

  addSubtreeToView(tor, parent, sub, index) {
    if (sub.parent) {
      this.addNodeToView(tor, parent, sub, index++);
    }
    if (sub.children) {
      for (const value of Object.values(sub.children)) {
        index = this.addSubtreeToView(tor, parent, value, index);
      }
    }
    return index;
  }

  _updateFiles() {
    const { list } = this.elements.files;
    const { file_rows, file_torrent, file_torrent_n, torrents } = this;

    // only show one torrent at a time
    if (torrents.length !== 1) {
      this._clearFileList();
      return;
    }

    const [tor] = torrents;
    const n = tor.getFiles().length;
    if (tor !== file_torrent || n !== file_torrent_n) {
      // rebuild the file list...
      this._clearFileList();
      this.file_torrent = tor;
      this.file_torrent_n = n;
      this.file_rows = [];
      const fragment = document.createDocumentFragment();
      const tree = Inspector.createFileTreeModel(tor);
      this.addSubtreeToView(tor, fragment, tree, 0);
      list.append(fragment);
    } else {
      // ...refresh the already-existing file list
      for (const row of file_rows) {
        row.refresh();
      }
    }
  }
}


/***/ }),

/***/ "./src/move-dialog.js":
/*!****************************!*\
  !*** ./src/move-dialog.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "MoveDialog": () => (/* binding */ MoveDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */

let default_path = '';



class MoveDialog extends EventTarget {
  constructor(controller, remote) {
    super();

    this.controller = controller;
    this.remote = remote;
    this.elements = {};
    this.torrents = [];

    this.show();
  }

  show() {
    const torrents = this.controller.getSelectedTorrents();
    if (torrents.length === 0) {
      return;
    }

    default_path = default_path || torrents[0].getDownloadDir();

    this.torrents = torrents;
    this.elements = MoveDialog._create();
    this.elements.confirm.addEventListener('click', () => this._onConfirm());
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    this.elements.entry.value = default_path;
    document.body.append(this.elements.root);

    this.elements.entry.focus();
  }

  close() {
    this.elements.root.remove();

    this.dispatchEvent(new Event('close'));

    delete this.controller;
    delete this.remote;
    delete this.elements;
    delete this.torrents;
  }

  _onDismiss() {
    this.close();
  }

  _onConfirm() {
    const ids = this.torrents.map((tor) => tor.getId());
    const path = this.elements.entry.value.trim();
    default_path = path;
    this.remote.moveTorrents(ids, path);
    this.close();
  }

  static _create() {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('move-dialog');
    elements.root.setAttribute('aria-label', 'Move Torrent');
    elements.heading.textContent = 'Set Torrent Location';
    confirm.textContent = 'Apply';

    const label = document.createElement('label');
    label.setAttribute('for', 'torrent-path');
    label.textContent = 'Location:';
    elements.workarea.append(label);

    const entry = document.createElement('input');
    entry.setAttribute('type', 'text');
    entry.id = 'torrent-path';
    elements.entry = entry;
    elements.workarea.append(entry);

    return elements;
  }
}


/***/ }),

/***/ "./src/notifications.js":
/*!******************************!*\
  !*** ./src/notifications.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Notifications": () => (/* binding */ Notifications)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class Notifications {
  constructor(prefs) {
    this._prefs = prefs;
    this._elements = {
      toggle: document.querySelector('#toggle-notifications'),
    };
  }

  _setEnabled(enabled) {
    this.prefs.notifications_enabled = enabled;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.setTextContent)(
      this._toggle,
      `${enabled ? 'Disable' : 'Enable'} Notifications`
    );
  }

  _requestPermission() {
    Notification.requestPermission().then((s) =>
      this._setEnabled(s === 'granted')
    );
  }

  toggle() {
    if (this._enabled) {
      this._setEnabled(false);
    } else if (Notification.permission === 'granted') {
      this._setEnabled(true);
    } else if (Notification.permission !== 'denied') {
      this._requestPermission();
    }
  }

  /*
  // TODO:
  // $(transmission).bind('downloadComplete seedingComplete', (event, torrent) => {
  //  if (notificationsEnabled) {
      const title = `${event.type === 'downloadComplete' ? 'Download' : 'Seeding'} complete`;
      const content = torrent.getName();
      const notification = window.webkitNotifications.createNotification(
        'style/transmission/images/logo.png',
        title,
        content
      );
      notification.show();
      setTimeout(() => {
        notification.cancel();
      }, 5000);
    }
  });
*/
}


/***/ }),

/***/ "./src/open-dialog.js":
/*!****************************!*\
  !*** ./src/open-dialog.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "OpenDialog": () => (/* binding */ OpenDialog)
/* harmony export */ });
/* harmony import */ var _alert_dialog_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./alert-dialog.js */ "./src/alert-dialog.js");
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */





class OpenDialog extends EventTarget {
  constructor(controller, remote) {
    super();

    this.controller = controller;
    this.remote = remote;

    this.elements = this._create();
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    this.elements.confirm.addEventListener('click', () => this._onConfirm());
    this._updateFreeSpaceInAddDialog();
    document.body.append(this.elements.root);
    this.elements.url_input.focus();
  }

  close() {
    if (!this.closed) {
      clearInterval(this.interval);

      this.elements.root.remove();
      this.dispatchEvent(new Event('close'));

      for (const key of Object.keys(this)) {
        delete this[key];
      }
      this.closed = true;
    }
  }

  _onDismiss() {
    this.close();
  }

  _updateFreeSpaceInAddDialog() {
    const path = this.elements.folder_input.value;
    this.remote.getFreeSpace(path, (dir, bytes) => {
      const string = bytes > 0 ? `${_formatter_js__WEBPACK_IMPORTED_MODULE_1__.Formatter.size(bytes)} Free` : '';
      this.elements.freespace.textContent = string;
    });
  }

  _onConfirm() {
    const { controller, elements, remote } = this;
    const { file_input, folder_input, start_input, url_input } = elements;
    const paused = !start_input.checked;
    const destination = folder_input.value.trim();

    for (const file of file_input.files) {
      const reader = new FileReader();
      reader.addEventListener('load', (e) => {
        const contents = e.target.result;
        const key = 'base64,';
        const index = contents.indexOf(key);
        if (index === -1) {
          return;
        }
        const o = {
          arguments: {
            'download-dir': destination,
            metainfo: contents.slice(Math.max(0, index + key.length)),
            paused,
          },
          method: 'torrent-add',
        };
        console.log(o);
        remote.sendRequest(o, (response) => {
          if (response.result !== 'success') {
            alert(`Error adding "${file.name}": ${response.result}`);
            controller.setCurrentPopup(
              new _alert_dialog_js__WEBPACK_IMPORTED_MODULE_0__.AlertDialog({
                heading: `Error adding "${file.name}"`,
                message: response.result,
              })
            );
          }
        });
      });
      reader.readAsDataURL(file);
    }

    let url = url_input.value.trim();
    if (url.length > 0) {
      if (/^[\da-f]{40}$/i.test(url)) {
        url = `magnet:?xt=urn:btih:${url}`;
      }
      const o = {
        arguments: {
          'download-dir': destination,
          filename: url,
          paused,
        },
        method: 'torrent-add',
      };
      console.log(o);
      remote.sendRequest(o, (payload, response) => {
        if (response.result !== 'success') {
          controller.setCurrentPopup(
            new _alert_dialog_js__WEBPACK_IMPORTED_MODULE_0__.AlertDialog({
              heading: `Error adding "${url}"`,
              message: response.result,
            })
          );
        }
      });
    }

    this._onDismiss();
  }

  _create() {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.createDialogContainer)();
    const { confirm, root, heading, workarea } = elements;

    root.classList.add('open-torrent');
    heading.textContent = 'Add Torrents';
    confirm.textContent = 'Add';

    let input_id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.makeUUID)();
    let label = document.createElement('label');
    label.setAttribute('for', input_id);
    label.textContent = 'Please select torrent files to add:';
    workarea.append(label);

    let input = document.createElement('input');
    input.type = 'file';
    input.name = 'torrent-files[]';
    input.id = input_id;
    input.multiple = 'multiple';
    workarea.append(input);
    elements.file_input = input;

    input_id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.makeUUID)();
    label = document.createElement('label');
    label.setAttribute('for', input_id);
    label.textContent = 'Or enter a URL:';
    workarea.append(label);

    input = document.createElement('input');
    input.type = 'url';
    input.id = input_id;
    workarea.append(input);
    elements.url_input = input;

    input_id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.makeUUID)();
    label = document.createElement('label');
    label.id = 'add-dialog-folder-label';
    label.for = input_id;
    label.textContent = 'Destination folder:';
    workarea.append(label);

    const freespace = document.createElement('span');
    freespace.id = 'free-space-text';
    label.append(freespace);
    workarea.append(label);
    elements.freespace = freespace;

    input = document.createElement('input');
    input.type = 'text';
    input.id = 'add-dialog-folder-input';
    input.addEventListener('change', () => this._updateFreeSpaceInAddDialog());
    input.value = this.controller.session_properties['download-dir'];
    workarea.append(input);
    elements.folder_input = input;

    const checkarea = document.createElement('div');
    workarea.append(checkarea);

    const check = document.createElement('input');
    check.type = 'checkbox';
    check.id = 'auto-start-check';
    check.checked = this.controller.shouldAddedTorrentsStart();
    checkarea.append(check);
    elements.start_input = check;

    label = document.createElement('label');
    label.id = 'auto-start-label';
    label.setAttribute('for', check.id);
    label.textContent = 'Start when added';
    checkarea.append(label);

    return elements;
  }
}


/***/ }),

/***/ "./src/overflow-menu.js":
/*!******************************!*\
  !*** ./src/overflow-menu.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "OverflowMenu": () => (/* binding */ OverflowMenu)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _prefs_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./prefs.js */ "./src/prefs.js");
/* harmony import */ var _remote_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./remote.js */ "./src/remote.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */






function make_section(classname, title) {
  const section = document.createElement('fieldset');
  section.classList.add('section', classname);
  const legend = document.createElement('legend');
  legend.classList.add('title');
  legend.textContent = title;
  section.append(legend);
  return section;
}

function make_button(parent, text, action, on_click) {
  const button = document.createElement('button');
  button.textContent = text;
  button.addEventListener('click', on_click);
  parent.append(button);
  button.dataset.action = action;
  return button;
}

class OverflowMenu extends EventTarget {
  constructor(session_manager, prefs, remote, action_manager) {
    super();

    this.action_listener = this._onActionChange.bind(this);
    this.action_manager = action_manager;
    this.action_manager.addEventListener('change', this.action_listener);

    this.prefs_listener = this._onPrefsChange.bind(this);
    this.prefs = prefs;
    this.prefs.addEventListener('change', this.prefs_listener);

    this.closed = false;
    this.remote = remote;
    this.name = 'overflow-menu';

    this.session_listener = this._onSessionChange.bind(this);
    this.session_manager = session_manager;
    this.session_manager.addEventListener(
      'session-change',
      this.session_listener
    );

    const { session_properties } = session_manager;
    Object.assign(this, this._create(session_properties));

    this.outside = new _utils_js__WEBPACK_IMPORTED_MODULE_3__.OutsideClickListener(this.root);
    this.outside.addEventListener('click', () => this.close());
    Object.seal(this);

    this.show();
  }

  show() {
    document.body.append(this.root);
  }

  close() {
    if (!this.closed) {
      this.outside.stop();
      this.session_manager.removeEventListener(
        'session-change',
        this.session_listener
      );
      this.action_manager.removeEventListener('change', this.action_listener);
      this.prefs.removeEventListener('change', this.prefs_listener);

      this.root.remove();
      this.dispatchEvent(new Event('close'));

      for (const key of Object.keys(this)) {
        this[key] = null;
      }
      this.closed = true;
    }
  }

  _onSessionChange(event_) {
    const { alt_speed_check } = this.elements;
    const { session_properties } = event_;
    alt_speed_check.checked = session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._TurtleState];
  }

  _onPrefsChange(event_) {
    switch (event_.key) {
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortDirection:
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortMode:
        this.root.querySelector(`[data-pref="${event_.key}"]`).value =
          event_.value;
        break;
      default:
        break;
    }
  }

  _onActionChange(event_) {
    const element = this.actions[event_.action];
    if (element) {
      this._updateElement(element);
    }
  }

  _updateElement(element) {
    if (element.dataset.action) {
      const { action } = element.dataset;
      const shortcuts = this.action_manager.keyshortcuts(action);
      if (shortcuts) {
        element.setAttribute('aria-keyshortcuts', shortcuts);
      }
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_3__.setEnabled)(element, this.action_manager.isEnabled(action));
    }
  }

  _onClick(event_) {
    const { action, pref } = event_.target.dataset;

    if (action) {
      this.action_manager.click(action);
      return;
    }

    if (pref) {
      this.prefs[pref] = event_.target.value;
      return;
    }

    console.log('unhandled');
    console.log(event_);
    console.trace();
  }

  _create(session_properties) {
    const actions = {};
    const elements = {};
    const on_click = this._onClick.bind(this);

    const root = document.createElement('div');
    root.classList.add('overflow-menu', 'popup');

    let section = make_section('display', 'Display');
    root.append(section);

    let options = document.createElement('div');
    options.id = 'display-options';
    section.append(options);

    // sort mode

    let div = document.createElement('div');
    options.append(div);

    let label = document.createElement('label');
    label.id = 'display-sort-mode-label';
    label.textContent = 'Sort by';
    div.append(label);

    let select = document.createElement('select');
    select.id = 'display-sort-mode-select';
    select.dataset.pref = _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortMode;
    div.append(select);

    const sort_modes = [
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByActivity, 'Activity'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByAge, 'Age'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByName, 'Name'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByProgress, 'Progress'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByQueue, 'Queue order'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByRatio, 'Ratio'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortBySize, 'Size'],
      [_prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByState, 'State'],
    ];
    for (const [value, text] of sort_modes) {
      const option = document.createElement('option');
      option.value = value;
      option.textContent = text;
      select.append(option);
    }

    label.setAttribute('for', select.id);
    select.value = this.prefs.sort_mode;
    select.addEventListener('change', (event_) => {
      this.prefs.sort_mode = event_.target.value;
    });

    // sort direction

    div = document.createElement('div');
    options.append(div);

    let check = document.createElement('input');
    check.id = 'display-sort-reverse-check';
    check.dataset.pref = _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortDirection;
    check.type = 'checkbox';
    div.append(check);

    label = document.createElement('label');
    label.id = 'display-sort-reverse-label';
    label.setAttribute('for', check.id);
    label.textContent = 'Reverse sort';
    div.append(label);

    check.checked = this.prefs.sort_direction !== _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortAscending;
    check.addEventListener('input', (event_) => {
      this.prefs.sort_direction = event_.target.checked
        ? _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortDescending
        : _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortAscending;
    });

    // compact

    div = document.createElement('div');
    options.append(div);

    const action = 'toggle-compact-rows';
    check = document.createElement('input');
    check.id = 'display-compact-check';
    check.dataset.action = action;
    check.type = 'checkbox';
    div.append(check);

    label = document.createElement('label');
    label.id = 'display-compact-label';
    label.for = check.id;
    label.setAttribute('for', check.id);
    label.textContent = this.action_manager.text(action);
    div.append(label);

    check.checked = this.prefs.display_mode === _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.DisplayCompact;
    check.addEventListener('input', (event_) => {
      const { checked } = event_.target;
      this.prefs.display_mode = checked
        ? _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.DisplayCompact
        : _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.DisplayFull;
    });

    // fullscreen

    div = document.createElement('div');
    options.append(div);

    check = document.createElement('input');
    check.id = 'display-fullscreen-check';
    check.type = 'checkbox';
    const is_fullscreen = () => document.fullscreenElement !== null;
    check.checked = is_fullscreen();
    check.addEventListener('input', () => {
      if (is_fullscreen()) {
        document.exitFullscreen();
      } else {
        document.body.requestFullscreen();
      }
    });
    document.addEventListener('fullscreenchange', () => {
      check.checked = is_fullscreen();
    });
    div.append(check);

    label = document.createElement('label');
    label.id = 'display-fullscreen-label';
    label.for = check.id;
    label.setAttribute('for', check.id);
    label.textContent = 'Fullscreen';
    div.append(label);

    section = make_section('speed', 'Speed Limit');
    root.append(section);

    options = document.createElement('div');
    options.id = 'speed-options';
    section.append(options);

    // speed up

    div = document.createElement('div');
    div.classList.add('speed-up');
    options.append(div);

    label = document.createElement('label');
    label.id = 'speed-up-label';
    label.textContent = 'Upload:';
    div.append(label);

    const unlimited = 'Unlimited';
    select = document.createElement('select');
    select.id = 'speed-up-select';
    div.append(select);

    const speeds = ['10', '100', '200', '500', '750', unlimited];
    for (const speed of [
      ...new Set(speeds)
        .add(`${session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimit]}`)
        .values(),
    ].sort()) {
      const option = document.createElement('option');
      option.value = speed;
      option.textContent =
        speed === unlimited ? unlimited : _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.speed(speed);
      select.append(option);
    }

    label.setAttribute('for', select.id);
    select.value = session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimited]
      ? `${session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimit]}`
      : unlimited;
    select.addEventListener('change', (event_) => {
      const { value } = event_.target;
      console.log(event_);
      if (value === unlimited) {
        this.remote.savePrefs({ [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimited]: false });
      } else {
        this.remote.savePrefs({
          [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimited]: true,
          [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._UpSpeedLimit]: Number.parseInt(value, 10),
        });
      }
    });

    // speed down

    div = document.createElement('div');
    div.classList.add('speed-down');
    options.append(div);

    label = document.createElement('label');
    label.id = 'speed-down-label';
    label.textContent = 'Download:';
    div.append(label);

    select = document.createElement('select');
    select.id = 'speed-down-select';
    div.append(select);

    for (const speed of [
      ...new Set(speeds)
        .add(`${session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimit]}`)
        .values(),
    ].sort()) {
      const option = document.createElement('option');
      option.value = speed;
      option.textContent = speed;
      select.append(option);
    }

    label.setAttribute('for', select.id);
    select.value = session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimited]
      ? `${session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimit]}`
      : unlimited;
    select.addEventListener('change', (event_) => {
      const { value } = event_.target;
      console.log(event_);
      if (value === unlimited) {
        this.remote.savePrefs({ [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimited]: false });
      } else {
        this.remote.savePrefs({
          [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimited]: true,
          [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._DownSpeedLimit]: Number.parseInt(value, 10),
        });
      }
    });

    // alt speed

    div = document.createElement('div');
    div.classList.add('alt-speed');
    options.append(div);

    check = document.createElement('input');
    check.id = 'alt-speed-check';
    check.type = 'checkbox';
    check.checked = session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._TurtleState];
    check.addEventListener('change', (event_) => {
      this.remote.savePrefs({
        [_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._TurtleState]: event_.target.checked,
      });
    });
    div.append(check);
    elements.alt_speed_check = check;

    label = document.createElement('label');
    label.id = 'alt-speed-image';
    label.setAttribute('for', check.id);
    div.append(label);

    label = document.createElement('label');
    label.id = 'alt-speed-label';
    label.setAttribute('for', check.id);
    label.textContent = 'Use Temp limits';
    div.append(label);

    label = document.createElement('label');
    label.id = 'alt-speed-values-label';
    label.setAttribute('for', check.id);

    const up = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.speed(session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._TurtleUpSpeedLimit]);
    const dn = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.speed(session_properties[_remote_js__WEBPACK_IMPORTED_MODULE_2__.RPC._TurtleDownSpeedLimit]);
    label.textContent = `(${up} up, ${dn} down)`;
    div.append(label);

    section = make_section('actions', 'Actions');
    root.append(section);

    for (const action_name of [
      'show-preferences-dialog',
      'pause-all-torrents',
      'start-all-torrents',
    ]) {
      const text = this.action_manager.text(action_name);
      actions[action_name] = make_button(section, text, action_name, on_click);
    }

    section = make_section('info', 'Info');
    root.append(section);

    options = document.createElement('div');
    section.append(options);

    for (const action_name of [
      'show-about-dialog',
      'show-shortcuts-dialog',
      'show-statistics-dialog',
    ]) {
      const text = this.action_manager.text(action_name);
      actions[action_name] = make_button(options, text, action_name, on_click);
    }

    section = make_section('links', 'Links');
    root.append(section);

    options = document.createElement('div');
    section.append(options);

    let e = document.createElement('a');
    e.href = 'https://transmissionbt.com/';
    e.tabindex = '0';
    e.textContent = 'Homepage';
    options.append(e);

    e = document.createElement('a');
    e.href = 'https://transmissionbt.com/donate/';
    e.tabindex = '0';
    e.textContent = 'Tip Jar';
    options.append(e);

    e = document.createElement('a');
    e.href = 'https://github.com/transmission/transmission/';
    e.tabindex = '0';
    e.textContent = 'Source Code';
    options.append(e);

    this._updateElement = this._updateElement.bind(this);

    return { actions, elements, root };
  }
}


/***/ }),

/***/ "./src/prefs-dialog.js":
/*!*****************************!*\
  !*** ./src/prefs-dialog.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "PrefsDialog": () => (/* binding */ PrefsDialog)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */




class PrefsDialog extends EventTarget {
  static _initTimeDropDown(e) {
    for (let index = 0; index < 24 * 4; ++index) {
      const hour = Number.parseInt(index / 4, 10);
      const mins = (index % 4) * 15;
      const value = index * 15;
      const content = `${hour}:${mins || '00'}`;
      e.options[index] = new Option(content, value);
    }
  }

  static _initDayDropDown(e) {
    const options = [
      ['Everyday', '127'],
      ['Weekdays', '62'],
      ['Weekends', '65'],
      ['Sunday', '1'],
      ['Monday', '2'],
      ['Tuesday', '4'],
      ['Wednesday', '8'],
      ['Thursday', '16'],
      ['Friday', '32'],
      ['Saturday', '64'],
    ];
    for (let index = 0; options[index]; ++index) {
      const [text, value] = options[index];
      e.options[index] = new Option(text, value);
    }
  }

  _checkPort() {
    const element = this.elements.network.port_status_label;
    element.removeAttribute('data-open');
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(element, 'Checking...');
    this.remote.checkPort(this._onPortChecked, this);
  }

  _onPortChecked(response) {
    const element = this.elements.network.port_status_label;
    const is_open = response.arguments['port-is-open'];
    element.dataset.open = is_open;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(element, is_open ? 'Open' : 'Closed');
  }

  _setBlocklistButtonEnabled(b) {
    const e = this.elements.peers.blocklist_update_button;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setEnabled)(e, b);
    e.value = b ? 'Update' : 'Updating...';
  }

  static _getValue(e) {
    switch (e.type) {
      case 'checkbox':
      case 'radio':
        return e.checked;

      case 'number':
      case 'text':
      case 'url': {
        const string = e.value;
        if (Number.parseInt(string, 10).toString() === string) {
          return Number.parseInt(string, 10);
        }
        if (Number.parseFloat(string).toString() === string) {
          return Number.parseFloat(string);
        }
        return string;
      }

      default:
        return null;
    }
  }

  // this callback is for controls whose changes can be applied
  // immediately, like checkboxs, radioboxes, and selects
  _onControlChanged(event_) {
    const { key } = event_.target.dataset;
    this.remote.savePrefs({
      [key]: PrefsDialog._getValue(event_.target),
    });
    if (key === 'peer-port' || key === 'port-forwarding-enabled') {
      this._checkPort();
    }
  }

  _onDialogClosed() {
    this.dispatchEvent(new Event('closed'));
  }

  // update the dialog's controls
  _update(o) {
    if (!o) {
      return;
    }

    this._setBlocklistButtonEnabled(true);

    for (const [key, value] of Object.entries(o)) {
      for (const element of this.elements.root.querySelectorAll(
        `[data-key="${key}"]`
      )) {
        if (key === 'blocklist-size') {
          const n = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.number(value);
          element.innerHTML = `Blocklist has <span class="blocklist-size-number">${n}</span> rules`;
          (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.peers.blocklist_update_button, 'Update');
        } else {
          switch (element.type) {
            case 'checkbox':
            case 'radio':
              if (element.checked !== value) {
                element.checked = value;
                element.dispatchEvent(new Event('change'));
              }
              break;
            case 'text':
            case 'url':
            case 'email':
            case 'number':
            case 'search':
              // don't change the text if the user's editing it.
              // it's very annoying when that happens!
              if (
                // eslint-disable-next-line eqeqeq
                element.value != value &&
                element !== document.activeElement
              ) {
                element.value = value;
                element.dispatchEvent(new Event('change'));
              }
              break;
            case 'select-one':
              if (element.value !== value) {
                element.value = value;
                element.dispatchEvent(new Event('change'));
              }
              break;
            default:
              break;
          }
        }
      }
    }
  }

  shouldAddedTorrentsStart() {
    return this.data.elements.root.find('#start-added-torrents')[0].checked;
  }

  static _createCheckAndLabel(id, text) {
    const root = document.createElement('div');
    root.id = id;

    const check = document.createElement('input');
    check.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    check.type = 'checkbox';
    root.append(check);

    const label = document.createElement('label');
    label.textContent = text;
    label.setAttribute('for', check.id);
    root.append(label);

    return { check, label, root };
  }

  static _enableIfChecked(element, check) {
    const callback = () => {
      if (element.tagName === 'INPUT') {
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setEnabled)(element, check.checked);
      } else {
        element.classList.toggle('disabled', !check.checked);
      }
    };
    check.addEventListener('change', callback);
    callback();
  }

  static _createTorrentsPage() {
    const root = document.createElement('div');
    root.classList.add('prefs-torrents-page');

    let label = document.createElement('div');
    label.textContent = 'Downloading';
    label.classList.add('section-label');
    root.append(label);

    label = document.createElement('label');
    label.textContent = 'Download to:';
    root.append(label);

    let input = document.createElement('input');
    input.type = 'text';
    input.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    input.dataset.key = 'download-dir';
    label.setAttribute('for', input.id);
    root.append(input);
    const download_dir = input;

    let cal = PrefsDialog._createCheckAndLabel(
      'autostart-div',
      'Start when added'
    );
    cal.check.dataset.key = 'start-added-torrents';
    root.append(cal.root);
    const autostart_check = cal.check;

    cal = PrefsDialog._createCheckAndLabel(
      'suffix-div',
      `Append "part" to incomplete files' names`
    );
    cal.check.dataset.key = 'rename-partial-files';
    root.append(cal.root);
    const suffix_check = cal.check;

    label = document.createElement('div');
    label.textContent = 'Seeding';
    label.classList.add('section-label');
    root.append(label);

    cal = PrefsDialog._createCheckAndLabel(
      'stop-ratio-div',
      'Stop seeding at ratio:'
    );
    cal.check.dataset.key = 'seedRatioLimited';
    root.append(cal.root);
    const stop_ratio_check = cal.check;

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'seedRatioLimit';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const stop_ratio_input = input;

    cal = PrefsDialog._createCheckAndLabel(
      'stop-idle-div',
      'Stop seeding if idle for N mins:'
    );
    cal.check.dataset.key = 'idle-seeding-limit-enabled';
    root.append(cal.root);
    const stop_idle_check = cal.check;

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'idle-seeding-limit';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const stop_idle_input = input;

    return {
      autostart_check,
      download_dir,
      root,
      stop_idle_check,
      stop_idle_input,
      stop_ratio_check,
      stop_ratio_input,
      suffix_check,
    };
  }

  static _createSpeedPage() {
    const root = document.createElement('div');
    root.classList.add('prefs-speed-page');

    let label = document.createElement('div');
    label.textContent = 'Speed Limits';
    label.classList.add('section-label');
    root.append(label);

    let cal = PrefsDialog._createCheckAndLabel(
      'upload-speed-div',
      'Upload (kB/s):'
    );
    cal.check.dataset.key = 'speed-limit-up-enabled';
    root.append(cal.root);
    const upload_speed_check = cal.check;

    let input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'speed-limit-up';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const upload_speed_input = input;

    cal = PrefsDialog._createCheckAndLabel(
      'download-speed-div',
      'Download (kB/s):'
    );
    cal.check.dataset.key = 'speed-limit-down-enabled';
    root.append(cal.root);
    const download_speed_check = cal.check;

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'speed-limit-down';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const download_speed_input = input;

    label = document.createElement('div');
    label.textContent = 'Alternative Speed Limits';
    label.classList.add('section-label', 'alt-speed-section-label');
    root.append(label);

    label = document.createElement('div');
    label.textContent =
      'Override normal speed limits manually or at scheduled times';
    label.classList.add('alt-speed-label');
    root.append(label);

    label = document.createElement('label');
    label.textContent = 'Upload (kB/s):';
    root.append(label);

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'alt-speed-up';
    input.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    label.setAttribute('for', input.id);
    root.append(input);
    const alt_upload_speed_input = input;

    label = document.createElement('label');
    label.textContent = 'Download (kB/s):';
    root.append(label);

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'alt-speed-down';
    input.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    label.setAttribute('for', input.id);
    root.append(input);
    const alt_download_speed_input = input;

    cal = PrefsDialog._createCheckAndLabel('alt-times-div', 'Scheduled times');
    cal.check.dataset.key = 'alt-speed-time-enabled';
    root.append(cal.root);
    const alt_times_check = cal.check;

    label = document.createElement('label');
    label.textContent = 'From:';
    PrefsDialog._enableIfChecked(label, cal.check);
    root.append(label);

    let select = document.createElement('select');
    select.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    select.dataset.key = 'alt-speed-time-begin';
    PrefsDialog._initTimeDropDown(select);
    label.setAttribute('for', select.id);
    root.append(select);
    PrefsDialog._enableIfChecked(select, cal.check);
    const alt_from_select = select;

    label = document.createElement('label');
    label.textContent = 'To:';
    PrefsDialog._enableIfChecked(label, cal.check);
    root.append(label);

    select = document.createElement('select');
    select.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    select.dataset.key = 'alt-speed-time-end';
    PrefsDialog._initTimeDropDown(select);
    label.setAttribute('for', select.id);
    root.append(select);
    PrefsDialog._enableIfChecked(select, cal.check);
    const alt_to_select = select;

    label = document.createElement('label');
    label.textContent = 'On days:';
    PrefsDialog._enableIfChecked(label, cal.check);
    root.append(label);

    select = document.createElement('select');
    select.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    select.dataset.key = 'alt-speed-time-day';
    PrefsDialog._initDayDropDown(select);
    label.setAttribute('for', select.id);
    root.append(select);
    PrefsDialog._enableIfChecked(select, cal.check);
    const alt_days_select = select;

    return {
      alt_days_select,
      alt_download_speed_input,
      alt_from_select,
      alt_times_check,
      alt_to_select,
      alt_upload_speed_input,
      download_speed_check,
      download_speed_input,
      root,
      upload_speed_check,
      upload_speed_input,
    };
  }

  static _createPeersPage() {
    const root = document.createElement('div');
    root.classList.add('prefs-peers-page');

    let label = document.createElement('div');
    label.textContent = 'Connections';
    label.classList.add('section-label');
    root.append(label);

    let cal = PrefsDialog._createCheckAndLabel(
      'max-peers-per-torrent-div',
      'Max peers per torrent:'
    );
    root.append(cal.root);
    const max_peers_per_torrent_check = cal.check;

    let input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'peer-limit-per-torrent';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const max_peers_per_torrent_input = input;

    cal = PrefsDialog._createCheckAndLabel(
      'max-peers-overall-div',
      'Max peers overall:'
    );
    root.append(cal.root);
    const max_peers_overall_check = cal.check;

    input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'peer-limit-global';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const max_peers_overall_input = input;

    label = document.createElement('div');
    label.textContent = 'Options';
    label.classList.add('section-label');
    root.append(label);

    label = document.createElement('label');
    label.textContent = 'Encryption mode:';
    root.append(label);

    const select = document.createElement('select');
    select.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    select.dataset.key = 'encryption';
    select.options[0] = new Option('Prefer encryption', 'preferred');
    select.options[1] = new Option('Allow encryption', 'tolerated');
    select.options[2] = new Option('Require encryption', 'required');
    root.append(select);
    const encryption_select = select;

    cal = PrefsDialog._createCheckAndLabel(
      'use-pex-div',
      'Use PEX to find more peers'
    );
    cal.check.title =
      "PEX is a tool for exchanging peer lists with the peers you're connected to.";
    cal.check.dataset.key = 'pex-enabled';
    cal.label.title = cal.check.title;
    root.append(cal.root);
    const pex_check = cal.check;

    cal = PrefsDialog._createCheckAndLabel(
      'use-dht-div',
      'Use DHT to find more peers'
    );
    cal.check.title = 'DHT is a tool for finding peers without a tracker.';
    cal.check.dataset.key = 'dht-enabled';
    cal.label.title = cal.check.title;
    root.append(cal.root);
    const dht_check = cal.check;

    cal = PrefsDialog._createCheckAndLabel(
      'use-lpd-div',
      'Use LPD to find more peers'
    );
    cal.check.title = 'LPD is a tool for finding peers on your local network.';
    cal.check.dataset.key = 'lpd-enabled';
    cal.label.title = cal.check.title;
    root.append(cal.root);
    const lpd_check = cal.check;

    label = document.createElement('div');
    label.textContent = 'Blocklist';
    label.classList.add('section-label');
    root.append(label);

    cal = PrefsDialog._createCheckAndLabel(
      'blocklist-enabled-div',
      'Enable blocklist:'
    );
    cal.check.dataset.key = 'blocklist-enabled';
    root.append(cal.root);
    const blocklist_enabled_check = cal.check;

    input = document.createElement('input');
    input.type = 'url';
    input.value = 'http://www.example.com/blocklist';
    input.dataset.key = 'blocklist-url';
    root.append(input);
    PrefsDialog._enableIfChecked(input, cal.check);
    const blocklist_url_input = input;

    label = document.createElement('label');
    label.textContent = 'Blocklist has {n} rules';
    label.dataset.key = 'blocklist-size';
    label.classList.add('blocklist-size-label');
    PrefsDialog._enableIfChecked(label, cal.check);
    root.append(label);

    const button = document.createElement('button');
    button.classList.add('blocklist-update-button');
    button.textContent = 'Update';
    root.append(button);
    PrefsDialog._enableIfChecked(button, cal.check);
    const blocklist_update_button = button;

    return {
      blocklist_enabled_check,
      blocklist_update_button,
      blocklist_url_input,
      dht_check,
      encryption_select,
      lpd_check,
      max_peers_overall_check,
      max_peers_overall_input,
      max_peers_per_torrent_check,
      max_peers_per_torrent_input,
      pex_check,
      root,
    };
  }

  static _createNetworkPage() {
    const root = document.createElement('div');
    root.classList.add('prefs-network-page');

    let label = document.createElement('div');
    label.textContent = 'Listening Port';
    label.classList.add('section-label');
    root.append(label);

    label = document.createElement('label');
    label.textContent = 'Peer listening port:';
    root.append(label);

    const input = document.createElement('input');
    input.type = 'number';
    input.dataset.key = 'peer-port';
    input.id = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.makeUUID)();
    label.setAttribute('for', input.id);
    root.append(input);
    const port_input = input;

    const port_status_div = document.createElement('div');
    port_status_div.classList.add('port-status');
    label = document.createElement('label');
    label.textContent = 'Port is';
    port_status_div.append(label);
    const port_status_label = document.createElement('label');
    port_status_label.textContent = '?';
    port_status_label.classList.add('port-status-label');
    port_status_div.append(port_status_label);
    root.append(port_status_div);

    let cal = PrefsDialog._createCheckAndLabel(
      'randomize-port',
      'Randomize port on launch'
    );
    cal.check.dataset.key = 'peer-port-random-on-start';
    root.append(cal.root);
    const random_port_check = cal.check;

    cal = PrefsDialog._createCheckAndLabel(
      'port-forwarding',
      'Use port forwarding from my router'
    );
    cal.check.dataset.key = 'port-forwarding-enabled';
    root.append(cal.root);
    const port_forwarding_check = cal.check;

    label = document.createElement('div');
    label.textContent = 'Options';
    label.classList.add('section-label');
    root.append(label);

    cal = PrefsDialog._createCheckAndLabel(
      'utp-enabled',
      'Enable uTP for peer communication'
    );
    cal.check.dataset.key = 'utp-enabled';
    root.append(cal.root);
    const utp_check = cal.check;

    return {
      port_forwarding_check,
      port_input,
      port_status_label,
      random_port_check,
      root,
      utp_check,
    };
  }

  static _create() {
    const pages = {
      network: PrefsDialog._createNetworkPage(),
      peers: PrefsDialog._createPeersPage(),
      speed: PrefsDialog._createSpeedPage(),
      torrents: PrefsDialog._createTorrentsPage(),
    };

    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.createTabsContainer)('prefs-dialog', [
      ['prefs-tab-torrent', pages.torrents.root],
      ['prefs-tab-speed', pages.speed.root],
      ['prefs-tab-peers', pages.peers.root],
      ['prefs-tab-network', pages.network.root],
    ]);

    return { ...elements, ...pages };
  }

  constructor(session_manager, remote) {
    super();

    this.closed = false;
    this.session_manager = session_manager;
    this.remote = remote;
    this.update_soon = () =>
      this._update(this.session_manager.session_properties);

    this.elements = PrefsDialog._create();
    this.elements.peers.blocklist_update_button.addEventListener(
      'click',
      (event_) => {
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(event_.target, 'Updating blocklist...');
        this.remote.updateBlocklist();
        this._setBlocklistButtonEnabled(false);
      }
    );
    this.outside = new _utils_js__WEBPACK_IMPORTED_MODULE_1__.OutsideClickListener(this.elements.root);
    this.outside.addEventListener('click', () => this.close());

    Object.seal(this);

    // listen for user input
    const on_change = this._onControlChanged.bind(this);
    const walk = (o) => {
      for (const element of Object.values(o)) {
        if (element.tagName === 'INPUT') {
          switch (element.type) {
            case 'checkbox':
            case 'radio':
            case 'number':
            case 'text':
            case 'url':
              element.addEventListener('change', on_change);
              break;
            default:
              console.trace(`unhandled input: ${element.type}`);
              break;
          }
        }
      }
    };
    walk(this.elements.network);
    walk(this.elements.peers);
    walk(this.elements.speed);
    walk(this.elements.torrents);

    this.session_manager.addEventListener('session-change', this.update_soon);
    this.update_soon();

    document.body.append(this.elements.root);
  }

  close() {
    if (!this.closed) {
      this.outside.stop();
      this.session_manager.removeEventListener(
        'session-change',
        this.update_soon
      );
      this.elements.root.remove();
      dispatchEvent(new Event('close'));
      for (const key of Object.keys(this)) {
        this[key] = null;
      }
      this.closed = true;
    }
  }
}


/***/ }),

/***/ "./src/prefs.js":
/*!**********************!*\
  !*** ./src/prefs.js ***!
  \**********************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Prefs": () => (/* binding */ Prefs)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class Prefs extends EventTarget {
  constructor() {
    super();

    this._cache = {};

    this.dispatchPrefsChange = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.debounce)((key, old_value, value) => {
      const event = new Event('change');
      Object.assign(event, { key, old_value, value });
      this.dispatchEvent(event);
    });

    for (const [key, default_value] of Object.entries(Prefs._Defaults)) {
      // populate the cache...
      this._set(key, Prefs._getCookie(key, default_value));

      // add property getter/setters...
      Object.defineProperty(this, key.replaceAll('-', '_'), {
        get: () => this._get(key),
        set: (value) => {
          this._set(key, value);
        },
      });
    }

    Object.seal(this);
  }

  entries() {
    return Object.entries(this._cache);
  }

  keys() {
    return Object.keys(this._cache);
  }

  _get(key) {
    const { _cache } = this;
    if (!Object.prototype.hasOwnProperty.call(_cache, key)) {
      throw new Error(key);
    }
    return _cache[key];
  }

  _set(key, value) {
    const { _cache } = this;
    const old_value = _cache[key];
    if (old_value !== value) {
      _cache[key] = value;
      Prefs._setCookie(key, value);
      this.dispatchPrefsChange(key, old_value, value);
    }
  }

  static _setCookie(key, value) {
    const date = new Date();
    date.setFullYear(date.getFullYear() + 1);
    // eslint-disable-next-line unicorn/no-document-cookie
    document.cookie = `${key}=${value}; SameSite=Strict; expires=${date.toGMTString()}; path=/`;
  }

  static _getCookie(key, fallback) {
    const value = Prefs._readCookie(key);
    if (value === null) {
      return fallback;
    }
    if (value === 'true') {
      return true;
    }
    if (value === 'false') {
      return false;
    }
    if (/^\d+$/.test(value)) {
      return Number.parseInt(value, 10);
    }
    return value;
  }

  static _readCookie(key) {
    const value = `; ${document.cookie}`;
    const parts = value.split(`; ${key}=`);
    return parts.length === 2 ? parts.pop().split(';').shift() : null;
  }
}

Prefs.AltSpeedEnabled = 'alt-speed-enabled';
Prefs.DisplayCompact = 'compact';
Prefs.DisplayFull = 'full';
Prefs.DisplayMode = 'display-mode';
Prefs.FilterActive = 'active';
Prefs.FilterAll = 'all';
Prefs.FilterDownloading = 'downloading';
Prefs.FilterFinished = 'finished';
Prefs.FilterMode = 'filter-mode';
Prefs.FilterPaused = 'paused';
Prefs.FilterSeeding = 'seeding';
Prefs.NotificationsEnabled = 'notifications-enabled';
Prefs.RefreshRate = 'refresh-rate-sec';
Prefs.SortAscending = 'ascending';
Prefs.SortByActivity = 'activity';
Prefs.SortByAge = 'age';
Prefs.SortByName = 'name';
Prefs.SortByProgress = 'progress';
Prefs.SortByQueue = 'queue';
Prefs.SortByRatio = 'ratio';
Prefs.SortBySize = 'size';
Prefs.SortByState = 'state';
Prefs.SortDescending = 'descending';
Prefs.SortDirection = 'sort-direction';
Prefs.SortMode = 'sort-mode';

Prefs._Defaults = {
  [Prefs.AltSpeedEnabled]: false,
  [Prefs.DisplayMode]: Prefs.DisplayFull,
  [Prefs.FilterMode]: Prefs.FilterAll,
  [Prefs.NotificationsEnabled]: false,
  [Prefs.RefreshRate]: 5,
  [Prefs.SortDirection]: Prefs.SortAscending,
  [Prefs.SortMode]: Prefs.SortByName,
};


/***/ }),

/***/ "./src/remote.js":
/*!***********************!*\
  !*** ./src/remote.js ***!
  \***********************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "RPC": () => (/* binding */ RPC),
/* harmony export */   "Remote": () => (/* binding */ Remote)
/* harmony export */ });
/* harmony import */ var _alert_dialog_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./alert-dialog.js */ "./src/alert-dialog.js");
/**
 * @license
 *
 * Copyright © Charles Kerr, Dave Perrett, Malcolm Jarvis and Bruno Bierbaumer
 *
 * This file is licensed under the GPLv2.
 * http://www.gnu.org/licenses/old-licenses/gpl-2.0.html
 */



const RPC = {
  _DaemonVersion: 'version',
  _DownSpeedLimit: 'speed-limit-down',
  _DownSpeedLimited: 'speed-limit-down-enabled',
  _QueueMoveBottom: 'queue-move-bottom',
  _QueueMoveDown: 'queue-move-down',
  _QueueMoveTop: 'queue-move-top',
  _QueueMoveUp: 'queue-move-up',
  _Root: '../rpc',
  _TurtleDownSpeedLimit: 'alt-speed-down',
  _TurtleState: 'alt-speed-enabled',
  _TurtleUpSpeedLimit: 'alt-speed-up',
  _UpSpeedLimit: 'speed-limit-up',
  _UpSpeedLimited: 'speed-limit-up-enabled',
};

class Remote {
  // TODO: decouple from controller
  constructor(controller) {
    this._controller = controller;
    this._error = '';
    this._session_id = '';
  }

  sendRequest(data, callback, context) {
    const headers = new Headers();
    headers.append('cache-control', 'no-cache');
    headers.append('content-type', 'application/json');
    headers.append('pragma', 'no-cache');
    if (this._session_id) {
      headers.append(Remote._SessionHeader, this._session_id);
    }

    let response_argument = null;
    fetch(RPC._Root, {
      body: JSON.stringify(data),
      headers,
      method: 'POST',
    })
      .then((response) => {
        response_argument = response;
        if (response.status === 409) {
          const error = new Error(Remote._SessionHeader);
          error.header = response.headers.get(Remote._SessionHeader);
          throw error;
        }
        return response.json();
      })
      .then((payload) => {
        if (callback) {
          callback.call(context, payload, response_argument);
        }
      })
      .catch((error) => {
        if (error.message === Remote._SessionHeader) {
          // copy the session header and try again
          this._session_id = error.header;
          this.sendRequest(data, callback, context);
          return;
        }
        console.trace(error);
        this._controller.togglePeriodicSessionRefresh(false);
        this._controller.setCurrentPopup(
          new _alert_dialog_js__WEBPACK_IMPORTED_MODULE_0__.AlertDialog({
            heading: 'Connection failed',
            message:
              'Could not connect to the server. You may need to reload the page to reconnect.',
          })
        );
      });
  }

  // TODO: return a Promise
  loadDaemonPrefs(callback, context) {
    const o = {
      method: 'session-get',
    };
    this.sendRequest(o, callback, context);
  }

  checkPort(callback, context) {
    const o = {
      method: 'port-test',
    };
    this.sendRequest(o, callback, context);
  }

  renameTorrent(torrentIds, oldpath, newname, callback, context) {
    const o = {
      arguments: {
        ids: torrentIds,
        name: newname,
        path: oldpath,
      },
      method: 'torrent-rename-path',
    };
    this.sendRequest(o, callback, context);
  }

  loadDaemonStats(callback, context) {
    const o = {
      method: 'session-stats',
    };
    this.sendRequest(o, callback, context);
  }

  updateTorrents(torrentIds, fields, callback, context) {
    const o = {
      arguments: {
        fields,
        format: 'table',
      },
      method: 'torrent-get',
    };
    if (torrentIds) {
      o.arguments.ids = torrentIds;
    }
    this.sendRequest(o, (response) => {
      const arguments_ = response['arguments'];
      callback.call(context, arguments_.torrents, arguments_.removed);
    });
  }

  getFreeSpace(dir, callback, context) {
    const o = {
      arguments: {
        path: dir,
      },
      method: 'free-space',
    };
    this.sendRequest(o, (response) => {
      const arguments_ = response['arguments'];
      callback.call(context, arguments_.path, arguments_['size-bytes']);
    });
  }

  changeFileCommand(torrentId, fileIndices, command) {
    const arguments_ = {
      ids: [torrentId],
    };
    arguments_[command] = fileIndices;
    this.sendRequest(
      {
        arguments: arguments_,
        method: 'torrent-set',
      },
      () => {
        this._controller.refreshTorrents([torrentId]);
      }
    );
  }

  sendTorrentSetRequests(method, torrent_ids, arguments_, callback, context) {
    if (!arguments_) {
      arguments_ = {};
    }
    arguments_['ids'] = torrent_ids;
    const o = {
      arguments: arguments_,
      method,
    };
    this.sendRequest(o, callback, context);
  }

  sendTorrentActionRequests(method, torrent_ids, callback, context) {
    this.sendTorrentSetRequests(method, torrent_ids, null, callback, context);
  }

  startTorrents(torrent_ids, noqueue, callback, context) {
    const name = noqueue ? 'torrent-start-now' : 'torrent-start';
    this.sendTorrentActionRequests(name, torrent_ids, callback, context);
  }
  stopTorrents(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      'torrent-stop',
      torrent_ids,
      callback,
      context
    );
  }

  moveTorrents(torrent_ids, new_location, callback, context) {
    this.sendTorrentSetRequests(
      'torrent-set-location',
      torrent_ids,
      {
        location: new_location,
        move: true,
      },
      callback,
      context
    );
  }

  removeTorrents(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      'torrent-remove',
      torrent_ids,
      callback,
      context
    );
  }
  removeTorrentsAndData(torrents) {
    const o = {
      arguments: {
        'delete-local-data': true,
        ids: [],
      },
      method: 'torrent-remove',
    };

    if (torrents) {
      for (let index = 0, length_ = torrents.length; index < length_; ++index) {
        o.arguments.ids.push(torrents[index].getId());
      }
    }
    this.sendRequest(o, () => {
      this._controller.refreshTorrents();
    });
  }
  verifyTorrents(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      'torrent-verify',
      torrent_ids,
      callback,
      context
    );
  }
  reannounceTorrents(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      'torrent-reannounce',
      torrent_ids,
      callback,
      context
    );
  }
  addTorrentByUrl(url, options) {
    if (/^[\da-f]{40}$/i.test(url)) {
      url = `magnet:?xt=urn:btih:${url}`;
    }
    const o = {
      arguments: {
        filename: url,
        paused: options.paused,
      },
      method: 'torrent-add',
    };
    this.sendRequest(o, () => {
      this._controller.refreshTorrents();
    });
  }
  savePrefs(arguments_) {
    const o = {
      arguments: arguments_,
      method: 'session-set',
    };
    this.sendRequest(o, () => {
      this._controller.loadDaemonPrefs();
    });
  }
  updateBlocklist() {
    const o = {
      method: 'blocklist-update',
    };
    this.sendRequest(o, () => {
      this._controller.loadDaemonPrefs();
    });
  }

  // Added queue calls
  moveTorrentsToTop(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      RPC._QueueMoveTop,
      torrent_ids,
      callback,
      context
    );
  }
  moveTorrentsToBottom(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      RPC._QueueMoveBottom,
      torrent_ids,
      callback,
      context
    );
  }
  moveTorrentsUp(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      RPC._QueueMoveUp,
      torrent_ids,
      callback,
      context
    );
  }
  moveTorrentsDown(torrent_ids, callback, context) {
    this.sendTorrentActionRequests(
      RPC._QueueMoveDown,
      torrent_ids,
      callback,
      context
    );
  }
}

Remote._SessionHeader = 'X-Transmission-Session-Id';


/***/ }),

/***/ "./src/remove-dialog.js":
/*!******************************!*\
  !*** ./src/remove-dialog.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "RemoveDialog": () => (/* binding */ RemoveDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class RemoveDialog extends EventTarget {
  constructor(options) {
    super();

    // options: remote, torrents, trash
    this.options = options;
    this.elements = RemoveDialog._create(options);
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    this.elements.confirm.addEventListener('click', () => this._onConfirm());
    document.body.append(this.elements.root);
    this.elements.dismiss.focus();
  }

  close() {
    if (!this.closed) {
      this.elements.root.remove();
      this.dispatchEvent(new Event('close'));
      for (const key of Object.keys(this)) {
        delete this[key];
      }
      this.closed = true;
    }
  }

  _onDismiss() {
    this.close();
  }

  _onConfirm() {
    const { remote, torrents, trash } = this.options;
    if (torrents.length > 0) {
      if (trash) {
        remote.removeTorrentsAndData(torrents);
      } else {
        remote.removeTorrents(torrents);
      }
    }

    this.close();
  }

  static _create(options) {
    const { trash } = options;
    const { heading, message } = RemoveDialog._createMessage(options);

    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('remove-dialog');
    elements.heading.textContent = heading;
    elements.message.textContent = message;
    elements.confirm.textContent = trash ? 'Trash' : 'Remove';
    return elements;
  }

  static _createMessage(options) {
    let heading = null;
    let message = null;
    const { torrents, trash } = options;
    const [torrent] = torrents;
    if (trash && torrents.length === 1) {
      heading = `Remove ${torrent.getName()} and delete data?`;
      message =
        'All data downloaded for this torrent will be deleted. Are you sure you want to remove it?';
    } else if (trash) {
      heading = `Remove ${torrents.length} transfers and delete data?`;
      message =
        'All data downloaded for these torrents will be deleted. Are you sure you want to remove them?';
    } else if (torrents.length === 1) {
      heading = `Remove ${torrent.getName()}?`;
      message =
        'Once removed, continuing the transfer will require the torrent file. Are you sure you want to remove it?';
    } else {
      heading = `Remove ${torrents.length} transfers?`;
      message =
        'Once removed, continuing the transfers will require the torrent files. Are you sure you want to remove them?';
    }
    return { heading, message };
  }
}


/***/ }),

/***/ "./src/rename-dialog.js":
/*!******************************!*\
  !*** ./src/rename-dialog.js ***!
  \******************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "RenameDialog": () => (/* binding */ RenameDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class RenameDialog extends EventTarget {
  constructor(controller, remote) {
    super();

    this.controller = controller;
    this.remote = remote;
    this.elements = {};
    this.torrents = [];

    this.show();
  }

  show() {
    const torrents = this.controller.getSelectedTorrents();
    if (torrents.length !== 1) {
      console.trace();
      return;
    }

    this.torrents = torrents;
    this.elements = RenameDialog._create();
    this.elements.entry.value = torrents[0].getName();
    document.body.append(this.elements.root);

    this.elements.entry.focus();
  }

  close() {
    this.elements.root.remove();

    this.dispatchEvent(new Event('close'));

    delete this.controller;
    delete this.remote;
    delete this.elements;
    delete this.torrents;
  }

  _onDismiss() {
    this.close();
  }

  _onConfirm() {
    const [tor] = this.torrents;
    const old_name = tor.getName();
    const new_name = this.elements.entry.value;
    this.remote.renameTorrent([tor.getId()], old_name, new_name, (response) => {
      if (response.result === 'success') {
        tor.refresh(response.arguments);
      }
    });

    this.close();
  }

  static _create() {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('rename-dialog');
    elements.root.setAttribute('aria-label', 'Rename Torrent');
    elements.heading.textContent = 'Enter new name:';
    elements.confirm.textContent = 'Rename';
    elements.dismiss.addEventListener('click', () => this._onDismiss());
    elements.confirm.addEventListener('click', () => this._onConfirm());

    const label = document.createElement('label');
    label.setAttribute('for', 'torrent-rename-name');
    label.textContent = 'Enter new name:';
    elements.workarea.append(label);

    const entry = document.createElement('input');
    entry.setAttribute('type', 'text');
    entry.id = 'torrent-rename-name';
    elements.entry = entry;
    elements.workarea.append(entry);

    return elements;
  }
}


/***/ }),

/***/ "./src/shortcuts-dialog.js":
/*!*********************************!*\
  !*** ./src/shortcuts-dialog.js ***!
  \*********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ShortcutsDialog": () => (/* binding */ ShortcutsDialog)
/* harmony export */ });
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



class ShortcutsDialog extends EventTarget {
  constructor(action_manager) {
    super();

    this.elements = ShortcutsDialog._create(action_manager);
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    document.body.append(this.elements.root);
    this.elements.dismiss.focus();
  }

  close() {
    this.elements.root.remove();
    this.dispatchEvent(new Event('close'));
    delete this.elements;
  }

  _onDismiss() {
    this.close();
  }

  static _create(action_manager) {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.createDialogContainer)('shortcuts-dialog');
    elements.root.setAttribute('aria-label', 'Keyboard Shortcuts');

    const table = document.createElement('table');
    const thead = document.createElement('thead');
    table.append(thead);

    let tr = document.createElement('tr');
    thead.append(tr);

    let th = document.createElement('th');
    th.textContent = 'Key';
    tr.append(th);

    th = document.createElement('th');
    th.textContent = 'Action';
    tr.append(th);

    const tbody = document.createElement('tbody');
    table.append(tbody);

    const o = {};
    for (const [shortcut, name] of action_manager.allShortcuts().entries()) {
      const tokens = shortcut.split('+');
      const sortKey = [tokens.pop(), ...tokens].join('+');
      o[sortKey] = { name, shortcut };
    }

    for (const [, values] of Object.entries(o).sort()) {
      const { name, shortcut } = values;
      tr = document.createElement('tr');
      tbody.append(tr);

      let td = document.createElement('td');
      td.textContent = shortcut.replaceAll('+', ' + ');
      tr.append(td);

      td = document.createElement('td');
      td.textContent = action_manager.text(name);
      tr.append(td);
    }

    elements.heading.textContent = 'Transmission';
    elements.dismiss.textContent = 'Close';

    elements.heading.textContent = 'Keyboard shortcuts';
    elements.message.append(table);
    elements.confirm.remove();
    delete elements.confirm;
    return elements;
  }
}


/***/ }),

/***/ "./src/statistics-dialog.js":
/*!**********************************!*\
  !*** ./src/statistics-dialog.js ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "StatisticsDialog": () => (/* binding */ StatisticsDialog)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */




class StatisticsDialog extends EventTarget {
  constructor(remote) {
    super();

    this.remote = remote;

    const updateDaemon = () =>
      this.remote.loadDaemonStats((data) => this._update(data.arguments));
    const delay_msec = 5000;
    this.interval = setInterval(updateDaemon, delay_msec);
    updateDaemon();

    this.elements = StatisticsDialog._create();
    this.elements.dismiss.addEventListener('click', () => this._onDismiss());
    document.body.append(this.elements.root);
    this.elements.dismiss.focus();
  }

  close() {
    if (!this.closed) {
      clearInterval(this.interval);
      this.elements.root.remove();
      for (const key of Object.keys(this)) {
        delete this[key];
      }
      this.closed = true;
    }
  }

  _onDismiss() {
    this.close();
  }

  _update(stats) {
    console.log(stats);
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter;

    let s = stats['current-stats'];
    let ratio = _utils_js__WEBPACK_IMPORTED_MODULE_1__.Utils.ratio(s.uploadedBytes, s.downloadedBytes);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.session.up, fmt.size(s.uploadedBytes));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.session.down, fmt.size(s.downloadedBytes));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.session.ratio, fmt.ratioString(ratio));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(
      this.elements.session.time,
      fmt.timeInterval(s.secondsActive)
    );

    s = stats['cumulative-stats'];
    ratio = _utils_js__WEBPACK_IMPORTED_MODULE_1__.Utils.ratio(s.uploadedBytes, s.downloadedBytes);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.total.up, fmt.size(s.uploadedBytes));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.total.down, fmt.size(s.downloadedBytes));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.total.ratio, fmt.ratioString(ratio));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.setTextContent)(this.elements.total.time, fmt.timeInterval(s.secondsActive));
  }

  static _create() {
    const elements = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.createDialogContainer)('statistics-dialog');
    const { confirm, dismiss, heading, root, workarea } = elements;
    confirm.remove();
    dismiss.textContent = 'Close';
    delete elements.confirm;

    const heading_text = 'Statistics';
    root.setAttribute('aria-label', heading_text);
    heading.textContent = heading_text;

    const labels = ['Uploaded:', 'Downloaded:', 'Ratio:', 'Running time:'];
    let section = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.createInfoSection)('Current session', labels);
    const [sup, sdown, sratio, stime] = section.children;
    const session = (elements.session = {});
    session.up = sup;
    session.down = sdown;
    session.ratio = sratio;
    session.time = stime;
    workarea.append(section.root);

    section = (0,_utils_js__WEBPACK_IMPORTED_MODULE_1__.createInfoSection)('Total', labels);
    const [tup, tdown, tratio, ttime] = section.children;
    const total = (elements.total = {});
    total.up = tup;
    total.down = tdown;
    total.ratio = tratio;
    total.time = ttime;
    workarea.append(section.root);

    return elements;
  }
}


/***/ }),

/***/ "./src/torrent-row.js":
/*!****************************!*\
  !*** ./src/torrent-row.js ***!
  \****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "TorrentRendererFull": () => (/* binding */ TorrentRendererFull),
/* harmony export */   "TorrentRendererCompact": () => (/* binding */ TorrentRendererCompact),
/* harmony export */   "TorrentRow": () => (/* binding */ TorrentRow)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _torrent_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./torrent.js */ "./src/torrent.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */





const TorrentRendererHelper = {
  formatDL: (t) => {
    return `▼${_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.speedBps(t.getDownloadSpeed())}`;
  },
  formatETA: (t) => {
    const eta = t.getETA();
    if (eta < 0 || eta >= 999 * 60 * 60) {
      return '';
    }
    return `ETA: ${_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.timeInterval(eta)}`;
  },
  formatUL: (t) => {
    return `▲${_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.speedBps(t.getUploadSpeed())}`;
  },
  getProgressInfo: (controller, t) => {
    const status = t.getStatus();
    const classList = ['torrent-progress-bar'];
    let percent = null;

    if (status === _torrent_js__WEBPACK_IMPORTED_MODULE_1__.Torrent._StatusStopped) {
      classList.push('paused');
    }

    if (t.needsMetaData()) {
      classList.push('magnet');
      percent = Math.round(t.getMetadataPercentComplete() * 100);
    } else if (status === _torrent_js__WEBPACK_IMPORTED_MODULE_1__.Torrent._StatusCheck) {
      classList.push('verify');
      percent = Math.round(t.getRecheckProgress() * 100);
    } else if (t.getLeftUntilDone() > 0) {
      classList.push('leech');
      percent = Math.round(t.getPercentDone() * 100);
    } else {
      classList.push('seed');
      const seed_ratio_limit = t.seedRatioLimit(controller);
      percent =
        seed_ratio_limit > 0
          ? (t.getUploadRatio() * 100) / seed_ratio_limit
          : 100;
    }
    if (t.isQueued()) {
      classList.push('queued');
    }

    return {
      classList,
      percent,
    };
  },

  renderProgressbar: (controller, t, progressbar) => {
    const info = TorrentRendererHelper.getProgressInfo(controller, t);
    progressbar.className = info.classList.join(' ');
    progressbar.style['background-size'] = `${info.percent}% 100%, 100% 100%`;
  },
};

///

class TorrentRendererFull {
  static getPeerDetails(t) {
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter;

    const error = t.getErrorMessage();
    if (error) {
      return error;
    }

    if (t.isDownloading()) {
      const peer_count = t.getPeersConnected();
      const webseed_count = t.getWebseedsSendingToUs();

      if (webseed_count && peer_count) {
        // Downloading from 2 of 3 peer(s) and 2 webseed(s)
        return [
          'Downloading from',
          t.getPeersSendingToUs(),
          'of',
          fmt.countString('peer', 'peers', peer_count),
          'and',
          fmt.countString('web seed', 'web seeds', webseed_count),
          '–',
          TorrentRendererHelper.formatDL(t),
          TorrentRendererHelper.formatUL(t),
        ].join(' ');
      }
      if (webseed_count) {
        // Downloading from 2 webseed(s)
        return [
          'Downloading from',
          fmt.countString('web seed', 'web seeds', webseed_count),
          '–',
          TorrentRendererHelper.formatDL(t),
          TorrentRendererHelper.formatUL(t),
        ].join(' ');
      }

      // Downloading from 2 of 3 peer(s)
      return [
        'Downloading from',
        t.getPeersSendingToUs(),
        'of',
        fmt.countString('peer', 'peers', peer_count),
        '–',
        TorrentRendererHelper.formatDL(t),
        TorrentRendererHelper.formatUL(t),
      ].join(' ');
    }

    if (t.isSeeding()) {
      return [
        'Seeding to',
        t.getPeersGettingFromUs(),
        'of',
        fmt.countString('peer', 'peers', t.getPeersConnected()),
        '-',
        TorrentRendererHelper.formatUL(t),
      ].join(' ');
    }

    if (t.isChecking()) {
      return [
        'Verifying local data (',
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.percentString(100 * t.getRecheckProgress()),
        '% tested)',
      ].join('');
    }

    return t.getStateString();
  }

  static getProgressDetails(controller, t) {
    if (t.needsMetaData()) {
      let MetaDataStatus = 'retrieving';
      if (t.isStopped()) {
        MetaDataStatus = 'needs';
      }
      const percent = 100 * t.getMetadataPercentComplete();
      return [
        `Magnetized transfer - ${MetaDataStatus} metadata (`,
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.percentString(percent),
        '%)',
      ].join('');
    }

    const sizeWhenDone = t.getSizeWhenDone();
    const totalSize = t.getTotalSize();
    const is_done = t.isDone() || t.isSeeding();
    const c = [];

    if (is_done) {
      if (totalSize === sizeWhenDone) {
        // seed: '698.05 MiB'
        c.push(_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(totalSize));
      } else {
        // partial seed: '127.21 MiB of 698.05 MiB (18.2%)'
        c.push(
          _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(sizeWhenDone),
          ' of ',
          _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(t.getTotalSize()),
          ' (',
          t.getPercentDoneStr(),
          '%)'
        );
      }
      // append UL stats: ', uploaded 8.59 GiB (Ratio: 12.3)'
      c.push(
        ', uploaded ',
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(t.getUploadedEver()),
        ' (Ratio ',
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.ratioString(t.getUploadRatio()),
        ')'
      );
    } else {
      // not done yet
      c.push(
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(sizeWhenDone - t.getLeftUntilDone()),
        ' of ',
        _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.size(sizeWhenDone),
        ' (',
        t.getPercentDoneStr(),
        '%)'
      );
    }

    // maybe append eta
    if (!t.isStopped() && (!is_done || t.seedRatioLimit(controller) > 0)) {
      c.push(' - ');
      const eta = t.getETA();
      if (eta < 0 || eta >= 999 * 60 * 60 /* arbitrary */) {
        c.push('remaining time unknown');
      } else {
        c.push(_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.timeInterval(t.getETA()), ' remaining');
      }
    }

    return c.join('');
  }

  // eslint-disable-next-line class-methods-use-this
  render(controller, t, root) {
    const is_stopped = t.isStopped();

    // name
    let e = root._name_container;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.setTextContent)(e, t.getName());
    e.classList.toggle('paused', is_stopped);

    // progressbar
    TorrentRendererHelper.renderProgressbar(controller, t, root._progressbar);
    root._progressbar.classList.add('full');

    // peer details
    const has_error = t.getError() !== _torrent_js__WEBPACK_IMPORTED_MODULE_1__.Torrent._ErrNone;
    e = root._peer_details_container;
    e.classList.toggle('error', has_error);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.setTextContent)(e, TorrentRendererFull.getPeerDetails(t));

    // progress details
    e = root._progress_details_container;
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.setTextContent)(e, TorrentRendererFull.getProgressDetails(controller, t));

    // pause/resume button
    e = root._toggle_running_button;
    e.alt = is_stopped ? 'Resume' : 'Pause';
    e.dataset.action = is_stopped ? 'resume' : 'pause';
  }

  // eslint-disable-next-line class-methods-use-this
  createRow(torrent) {
    const root = document.createElement('li');
    root.className = 'torrent';

    const icon = document.createElement('div');
    icon.classList.add('icon');
    icon.dataset.iconMimeType = torrent
      .getPrimaryMimeType()
      .split('/', 1)
      .pop();
    icon.dataset.iconMultifile = torrent.getFileCount() > 1 ? 'true' : 'false';

    const name = document.createElement('div');
    name.className = 'torrent-name';

    const peers = document.createElement('div');
    peers.className = 'torrent-peer-details';

    const progress = document.createElement('div');
    progress.classList.add('torrent-progress');
    const progressbar = document.createElement('div');
    progressbar.classList.add('torrent-progress-bar', 'full');
    progress.append(progressbar);
    const button = document.createElement('a');
    button.className = 'torrent-pauseresume-button';
    progress.append(button);

    const details = document.createElement('div');
    details.className = 'torrent-progress-details';

    root.append(icon);
    root.append(name);
    root.append(peers);
    root.append(progress);
    root.append(details);

    root._icon = icon;
    root._name_container = name;
    root._peer_details_container = peers;
    root._progress_details_container = details;
    root._progressbar = progressbar;
    root._toggle_running_button = button;

    return root;
  }
}

///

class TorrentRendererCompact {
  static getPeerDetails(t) {
    const errorMessage = t.getErrorMessage();
    if (errorMessage) {
      return errorMessage;
    }
    if (t.isDownloading()) {
      const have_dn = t.getDownloadSpeed() > 0;
      const have_up = t.getUploadSpeed() > 0;

      if (!have_up && !have_dn) {
        return 'Idle';
      }

      const s = [`${TorrentRendererHelper.formatETA(t)} `];
      if (have_dn) {
        s.push(TorrentRendererHelper.formatDL(t));
      }
      if (have_up) {
        s.push(TorrentRendererHelper.formatUL(t));
      }
      return s.join(' ');
    }
    if (t.isSeeding()) {
      return `Ratio: ${_formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.ratioString(
        t.getUploadRatio()
      )}, ${TorrentRendererHelper.formatUL(t)}`;
    }
    return t.getStateString();
  }

  // eslint-disable-next-line class-methods-use-this
  render(controller, t, root) {
    // name
    let e = root._name_container;
    e.classList.toggle('paused', t.isStopped());
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.setTextContent)(e, t.getName());

    // peer details
    const has_error = t.getError() !== _torrent_js__WEBPACK_IMPORTED_MODULE_1__.Torrent._ErrNone;
    e = root._details_container;
    e.classList.toggle('error', has_error);
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.setTextContent)(e, TorrentRendererCompact.getPeerDetails(t));

    // progressbar
    TorrentRendererHelper.renderProgressbar(controller, t, root._progressbar);
    root._progressbar.classList.add('compact');
  }

  // eslint-disable-next-line class-methods-use-this
  createRow(torrent) {
    const progressbar = document.createElement('div');
    progressbar.classList.add('torrent-progress-bar', 'compact');

    const icon = document.createElement('div');
    icon.classList.add('icon');
    icon.dataset.iconMimeType = torrent
      .getPrimaryMimeType()
      .split('/', 1)
      .pop();
    icon.dataset.iconMultifile = torrent.getFileCount() > 1 ? 'true' : 'false';

    const details = document.createElement('div');
    details.className = 'torrent-peer-details compact';

    const name = document.createElement('div');
    name.className = 'torrent-name compact';

    const root = document.createElement('li');
    root.append(progressbar);
    root.append(details);
    root.append(name);
    root.append(icon);
    root.className = 'torrent compact';
    root._progressbar = progressbar;
    root._details_container = details;
    root._name_container = name;
    return root;
  }
}

///

class TorrentRow {
  constructor(view, controller, torrent) {
    this._view = view;
    this._torrent = torrent;
    this._element = view.createRow(torrent);

    const update = () => this.render(controller);
    this._torrent.addEventListener('dataChanged', update);
    update();
  }

  getElement() {
    return this._element;
  }

  render(controller) {
    const tor = this.getTorrent();
    if (tor) {
      this._view.render(controller, tor, this.getElement());
    }
  }

  isSelected() {
    return this.getElement().classList.contains('selected');
  }

  getTorrent() {
    return this._torrent;
  }

  getTorrentId() {
    return this.getTorrent().getId();
  }
}


/***/ }),

/***/ "./src/torrent.js":
/*!************************!*\
  !*** ./src/torrent.js ***!
  \************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Torrent": () => (/* binding */ Torrent)
/* harmony export */ });
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _prefs_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./prefs.js */ "./src/prefs.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */





/// DOMAINS

// example: "tracker.ubuntu.com" returns "ubuntu.com"
function getDomainName(host) {
  const dot = host.indexOf('.');
  if (dot !== host.lastIndexOf('.')) {
    host = host.slice(dot + 1);
  }
  return host;
}

// example: "ubuntu.com" returns "Ubuntu"
function getReadableDomain(name) {
  if (name.length > 0) {
    name = name.charAt(0).toUpperCase() + name.slice(1);
  }
  const dot = name.indexOf('.');
  if (dot !== -1) {
    name = name.slice(0, dot);
  }
  return name;
}

// key: url string
// val: { domain, readable_domain }
const announce_to_domain_cache = {};

function getAnnounceDomain(announce) {
  if (announce_to_domain_cache[announce]) {
    return announce_to_domain_cache[announce];
  }

  const url = new URL(announce);
  const domain = getDomainName(url.host);
  const name = getReadableDomain(domain);
  const o = { domain, name, url };
  announce_to_domain_cache[announce] = o;
  return o;
}

///

class Torrent extends EventTarget {
  constructor(data) {
    super();

    this.fieldObservers = {};
    this.fields = {};
    this.refresh(data);
  }

  notifyOnFieldChange(field, callback) {
    this.fieldObservers[field] = this.fieldObservers[field] || [];
    this.fieldObservers[field].push(callback);
  }

  setField(o, name, value) {
    const old_value = o[name];

    if ((0,_utils_js__WEBPACK_IMPORTED_MODULE_2__.deepEqual)(old_value, value)) {
      return false;
    }

    const observers = this.fieldObservers[name];
    if (o === this.fields && observers && observers.length > 0) {
      for (const observer of observers) {
        observer.call(this, value, old_value, name);
      }
    }
    o[name] = value;
    return true;
  }

  // fields.files is an array of unions of RPC's "files" and "fileStats" objects.
  updateFiles(files) {
    let changed = false;
    const myfiles = this.fields.files || [];
    const keys = ['length', 'name', 'bytesCompleted', 'wanted', 'priority'];

    for (const [index, f] of files.entries()) {
      const myfile = myfiles[index] || {};
      for (const key of keys) {
        if (key in f) {
          changed |= this.setField(myfile, key, f[key]);
        }
      }
      myfiles[index] = myfile;
    }
    this.fields.files = myfiles;
    return changed;
  }

  static collateTrackers(trackers) {
    return trackers.map((t) => t.announce.toLowerCase()).join('\t');
  }

  refreshFields(data) {
    let changed = false;

    for (const [key, value] of Object.entries(data)) {
      switch (key) {
        case 'files':
        case 'fileStats': // merge files and fileStats together
          changed |= this.updateFiles(value);
          break;
        case 'trackerStats': // 'trackerStats' is a superset of 'trackers'...
          changed |= this.setField(this.fields, 'trackers', value);
          break;
        case 'trackers': // ...so only save 'trackers' if we don't have it already
          if (!(key in this.fields)) {
            changed |= this.setField(this.fields, key, value);
          }
          break;
        default:
          changed |= this.setField(this.fields, key, value);
      }
    }

    return changed;
  }

  refresh(data) {
    if (this.refreshFields(data)) {
      this.dispatchEvent(new Event('dataChanged'));
    }
  }

  ///

  // simple accessors
  getComment() {
    return this.fields.comment;
  }
  getCreator() {
    return this.fields.creator;
  }
  getDateAdded() {
    return this.fields.addedDate;
  }
  getDateCreated() {
    return this.fields.dateCreated;
  }
  getDesiredAvailable() {
    return this.fields.desiredAvailable;
  }
  getDownloadDir() {
    return this.fields.downloadDir;
  }
  getDownloadSpeed() {
    return this.fields.rateDownload;
  }
  getDownloadedEver() {
    return this.fields.downloadedEver;
  }
  getError() {
    return this.fields.error;
  }
  getErrorString() {
    return this.fields.errorString;
  }
  getETA() {
    return this.fields.eta;
  }
  getFailedEver() {
    return this.fields.corruptEver;
  }
  getFiles() {
    return this.fields.files || [];
  }
  getFile(index) {
    return this.fields.files[index];
  }
  getFileCount() {
    return this.fields['file-count'];
  }
  getHashString() {
    return this.fields.hashString;
  }
  getHave() {
    return this.getHaveValid() + this.getHaveUnchecked();
  }
  getHaveUnchecked() {
    return this.fields.haveUnchecked;
  }
  getHaveValid() {
    return this.fields.haveValid;
  }
  getId() {
    return this.fields.id;
  }
  getLastActivity() {
    return this.fields.activityDate;
  }
  getLeftUntilDone() {
    return this.fields.leftUntilDone;
  }
  getMetadataPercentComplete() {
    return this.fields.metadataPercentComplete;
  }
  getName() {
    return this.fields.name || 'Unknown';
  }
  getPeers() {
    return this.fields.peers || [];
  }
  getPeersConnected() {
    return this.fields.peersConnected;
  }
  getPeersGettingFromUs() {
    return this.fields.peersGettingFromUs;
  }
  getPeersSendingToUs() {
    return this.fields.peersSendingToUs;
  }
  getPieceCount() {
    return this.fields.pieceCount;
  }
  getPieceSize() {
    return this.fields.pieceSize;
  }
  getPrimaryMimeType() {
    return this.fields['primary-mime-type'];
  }
  getPrivateFlag() {
    return this.fields.isPrivate;
  }
  getQueuePosition() {
    return this.fields.queuePosition;
  }
  getRecheckProgress() {
    return this.fields.recheckProgress;
  }
  getSeedRatioLimit() {
    return this.fields.seedRatioLimit;
  }
  getSeedRatioMode() {
    return this.fields.seedRatioMode;
  }
  getSizeWhenDone() {
    return this.fields.sizeWhenDone;
  }
  getStartDate() {
    return this.fields.startDate;
  }
  getStatus() {
    return this.fields.status;
  }
  getTotalSize() {
    return this.fields.totalSize;
  }
  getTrackers() {
    const trackers = this.fields.trackers || [];
    for (const tracker of trackers) {
      if (tracker.announce && !tracker.domain) {
        Object.assign(tracker, getAnnounceDomain(tracker.announce));
      }
    }
    return this.fields.trackers;
  }
  getUploadSpeed() {
    return this.fields.rateUpload;
  }
  getUploadRatio() {
    return this.fields.uploadRatio;
  }
  getUploadedEver() {
    return this.fields.uploadedEver;
  }
  getWebseedsSendingToUs() {
    return this.fields.webseedsSendingToUs;
  }
  isFinished() {
    return this.fields.isFinished;
  }

  // derived accessors
  hasExtraInfo() {
    return 'hashString' in this.fields;
  }
  isSeeding() {
    return this.getStatus() === Torrent._StatusSeed;
  }
  isStopped() {
    return this.getStatus() === Torrent._StatusStopped;
  }
  isChecking() {
    return this.getStatus() === Torrent._StatusCheck;
  }
  isDownloading() {
    return this.getStatus() === Torrent._StatusDownload;
  }
  isQueued() {
    return (
      this.getStatus() === Torrent._StatusDownloadWait ||
      this.getStatus() === Torrent._StatusSeedWait
    );
  }
  isDone() {
    return this.getLeftUntilDone() < 1;
  }
  needsMetaData() {
    return this.getMetadataPercentComplete() < 1;
  }
  getActivity() {
    return this.getDownloadSpeed() + this.getUploadSpeed();
  }
  getPercentDoneStr() {
    return _formatter_js__WEBPACK_IMPORTED_MODULE_0__.Formatter.percentString(100 * this.getPercentDone());
  }
  getPercentDone() {
    return this.fields.percentDone;
  }
  getStateString() {
    switch (this.getStatus()) {
      case Torrent._StatusStopped:
        return this.isFinished() ? 'Seeding complete' : 'Paused';
      case Torrent._StatusCheckWait:
        return 'Queued for verification';
      case Torrent._StatusCheck:
        return 'Verifying local data';
      case Torrent._StatusDownloadWait:
        return 'Queued for download';
      case Torrent._StatusDownload:
        return 'Downloading';
      case Torrent._StatusSeedWait:
        return 'Queued for seeding';
      case Torrent._StatusSeed:
        return 'Seeding';
      case null:
        return 'Unknown';
      default:
        return 'Error';
    }
  }
  seedRatioLimit(controller) {
    switch (this.getSeedRatioMode()) {
      case Torrent._RatioUseGlobal:
        return controller.seedRatioLimit();
      case Torrent._RatioUseLocal:
        return this.getSeedRatioLimit();
      default:
        return -1;
    }
  }
  getErrorMessage() {
    const string = this.getErrorString();
    switch (this.getError()) {
      case Torrent._ErrTrackerWarning:
        return `Tracker returned a warning: ${string}`;
      case Torrent._ErrTrackerError:
        return `Tracker returned an error: ${string}`;
      case Torrent._ErrLocalError:
        return `Error: ${string}`;
      default:
        return null;
    }
  }
  getCollatedName() {
    const f = this.fields;
    if (!f.collatedName && f.name) {
      f.collatedName = f.name.toLowerCase();
    }
    return f.collatedName || '';
  }
  getCollatedTrackers() {
    const f = this.fields;
    if (!f.collatedTrackers && f.trackers) {
      f.collatedTrackers = Torrent.collateTrackers(f.trackers);
    }
    return f.collatedTrackers || '';
  }

  /****
   *****
   ****/

  testState(state) {
    const s = this.getStatus();

    switch (state) {
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.FilterActive:
        return (
          this.getPeersGettingFromUs() > 0 ||
          this.getPeersSendingToUs() > 0 ||
          this.getWebseedsSendingToUs() > 0 ||
          this.isChecking()
        );
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.FilterSeeding:
        return s === Torrent._StatusSeed || s === Torrent._StatusSeedWait;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.FilterDownloading:
        return (
          s === Torrent._StatusDownload || s === Torrent._StatusDownloadWait
        );
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.FilterPaused:
        return this.isStopped();
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.FilterFinished:
        return this.isFinished();
      default:
        return true;
    }
  }

  /**
   * @param filter one of Prefs.Filter*
   * @param search substring to look for, or null
   * @return true if it passes the test, false if it fails
   */
  test(state, search, tracker) {
    // flter by state...
    let pass = this.testState(state);

    // maybe filter by text...
    if (pass && search && search.length > 0) {
      pass = this.getCollatedName().includes(search.toLowerCase());
    }

    // maybe filter by tracker...
    if (pass && tracker && tracker.length > 0) {
      pass = this.getCollatedTrackers().includes(tracker);
    }

    return pass;
  }

  static compareById(ta, tb) {
    return ta.getId() - tb.getId();
  }
  static compareByName(ta, tb) {
    return (
      ta.getCollatedName().localeCompare(tb.getCollatedName()) ||
      Torrent.compareById(ta, tb)
    );
  }
  static compareByQueue(ta, tb) {
    return ta.getQueuePosition() - tb.getQueuePosition();
  }
  static compareByAge(ta, tb) {
    const a = ta.getDateAdded();
    const b = tb.getDateAdded();

    return b - a || Torrent.compareByQueue(ta, tb);
  }
  static compareByState(ta, tb) {
    const a = ta.getStatus();
    const b = tb.getStatus();

    return b - a || Torrent.compareByQueue(ta, tb);
  }
  static compareByActivity(ta, tb) {
    const a = ta.getActivity();
    const b = tb.getActivity();

    return b - a || Torrent.compareByState(ta, tb);
  }
  static compareByRatio(ta, tb) {
    const a = ta.getUploadRatio();
    const b = tb.getUploadRatio();

    if (a < b) {
      return 1;
    }
    if (a > b) {
      return -1;
    }
    return Torrent.compareByState(ta, tb);
  }
  static compareByProgress(ta, tb) {
    const a = ta.getPercentDone();
    const b = tb.getPercentDone();

    return a - b || Torrent.compareByRatio(ta, tb);
  }
  static compareBySize(ta, tb) {
    const a = ta.getTotalSize();
    const b = tb.getTotalSize();

    return a - b || Torrent.compareByName(ta, tb);
  }

  static compareTorrents(a, b, sortMode, sortDirection) {
    let index = 0;

    switch (sortMode) {
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByActivity:
        index = Torrent.compareByActivity(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByAge:
        index = Torrent.compareByAge(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByQueue:
        index = Torrent.compareByQueue(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByProgress:
        index = Torrent.compareByProgress(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortBySize:
        index = Torrent.compareBySize(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByState:
        index = Torrent.compareByState(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByRatio:
        index = Torrent.compareByRatio(a, b);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByName:
        index = Torrent.compareByName(a, b);
        break;
      default:
        console.log(`Unrecognized sort mode: ${sortMode}`);
        index = Torrent.compareByName(a, b);
        break;
    }

    if (sortDirection === _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortDescending) {
      index = -index;
    }

    return index;
  }

  /**
   * @param torrents an array of Torrent objects
   * @param sortMode one of Prefs.SortBy*
   * @param sortDirection Prefs.SortAscending or Prefs.SortDescending
   */
  static sortTorrents(torrents, sortMode, sortDirection) {
    switch (sortMode) {
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByActivity:
        torrents.sort(this.compareByActivity);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByAge:
        torrents.sort(this.compareByAge);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByName:
        torrents.sort(this.compareByName);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByProgress:
        torrents.sort(this.compareByProgress);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByQueue:
        torrents.sort(this.compareByQueue);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByRatio:
        torrents.sort(this.compareByRatio);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortBySize:
        torrents.sort(this.compareBySize);
        break;
      case _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortByState:
        torrents.sort(this.compareByState);
        break;
      default:
        console.log(`Unrecognized sort mode: ${sortMode}`);
        torrents.sort(this.compareByName);
        break;
    }

    if (sortDirection === _prefs_js__WEBPACK_IMPORTED_MODULE_1__.Prefs.SortDescending) {
      torrents.reverse();
    }

    return torrents;
  }
}

// Torrent.fields.status
Torrent._StatusStopped = 0;
Torrent._StatusCheckWait = 1;
Torrent._StatusCheck = 2;
Torrent._StatusDownloadWait = 3;
Torrent._StatusDownload = 4;
Torrent._StatusSeedWait = 5;
Torrent._StatusSeed = 6;

// Torrent.fields.seedRatioMode
Torrent._RatioUseGlobal = 0;
Torrent._RatioUseLocal = 1;
Torrent._RatioUnlimited = 2;

// Torrent.fields.error
Torrent._ErrNone = 0;
Torrent._ErrTrackerWarning = 1;
Torrent._ErrTrackerError = 2;
Torrent._ErrLocalError = 3;

// TrackerStats' announceState
Torrent._TrackerInactive = 0;
Torrent._TrackerWaiting = 1;
Torrent._TrackerQueued = 2;
Torrent._TrackerActive = 3;

Torrent.Fields = {};

// commonly used fields which only need to be loaded once,
// either on startup or when a magnet finishes downloading its metadata
// finishes downloading its metadata
Torrent.Fields.Metadata = [
  'addedDate',
  'file-count',
  'name',
  'primary-mime-type',
  'totalSize',
];

// commonly used fields which need to be periodically refreshed
Torrent.Fields.Stats = [
  'error',
  'errorString',
  'eta',
  'isFinished',
  'isStalled',
  'leftUntilDone',
  'metadataPercentComplete',
  'peersConnected',
  'peersGettingFromUs',
  'peersSendingToUs',
  'percentDone',
  'queuePosition',
  'rateDownload',
  'rateUpload',
  'recheckProgress',
  'seedRatioMode',
  'seedRatioLimit',
  'sizeWhenDone',
  'status',
  'trackers',
  'downloadDir',
  'uploadedEver',
  'uploadRatio',
  'webseedsSendingToUs',
];

// fields used by the inspector which only need to be loaded once
Torrent.Fields.InfoExtra = [
  'comment',
  'creator',
  'dateCreated',
  'files',
  'hashString',
  'isPrivate',
  'pieceCount',
  'pieceSize',
];

// fields used in the inspector which need to be periodically refreshed
Torrent.Fields.StatsExtra = [
  'activityDate',
  'corruptEver',
  'desiredAvailable',
  'downloadedEver',
  'fileStats',
  'haveUnchecked',
  'haveValid',
  'peers',
  'startDate',
  'trackerStats',
];


/***/ }),

/***/ "./src/transmission.js":
/*!*****************************!*\
  !*** ./src/transmission.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Transmission": () => (/* binding */ Transmission)
/* harmony export */ });
/* harmony import */ var _about_dialog_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./about-dialog.js */ "./src/about-dialog.js");
/* harmony import */ var _context_menu_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./context-menu.js */ "./src/context-menu.js");
/* harmony import */ var _formatter_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./formatter.js */ "./src/formatter.js");
/* harmony import */ var _inspector_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./inspector.js */ "./src/inspector.js");
/* harmony import */ var _move_dialog_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./move-dialog.js */ "./src/move-dialog.js");
/* harmony import */ var _open_dialog_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./open-dialog.js */ "./src/open-dialog.js");
/* harmony import */ var _overflow_menu_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./overflow-menu.js */ "./src/overflow-menu.js");
/* harmony import */ var _prefs_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./prefs.js */ "./src/prefs.js");
/* harmony import */ var _prefs_dialog_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./prefs-dialog.js */ "./src/prefs-dialog.js");
/* harmony import */ var _remote_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./remote.js */ "./src/remote.js");
/* harmony import */ var _remove_dialog_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./remove-dialog.js */ "./src/remove-dialog.js");
/* harmony import */ var _rename_dialog_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./rename-dialog.js */ "./src/rename-dialog.js");
/* harmony import */ var _shortcuts_dialog_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./shortcuts-dialog.js */ "./src/shortcuts-dialog.js");
/* harmony import */ var _statistics_dialog_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./statistics-dialog.js */ "./src/statistics-dialog.js");
/* harmony import */ var _torrent_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./torrent.js */ "./src/torrent.js");
/* harmony import */ var _torrent_row_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./torrent-row.js */ "./src/torrent-row.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/**
 * @license
 *
 * Copyright © Charles Kerr, Dave Perrett, Malcolm Jarvis and Bruno Bierbaumer
 *
 * This file is licensed under the GPLv2.
 * http://www.gnu.org/licenses/old-licenses/gpl-2.0.html
 */



















class Transmission extends EventTarget {
  constructor(action_manager, notifications, prefs) {
    super();

    // Initialize the helper classes
    this.action_manager = action_manager;
    this.notifications = notifications;
    this.prefs = prefs;
    this.remote = new _remote_js__WEBPACK_IMPORTED_MODULE_9__.Remote(this);

    this.addEventListener('torrent-selection-changed', (event_) =>
      this.action_manager.update(event_)
    );

    // Initialize the implementation fields
    this.filterText = '';
    this._torrents = {};
    this._rows = [];
    this.dirtyTorrents = new Set();

    this.refilterSoon = (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.debounce)(() => this._refilter(false));
    this.refilterAllSoon = (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.debounce)(() => this._refilter(true));

    this.boundPopupCloseListener = this.popupCloseListener.bind(this);
    this.dispatchSelectionChangedSoon = (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.debounce)(
      () => this._dispatchSelectionChanged(),
      200
    );

    // listen to actions
    // TODO: consider adding a mutator listener here to pick up dynamic additions
    for (const element of document.querySelectorAll(`button[data-action]`)) {
      const { action } = element.dataset;
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.setEnabled)(element, this.action_manager.isEnabled(action));
      element.addEventListener('click', () => {
        this.action_manager.click(action);
      });
    }

    document
      .querySelector('#filter-tracker')
      .addEventListener('change', (event_) => {
        this.setFilterTracker(
          event_.target.value === 'all' ? null : event_.target.value
        );
      });

    this.action_manager.addEventListener('change', (event_) => {
      for (const element of document.querySelectorAll(
        `[data-action="${event_.action}"]`
      )) {
        (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.setEnabled)(element, event_.enabled);
      }
    });

    this.action_manager.addEventListener('click', (event_) => {
      switch (event_.action) {
        case 'deselect-all':
          this._deselectAll();
          break;
        case 'move-bottom':
          this._moveBottom();
          break;
        case 'move-down':
          this._moveDown();
          break;
        case 'move-top':
          this._moveTop();
          break;
        case 'move-up':
          this._moveUp();
          break;
        case 'open-torrent':
          this.setCurrentPopup(new _open_dialog_js__WEBPACK_IMPORTED_MODULE_5__.OpenDialog(this, this.remote));
          break;
        case 'pause-all-torrents':
          this._stopTorrents(this._getAllTorrents());
          break;
        case 'pause-selected-torrents':
          this._stopTorrents(this.getSelectedTorrents());
          break;
        case 'reannounce-selected-torrents':
          this._reannounceTorrents(this.getSelectedTorrents());
          break;
        case 'remove-selected-torrents':
          this._removeSelectedTorrents(false);
          break;
        case 'resume-selected-torrents':
          this._startSelectedTorrents(false);
          break;
        case 'resume-selected-torrents-now':
          this._startSelectedTorrents(true);
          break;
        case 'select-all':
          this._selectAll();
          break;
        case 'show-about-dialog':
          this.setCurrentPopup(new _about_dialog_js__WEBPACK_IMPORTED_MODULE_0__.AboutDialog(this.version_info));
          break;
        case 'show-inspector':
          this.setCurrentPopup(new _inspector_js__WEBPACK_IMPORTED_MODULE_3__.Inspector(this));
          break;
        case 'show-move-dialog':
          this.setCurrentPopup(new _move_dialog_js__WEBPACK_IMPORTED_MODULE_4__.MoveDialog(this, this.remote));
          break;
        case 'show-overflow-menu':
          if (this.popup instanceof _overflow_menu_js__WEBPACK_IMPORTED_MODULE_6__.OverflowMenu) {
            this.setCurrentPopup(null);
          } else {
            this.setCurrentPopup(
              new _overflow_menu_js__WEBPACK_IMPORTED_MODULE_6__.OverflowMenu(
                this,
                this.prefs,
                this.remote,
                this.action_manager
              )
            );
            const btnbox = document
              .querySelector('#toolbar-overflow')
              .getBoundingClientRect();
            (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.movePopup)(
              this.popup.root,
              btnbox.left + btnbox.width,
              btnbox.top + btnbox.height,
              document.body
            );
          }
          break;
        case 'show-preferences-dialog':
          this.setCurrentPopup(new _prefs_dialog_js__WEBPACK_IMPORTED_MODULE_8__.PrefsDialog(this, this.remote));
          break;
        case 'show-shortcuts-dialog':
          this.setCurrentPopup(new _shortcuts_dialog_js__WEBPACK_IMPORTED_MODULE_12__.ShortcutsDialog(this.action_manager));
          break;
        case 'show-statistics-dialog':
          this.setCurrentPopup(new _statistics_dialog_js__WEBPACK_IMPORTED_MODULE_13__.StatisticsDialog(this.remote));
          break;
        case 'show-rename-dialog':
          this.setCurrentPopup(new _rename_dialog_js__WEBPACK_IMPORTED_MODULE_11__.RenameDialog(this, this.remote));
          break;
        case 'start-all-torrents':
          this._startTorrents(this._getAllTorrents());
          break;
        case 'toggle-compact-rows':
          this.prefs.display_mode =
            this.prefs.display_mode !== _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.DisplayCompact
              ? _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.DisplayCompact
              : _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.DisplayFull;
          break;
        case 'trash-selected-torrents':
          this._removeSelectedTorrents(true);
          break;
        case 'verify-selected-torrents':
          this._verifyTorrents(this.getSelectedTorrents());
          break;
        default:
          console.warn(`unhandled action: ${event_.action}`);
      }
    });

    // listen to filter changes
    let e = document.querySelector('#filter-mode');
    e.value = this.prefs.filter_mode;
    e.addEventListener('change', (event_) => {
      this.prefs.filter_mode = event_.target.value;
    });

    //if (!isMobileDevice) {
    document.addEventListener('keydown', this._keyDown.bind(this));
    document.addEventListener('keyup', this._keyUp.bind(this));
    e = document.querySelector('#torrent-container');
    e.addEventListener('click', () => {
      if (this.popup && this.popup.name !== 'inspector') {
        this.setCurrentPopup(null);
      } else {
        this._deselectAll();
      }
    });
    e.addEventListener('dragenter', Transmission._dragenter);
    e.addEventListener('dragover', Transmission._dragenter);
    e.addEventListener('drop', this._drop.bind(this));
    this._setupSearchBox();
    //}

    this.elements = {
      torrent_list: document.querySelector('#torrent-list'),
    };

    this.elements.torrent_list.addEventListener('contextmenu', (event_) => {
      // ensure the clicked row is selected
      let row_element = event.target;
      while (row_element && !row_element.classList.contains('torrent')) {
        row_element = row_element.parentNode;
      }
      const row = this._rows.find((r) => r.getElement() === row_element);
      if (row && !row.isSelected()) {
        this._setSelectedRow(row);
      }

      const popup = new _context_menu_js__WEBPACK_IMPORTED_MODULE_1__.ContextMenu(this.action_manager);
      this.setCurrentPopup(popup);
      (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.movePopup)(
        popup.root,
        event_.x,
        event_.y,
        document.querySelector('#torrent-container')
      );
      event_.preventDefault();
    });

    // Get preferences & torrents from the daemon
    this.loadDaemonPrefs();
    this._initializeTorrents();
    this.refreshTorrents();
    this.togglePeriodicSessionRefresh(true);

    // this.updateButtonsSoon();

    this.prefs.addEventListener('change', ({ key, value }) =>
      this._onPrefChanged(key, value)
    );
    for (const [key, value] of this.prefs.entries()) {
      this._onPrefChanged(key, value);
    }
  }

  loadDaemonPrefs() {
    this.remote.loadDaemonPrefs((data) => {
      this.session_properties = data.arguments;
    });
  }

  get session_properties() {
    return this._session_properties;
  }
  set session_properties(o) {
    if ((0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.deepEqual)(this._session_properties, o)) {
      return;
    }

    this._session_properties = Object.seal(o);
    const event = new Event('session-change');
    event.session_properties = o;
    this.dispatchEvent(event);

    // TODO: maybe have this in a listener handler?
    this._updateGuiFromSession(o);
  }

  _setupSearchBox() {
    const e = document.querySelector('#torrent-search');
    const blur_token = 'blur';
    e.classList.add(blur_token);
    e.addEventListener('blur', () => e.classList.add(blur_token));
    e.addEventListener('focus', () => e.classList.remove(blur_token));
    e.addEventListener('keyup', () => this._setFilterText(e.value));
  }

  _onPrefChanged(key, value) {
    switch (key) {
      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.DisplayMode: {
        this.torrentRenderer =
          value === 'compact'
            ? new _torrent_row_js__WEBPACK_IMPORTED_MODULE_15__.TorrentRendererCompact()
            : new _torrent_row_js__WEBPACK_IMPORTED_MODULE_15__.TorrentRendererFull();
        this.refilterAllSoon();
        break;
      }

      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.FilterMode:
      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.SortDirection:
      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.SortMode:
        this.refilterAllSoon();
        break;

      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.RefreshRate: {
        clearInterval(this.refreshTorrentsInterval);
        const callback = this.refreshTorrents.bind(this);
        const msec = Math.max(2, this.prefs.refresh_rate_sec) * 1000;
        this.refreshTorrentsInterval = setInterval(callback, msec);
        break;
      }

      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.AltSpeedEnabled:
      case _prefs_js__WEBPACK_IMPORTED_MODULE_7__.Prefs.NotificationsEnabled:
      default:
        /*noop*/
        break;
    }
  }

  /// UTILITIES

  _getAllTorrents() {
    return Object.values(this._torrents);
  }

  static _getTorrentIds(torrents) {
    return torrents.map((t) => t.getId());
  }

  seedRatioLimit() {
    const p = this.session_properties;
    if (p && p.seedRatioLimited) {
      return p.seedRatioLimit;
    }
    return -1;
  }

  /// SELECTION

  _getSelectedRows() {
    return this._rows.filter((r) => r.isSelected());
  }

  getSelectedTorrents() {
    return this._getSelectedRows().map((r) => r.getTorrent());
  }

  _getSelectedTorrentIds() {
    return Transmission._getTorrentIds(this.getSelectedTorrents());
  }

  _setSelectedRow(row) {
    const e_sel = row ? row.getElement() : null;
    for (const e of this.elements.torrent_list.children) {
      e.classList.toggle('selected', e === e_sel);
    }
    this.dispatchSelectionChangedSoon();
  }

  _selectRow(row) {
    row.getElement().classList.add('selected');
    this.dispatchSelectionChangedSoon();
  }

  _deselectRow(row) {
    row.getElement().classList.remove('selected');
    this.dispatchSelectionChangedSoon();
  }

  _selectAll() {
    for (const e of this.elements.torrent_list.children) {
      e.classList.add('selected');
    }
    this.dispatchSelectionChangedSoon();
  }

  _deselectAll() {
    for (const e of this.elements.torrent_list.children) {
      e.classList.remove('selected');
    }
    this.dispatchSelectionChangedSoon();
    delete this._last_torrent_clicked;
  }

  _indexOfLastTorrent() {
    return this._rows.findIndex(
      (row) => row.getTorrentId() === this._last_torrent_clicked
    );
  }

  // Select a range from this row to the last clicked torrent
  _selectRange(row) {
    const last = this._indexOfLastTorrent();

    if (last === -1) {
      this._selectRow(row);
    } else {
      // select the range between the prevous & current
      const next = this._rows.indexOf(row);
      const min = Math.min(last, next);
      const max = Math.max(last, next);
      for (let index = min; index <= max; ++index) {
        this._selectRow(this._rows[index]);
      }
    }

    this.dispatchSelectionChangedSoon();
  }

  _dispatchSelectionChanged() {
    const nonselected = [];
    const selected = [];
    for (const r of this._rows) {
      (r.isSelected() ? selected : nonselected).push(r.getTorrent());
    }

    const event = new Event('torrent-selection-changed');
    event.nonselected = nonselected;
    event.selected = selected;
    this.dispatchEvent(event);
  }

  /*--------------------------------------------
   *
   *  E V E N T   F U N C T I O N S
   *
   *--------------------------------------------*/

  static _createKeyShortcutFromKeyboardEvent(event_) {
    const a = [];
    if (event_.ctrlKey) {
      a.push('Control');
    }
    if (event_.altKey) {
      a.push('Alt');
    }
    if (event_.metaKey) {
      a.push('Meta');
    }
    if (event_.shitKey) {
      a.push('Shift');
    }
    a.push(event_.key.length === 1 ? event_.key.toUpperCase() : event_.key);
    return a.join('+');
  }

  // Process key events
  _keyDown(event_) {
    const { ctrlKey, keyCode, metaKey, shiftKey, target } = event_;

    // look for a shortcut
    const aria_keys = Transmission._createKeyShortcutFromKeyboardEvent(event_);
    const action = this.action_manager.getActionForShortcut(aria_keys);
    if (action) {
      event_.preventDefault();
      this.action_manager.click(action);
      return;
    }

    const esc_key = keyCode === 27; // esc key pressed
    if (esc_key && this.popup) {
      this.setCurrentPopup(null);
      event_.preventDefault();
      return;
    }

    const any_popup_active = document.querySelector('.popup:not(.hidden)');
    const is_input_focused = target.matches('input');
    const rows = this._rows;

    // Some shortcuts can only be used if the following conditions are met:
    // 1. when no input fields are focused
    // 2. when no other dialogs are visible
    // 3. when the meta or ctrl key isn't pressed (i.e. opening dev tools shouldn't trigger the info panel)
    if (!is_input_focused && !any_popup_active && !metaKey && !ctrlKey) {
      const shift_key = keyCode === 16; // shift key pressed
      const up_key = keyCode === 38; // up key pressed
      const dn_key = keyCode === 40; // down key pressed
      if ((up_key || dn_key) && rows.length > 0) {
        const last = this._indexOfLastTorrent();
        const anchor = this._shift_index;
        const min = 0;
        const max = rows.length - 1;
        let index = last;

        if (dn_key && index + 1 <= max) {
          ++index;
        } else if (up_key && index - 1 >= min) {
          --index;
        }

        const r = rows[index];

        if (anchor >= 0) {
          // user is extending the selection
          // with the shift + arrow keys...
          if (
            (anchor <= last && last < index) ||
            (anchor >= last && last > index)
          ) {
            this._selectRow(r);
          } else if (
            (anchor >= last && index > last) ||
            (anchor <= last && last > index)
          ) {
            this._deselectRow(rows[last]);
          }
        } else {
          if (shiftKey) {
            this._selectRange(r);
          } else {
            this._setSelectedRow(r);
          }
        }
        if (r) {
          this._last_torrent_clicked = r.getTorrentId();
          r.getElement().scrollIntoView();
          event_.preventDefault();
        }
      } else if (shift_key) {
        this._shift_index = this._indexOfLastTorrent();
      }
    }
  }

  _keyUp(event_) {
    if (event_.keyCode === 16) {
      // shift key pressed
      delete this._shift_index;
    }
  }

  static _dragenter(event_) {
    if (event_.dataTransfer && event_.dataTransfer.types) {
      const copy_types = new Set(['text/uri-list', 'text/plain']);
      if (event_.dataTransfer.types.some((type) => copy_types.has(type))) {
        event_.stopPropagation();
        event_.preventDefault();
        event_.dataTransfer.dropEffect = 'copy';
        return false;
      }
    } else if (event_.dataTransfer) {
      event_.dataTransfer.dropEffect = 'none';
    }
    return true;
  }

  static _isValidURL(string) {
    try {
      const url = new URL(string);
      return url ? true : false;
    } catch {
      return false;
    }
  }

  shouldAddedTorrentsStart() {
    return this.session_properties['start-added-torrents'];
  }

  _drop(event_) {
    const paused = !this.shouldAddedTorrentsStart();

    if (!event_.dataTransfer || !event_.dataTransfer.types) {
      return true;
    }

    const type = event_.data.Transfer.types
      .filter((t) => ['text/uri-list', 'text/plain'].contains(t))
      .pop();
    for (const uri of event_.dataTransfer
      .getData(type)
      .split('\n')
      .map((string) => string.trim())
      .filter((string) => Transmission._isValidURL(string))) {
      this.remote.addTorrentByUrl(uri, paused);
    }

    event_.preventDefault();
    return false;
  }

  // turn the periodic ajax session refresh on & off
  togglePeriodicSessionRefresh(enabled) {
    if (!enabled && this.sessionInterval) {
      clearInterval(this.sessionInterval);
      delete this.sessionInterval;
    }
    if (enabled) {
      this.loadDaemonPrefs();
      if (!this.sessionInterval) {
        const msec = 8000;
        this.sessionInterval = setInterval(
          this.loadDaemonPrefs.bind(this),
          msec
        );
      }
    }
  }

  _setFilterText(search) {
    this.filterText = search ? search.trim() : null;
    this.refilterAllSoon();
  }

  _onTorrentChanged(event_) {
    // update our dirty fields
    const tor = event_.currentTarget;
    this.dirtyTorrents.add(tor.getId());

    // enqueue ui refreshes
    this.refilterSoon();
  }

  updateTorrents(ids, fields) {
    this.remote.updateTorrents(ids, fields, (table, removed_ids) => {
      const needinfo = [];

      const keys = table.shift();
      const o = {};
      for (const row of table) {
        for (const [index, key] of keys.entries()) {
          o[key] = row[index];
        }
        const { id } = o;
        let t = this._torrents[id];
        if (t) {
          const needed = t.needsMetaData();
          t.refresh(o);
          if (needed && !t.needsMetaData()) {
            needinfo.push(id);
          }
        } else {
          t = this._torrents[id] = new _torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent(o);
          t.addEventListener('dataChanged', this._onTorrentChanged.bind(this));
          this.dirtyTorrents.add(id);
          // do we need more info for this torrent?
          if (!('name' in t.fields) || !('status' in t.fields)) {
            needinfo.push(id);
          }
        }
      }

      if (needinfo.length > 0) {
        // whee, new torrents! get their initial information.
        const more_fields = [
          'id',
          ..._torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.Fields.Metadata,
          ..._torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.Fields.Stats,
        ];
        this.updateTorrents(needinfo, more_fields);
        this.refilterSoon();
      }

      if (removed_ids) {
        this._deleteTorrents(removed_ids);
        this.refilterSoon();
      }
    });
  }
  /*
TODO: fix this when notifications get fixed
        t.notifyOnFieldChange('status', (newValue, oldValue) => {
          if (
            oldValue === Torrent._StatusDownload &&
            (newValue === Torrent._StatusSeed || newValue === Torrent._StatusSeedWait)
          ) {
            $(this).trigger('downloadComplete', [t]);
          } else if (
            oldValue === Torrent._StatusSeed &&
            newValue === Torrent._StatusStopped &&
            t.isFinished()
          ) {
            $(this).trigger('seedingComplete', [t]);
          } else {
            $(this).trigger('statusChange', [t]);
          }
        });
*/

  refreshTorrents() {
    const fields = ['id', ..._torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.Fields.Stats];
    this.updateTorrents('recently-active', fields);
  }

  _initializeTorrents() {
    const fields = ['id', ..._torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.Fields.Metadata, ..._torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.Fields.Stats];
    this.updateTorrents(null, fields);
  }

  _onRowClicked(event_) {
    const meta_key = event_.metaKey || event_.ctrlKey,
      { row } = event_.currentTarget;

    if (this.popup && this.popup.name !== 'inspector') {
      this.setCurrentPopup(null);
      return;
    }

    // handle the per-row pause/resume button
    if (event_.target.classList.contains('torrent-pauseresume-button')) {
      switch (event_.target.dataset.action) {
        case 'pause':
          this._stopTorrents([row.getTorrent()]);
          break;
        case 'resume':
          this._startTorrents([row.getTorrent()]);
          break;
        default:
          break;
      }
    }

    // Prevents click carrying to parent element
    // which deselects all on click
    event_.stopPropagation();

    // TODO: long-click should raise inspector
    if (event_.shiftKey) {
      this._selectRange(row);
      // Need to deselect any selected text
      window.focus();

      // Apple-Click, not selected
    } else if (!row.isSelected() && meta_key) {
      this._selectRow(row);

      // Regular Click, not selected
    } else if (!row.isSelected()) {
      this._setSelectedRow(row);

      // Apple-Click, selected
    } else if (row.isSelected() && meta_key) {
      this._deselectRow(row);

      // Regular Click, selected
    } else if (row.isSelected()) {
      this._setSelectedRow(row);
    }

    this._last_torrent_clicked = row.getTorrentId();
  }

  _deleteTorrents(ids) {
    if (ids && ids.length > 0) {
      for (const id of ids) {
        this.dirtyTorrents.add(id);
        delete this._torrents[id];
      }
      this.refilterSoon();
    }
  }

  _removeSelectedTorrents(trash) {
    const torrents = this.getSelectedTorrents();
    if (torrents.length > 0) {
      this.setCurrentPopup(
        new _remove_dialog_js__WEBPACK_IMPORTED_MODULE_10__.RemoveDialog({ remote: this.remote, torrents, trash })
      );
    }
  }

  _startSelectedTorrents(force) {
    this._startTorrents(this.getSelectedTorrents(), force);
  }

  _startTorrents(torrents, force) {
    this.remote.startTorrents(
      Transmission._getTorrentIds(torrents),
      force,
      this.refreshTorrents,
      this
    );
  }
  _verifyTorrents(torrents) {
    this.remote.verifyTorrents(
      Transmission._getTorrentIds(torrents),
      this.refreshTorrents,
      this
    );
  }

  _reannounceTorrents(torrents) {
    this.remote.reannounceTorrents(
      Transmission._getTorrentIds(torrents),
      this.refreshTorrents,
      this
    );
  }

  _stopTorrents(torrents) {
    this.remote.stopTorrents(
      Transmission._getTorrentIds(torrents),
      this.refreshTorrents,
      this
    );
  }
  changeFileCommand(torrentId, rowIndices, command) {
    this.remote.changeFileCommand(torrentId, rowIndices, command);
  }

  // Queue
  _moveTop() {
    this.remote.moveTorrentsToTop(
      this._getSelectedTorrentIds(),
      this.refreshTorrents,
      this
    );
  }
  _moveUp() {
    this.remote.moveTorrentsUp(
      this._getSelectedTorrentIds(),
      this.refreshTorrents,
      this
    );
  }
  _moveDown() {
    this.remote.moveTorrentsDown(
      this._getSelectedTorrentIds(),
      this.refreshTorrents,
      this
    );
  }
  _moveBottom() {
    this.remote.moveTorrentsToBottom(
      this._getSelectedTorrentIds(),
      this.refreshTorrents,
      this
    );
  }

  ///

  _updateGuiFromSession(o) {
    const [, version, checksum] = o.version.match(/(.*)\s\(([\da-f]+)\)/);
    this.version_info = {
      checksum,
      version,
    };

    const element = document.querySelector('#toolbar-overflow');
    element.classList.toggle('alt-speed-enabled', o[_remote_js__WEBPACK_IMPORTED_MODULE_9__.RPC._TurtleState]);
  }

  _updateStatusbar() {
    const fmt = _formatter_js__WEBPACK_IMPORTED_MODULE_2__.Formatter;
    const torrents = this._getAllTorrents();

    const u = torrents.reduce(
      (accumulator, tor) => accumulator + tor.getUploadSpeed(),
      0
    );
    const d = torrents.reduce(
      (accumulator, tor) => accumulator + tor.getDownloadSpeed(),
      0
    );
    const string = fmt.countString('Transfer', 'Transfers', this._rows.length);

    (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.setTextContent)(document.querySelector('#speed-up-label'), fmt.speedBps(u));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.setTextContent)(document.querySelector('#speed-dn-label'), fmt.speedBps(d));
    (0,_utils_js__WEBPACK_IMPORTED_MODULE_16__.setTextContent)(document.querySelector('#filter-count'), string);
  }

  _updateFilterSelect() {
    const trackers = this._getTrackers();
    const names = Object.keys(trackers).sort();

    // build the new html
    let string = '';
    string += !this.filterTracker
      ? '<option value="all" selected="selected">All</option>'
      : '<option value="all">All</option>';
    for (const name of names) {
      const o = trackers[name];
      string += `<option value="${o.domain}"`;
      if (trackers[name].domain === this.filterTracker) {
        string += ' selected="selected"';
      }
      string += `>${name}</option>`;
    }

    if (!this.filterTrackersStr || this.filterTrackersStr !== string) {
      this.filterTrackersStr = string;
      document.querySelector('#filter-tracker').innerHTML = string;
    }
  }

  /// FILTER

  sortRows(rows) {
    const torrents = rows.map((row) => row.getTorrent());
    const id2row = rows.reduce((accumulator, row) => {
      accumulator[row.getTorrent().getId()] = row;
      return accumulator;
    }, {});
    _torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.sortTorrents(
      torrents,
      this.prefs.sort_mode,
      this.prefs.sort_direction
    );
    for (const [index, tor] of torrents.entries()) {
      rows[index] = id2row[tor.getId()];
    }
  }

  _refilter(rebuildEverything) {
    const { sort_mode, sort_direction, filter_mode } = this.prefs;
    const filter_text = this.filterText;
    const filter_tracker = this.filterTracker;
    const renderer = this.torrentRenderer;
    const list = this.elements.torrent_list;

    const countRows = () => [...list.children].length;
    const countSelectedRows = () =>
      [...list.children].reduce(
        (n, e) => (n + e.classList.contains('selected') ? 1 : 0),
        0
      );
    const old_row_count = countRows();
    const old_sel_count = countSelectedRows();

    this._updateFilterSelect();

    clearTimeout(this.refilterTimer);
    delete this.refilterTimer;

    if (rebuildEverything) {
      while (list.firstChild) {
        list.firstChild.remove();
      }
      this._rows = [];
      this.dirtyTorrents = new Set(Object.keys(this._torrents));
    }

    // rows that overlap with dirtyTorrents need to be refiltered.
    // those that don't are 'clean' and don't need refiltering.
    const clean_rows = [];
    let dirty_rows = [];
    for (const row of this._rows) {
      if (this.dirtyTorrents.has(row.getTorrentId())) {
        dirty_rows.push(row);
      } else {
        clean_rows.push(row);
      }
    }

    // remove the dirty rows from the dom
    for (const row of dirty_rows) {
      row.getElement().remove();
    }

    // drop any dirty rows that don't pass the filter test
    const temporary = [];
    for (const row of dirty_rows) {
      const id = row.getTorrentId();
      const t = this._torrents[id];
      if (t && t.test(filter_mode, filter_text, filter_tracker)) {
        temporary.push(row);
      }
      this.dirtyTorrents.delete(id);
    }
    dirty_rows = temporary;

    // make new rows for dirty torrents that pass the filter test
    // but don't already have a row
    for (const id of this.dirtyTorrents.values()) {
      const t = this._torrents[id];
      if (t && t.test(filter_mode, filter_text, filter_tracker)) {
        const row = new _torrent_row_js__WEBPACK_IMPORTED_MODULE_15__.TorrentRow(renderer, this, t);
        const e = row.getElement();
        e.row = row;
        dirty_rows.push(row);
        e.addEventListener('click', this._onRowClicked.bind(this));
        e.addEventListener('dblclick', () =>
          this.action_manager.click('show-inspector')
        );
      }
    }

    // sort the dirty rows
    this.sortRows(dirty_rows);

    // now we have two sorted arrays of rows
    // and can do a simple two-way sorted merge.
    const rows = [];
    const cmax = clean_rows.length;
    const dmax = dirty_rows.length;
    const frag = document.createDocumentFragment();
    let ci = 0;
    let di = 0;
    while (ci !== cmax || di !== dmax) {
      let push_clean = null;
      if (ci === cmax) {
        push_clean = false;
      } else if (di === dmax) {
        push_clean = true;
      } else {
        const c = _torrent_js__WEBPACK_IMPORTED_MODULE_14__.Torrent.compareTorrents(
          clean_rows[ci].getTorrent(),
          dirty_rows[di].getTorrent(),
          sort_mode,
          sort_direction
        );
        push_clean = c < 0;
      }

      if (push_clean) {
        rows.push(clean_rows[ci++]);
      } else {
        const row = dirty_rows[di++];
        const e = row.getElement();

        if (ci !== cmax) {
          list.insertBefore(e, clean_rows[ci].getElement());
        } else {
          frag.append(e);
        }

        rows.push(row);
      }
    }
    list.append(frag);

    // update our implementation fields
    this._rows = rows;
    this.dirtyTorrents.clear();

    // set the odd/even property
    for (const [index, e] of rows.map((row) => row.getElement()).entries()) {
      const even = index % 2 === 0;
      e.classList.toggle('even', even);
      e.classList.toggle('odd', !even);
    }

    this._updateStatusbar();
    if (
      old_sel_count !== countSelectedRows() ||
      old_row_count !== countRows()
    ) {
      this.dispatchSelectionChangedSoon();
    }
  }

  setFilterTracker(domain) {
    const e = document.querySelector('#filter-tracker');
    e.value = domain ? Transmission._getReadableDomain(domain) : 'all';

    this.filterTracker = domain;
    this.refilterAllSoon();
  }

  _getTrackers() {
    const returnValue = {};

    for (const torrent of this._getAllTorrents()) {
      const names = new Set();

      for (const tracker of torrent.getTrackers()) {
        const { domain, name } = tracker;

        if (!returnValue[name]) {
          returnValue[name] = { count: 0, domain };
        }

        names.add(name);
      }

      for (const name of names.values()) {
        ++returnValue[name].count;
      }
    }

    return returnValue;
  }

  ///

  popupCloseListener(event_) {
    if (event_.target !== this.popup) {
      throw new Error(event_);
    }
    this.popup.removeEventListener('close', this.boundPopupCloseListener);
    delete this.popup;
  }

  setCurrentPopup(popup) {
    if (this.popup) {
      this.popup.close();
    }

    this.popup = popup;

    if (this.popup) {
      this.popup.addEventListener('close', this.boundPopupCloseListener);
    }
  }
}


/***/ }),

/***/ "./src/utils.js":
/*!**********************!*\
  !*** ./src/utils.js ***!
  \**********************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Utils": () => (/* binding */ Utils),
/* harmony export */   "createTabsContainer": () => (/* binding */ createTabsContainer),
/* harmony export */   "createDialogContainer": () => (/* binding */ createDialogContainer),
/* harmony export */   "makeUUID": () => (/* binding */ makeUUID),
/* harmony export */   "createSection": () => (/* binding */ createSection),
/* harmony export */   "createInfoSection": () => (/* binding */ createInfoSection),
/* harmony export */   "debounce": () => (/* binding */ debounce),
/* harmony export */   "deepEqual": () => (/* binding */ deepEqual),
/* harmony export */   "setEnabled": () => (/* binding */ setEnabled),
/* harmony export */   "setChecked": () => (/* binding */ setChecked),
/* harmony export */   "movePopup": () => (/* binding */ movePopup),
/* harmony export */   "OutsideClickListener": () => (/* binding */ OutsideClickListener),
/* harmony export */   "setTextContent": () => (/* binding */ setTextContent)
/* harmony export */ });
/* harmony import */ var lodash_isequal__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lodash.isequal */ "./node_modules/lodash.isequal/index.js");
/* harmony import */ var lodash_isequal__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(lodash_isequal__WEBPACK_IMPORTED_MODULE_0__);
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */



const Utils = {
  /** Given a numerator and denominator, return a ratio string */
  ratio(numerator, denominator) {
    let result = Math.floor((100 * numerator) / denominator) / 100;

    // check for special cases
    if (
      result === Number.POSITIVE_INFINITY ||
      result === Number.NEGATIVE_INFINITY
    ) {
      result = -2;
    } else if (Number.isNaN(result)) {
      result = -1;
    }

    return result;
  },

  /**
   * Checks to see if the content actually changed before poking the DOM.
   */
  setInnerHTML(e, html) {
    if (!e) {
      return;
    }

    /* innerHTML is listed as a string, but the browser seems to change it.
     * For example, "&infin;" gets changed to "∞" somewhere down the line.
     * So, let's use an arbitrary  different field to test our state... */
    if (e.currentHTML !== html) {
      e.currentHTML = html;
      e.innerHTML = html;
    }
  },
};

function createTabsContainer(id, tabs, callback) {
  const root = document.createElement('div');
  root.id = id;
  root.classList.add('tabs-container');

  const buttons = document.createElement('div');
  buttons.classList.add('tabs-buttons');
  root.append(buttons);

  const pages = document.createElement('div');
  pages.classList.add('tabs-pages');
  root.append(pages);

  const button_array = [];
  for (const [button_id, page] of tabs) {
    const button = document.createElement('button');
    button.id = button_id;
    button.classList.add('tabs-button');
    button.setAttribute('type', 'button');
    buttons.append(button);
    button_array.push(button);

    page.classList.add('hidden', 'tabs-page');
    pages.append(page);

    button.addEventListener('click', () => {
      for (const element of buttons.children) {
        element.classList.toggle('selected', element === button);
      }
      for (const element of pages.children) {
        element.classList.toggle('hidden', element !== page);
      }
      if (callback) {
        callback(page);
      }
    });
  }

  button_array[0].classList.add('selected');
  pages.children[0].classList.remove('hidden');

  return {
    buttons: button_array,
    root,
  };
}

function createDialogContainer(id) {
  const root = document.createElement('dialog');
  root.classList.add('dialog-container', 'popup', id);
  root.open = true;
  root.setAttribute('role', 'dialog');

  const win = document.createElement('div');
  win.classList.add('dialog-window');
  root.append(win);

  const logo = document.createElement('div');
  logo.classList.add('dialog-logo');
  win.append(logo);

  const heading = document.createElement('div');
  heading.classList.add('dialog-heading');
  win.append(heading);

  const message = document.createElement('div');
  message.classList.add('dialog-message');
  win.append(message);

  const workarea = document.createElement('div');
  workarea.classList.add('dialog-workarea');
  win.append(workarea);

  const buttons = document.createElement('div');
  buttons.classList.add('dialog-buttons');
  win.append(buttons);

  const bbegin = document.createElement('span');
  bbegin.classList.add('dialog-buttons-begin');
  buttons.append(bbegin);

  const dismiss = document.createElement('button');
  dismiss.classList.add('dialog-dismiss-button');
  dismiss.textContent = 'Cancel';
  buttons.append(dismiss);

  const confirm = document.createElement('button');
  confirm.textContent = 'OK';
  buttons.append(confirm);

  const bend = document.createElement('span');
  bend.classList.add('dialog-buttons-end');
  buttons.append(bend);

  return {
    confirm,
    dismiss,
    heading,
    message,
    root,
    workarea,
  };
}

function makeUUID() {
  // source: https://stackoverflow.com/a/2117523/6568470
  return ([1e7] + -1e3 + -4e3 + -8e3 + -1e11).replace(/[018]/g, (c) =>
    (
      c ^
      (crypto.getRandomValues(new Uint8Array(1))[0] & (15 >> (c / 4)))
    ).toString(16)
  );
}

function createSection(title) {
  const root = document.createElement('fieldset');
  root.classList.add('section');

  const legend = document.createElement('legend');
  legend.classList.add('title');
  legend.textContent = title;
  root.append(legend);

  const content = document.createElement('div');
  content.classList.add('content');
  root.append(content);

  return { content, root };
}

function createInfoSection(title, labels) {
  const children = [];
  const { root, content } = createSection(title);

  for (const label_text of labels) {
    const label_element = document.createElement('label');
    label_element.textContent = label_text;
    content.append(label_element);

    const item = document.createElement('div');
    item.id = makeUUID();
    content.append(item);
    label_element.setAttribute('for', item.id);
    children.push(item);
  }

  return { children, root };
}

function debounce(callback, wait = 100) {
  let timeout = null;
  return (...arguments_) => {
    if (!timeout) {
      timeout = setTimeout(() => {
        timeout = null;
        callback(arguments_);
      }, wait);
    }
  };
}

function deepEqual(a, b) {
  return lodash_isequal__WEBPACK_IMPORTED_MODULE_0___default()(a, b);
}

function setOrDeleteAttribute(element, attribute, b) {
  if (b) {
    element.setAttribute(attribute, true);
  } else {
    element.removeAttribute(attribute);
  }
}
function setEnabled(element, b) {
  setOrDeleteAttribute(element, 'disabled', !b);
}
function setChecked(element, b) {
  setOrDeleteAttribute(element, 'checked', b);
}

function getBestMenuPos(r, bounds) {
  let { x, y } = r;
  const { width, height } = r;

  if (x > bounds.x + bounds.width - width && x - width >= bounds.x) {
    x -= width;
  } else {
    x = Math.min(x, bounds.x + bounds.width - width);
  }

  if (y > bounds.y + bounds.height - height && y - height >= bounds.y) {
    y -= height;
  } else {
    y = Math.min(y, bounds.y + bounds.height - height);
  }

  return new DOMRect(x, y, width, height);
}

function movePopup(popup, x, y, boundingElement) {
  const initial_pos = new DOMRect(x, y, popup.clientWidth, popup.clientHeight);
  const clamped_pos = getBestMenuPos(
    initial_pos,
    boundingElement.getBoundingClientRect()
  );
  popup.style.left = `${clamped_pos.left}px`;
  popup.style.top = `${clamped_pos.top}px`;
}

class OutsideClickListener extends EventTarget {
  constructor(element) {
    super();
    this.listener = (event_) => {
      if (!element.contains(event_.target)) {
        this.dispatchEvent(new MouseEvent(event_.type, event_));
        event_.preventDefault();
      }
    };
    Object.seal(this);
    this.start();
  }
  start() {
    setTimeout(() => document.addEventListener('click', this.listener), 0);
  }
  stop() {
    document.removeEventListener('click', this.listener);
  }
}

function setTextContent(e, text) {
  if (e.textContent !== text) {
    e.textContent = text;
  }
}


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			loaded: false,
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/global */
/******/ 	(() => {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/node module decorator */
/******/ 	(() => {
/******/ 		__webpack_require__.nmd = (module) => {
/******/ 			module.paths = [];
/******/ 			if (!module.children) module.children = [];
/******/ 			return module;
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
/*!*********************!*\
  !*** ./src/main.js ***!
  \*********************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _action_manager_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./action-manager.js */ "./src/action-manager.js");
/* harmony import */ var _notifications_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./notifications.js */ "./src/notifications.js");
/* harmony import */ var _prefs_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./prefs.js */ "./src/prefs.js");
/* harmony import */ var _transmission_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./transmission.js */ "./src/transmission.js");
/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./utils.js */ "./src/utils.js");
/* harmony import */ var _style_transmission_app_scss__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../style/transmission-app.scss */ "./style/transmission-app.scss");
/**
 * @license
 *
 * This file Copyright (C) 2020 Mnemosyne LLC
 *
 * It may be used under the GNU GPL versions 2 or 3
 * or any future license endorsed by Mnemosyne LLC.
 */









function main() {
  const action_manager = new _action_manager_js__WEBPACK_IMPORTED_MODULE_0__.ActionManager();
  const prefs = new _prefs_js__WEBPACK_IMPORTED_MODULE_2__.Prefs();
  const notifications = new _notifications_js__WEBPACK_IMPORTED_MODULE_1__.Notifications(prefs);
  const transmission = new _transmission_js__WEBPACK_IMPORTED_MODULE_3__.Transmission(action_manager, notifications, prefs);

  const scroll_soon = (0,_utils_js__WEBPACK_IMPORTED_MODULE_4__.debounce)(() =>
    transmission.elements.torrent_list.scrollTo(0, 1)
  );
  window.addEventListener('load', scroll_soon);
  window.onorientationchange = scroll_soon;
}

document.addEventListener('DOMContentLoaded', main);

})();

/******/ })()
;
//# sourceMappingURL=transmission-app.js.map